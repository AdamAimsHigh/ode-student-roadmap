{
  "name": "ode-student-roadmap",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dry-run": "wrangler deploy --dry-run",
    "test:sync": "node scripts/test_sync_worker.mjs"
  },
  "dependencies": {
    "@playwright/test": "^1.60.0"
  },
  "devDependencies": {
    "wrangler": "^4.107.0"
  }
}
