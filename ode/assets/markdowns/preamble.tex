% ===========================================================================
% SHIM --- forwards to the master preamble at the root of the MATH tree.
%
% All shared style (package stack, palette, box system incl. historybox,
% header/footer, starred-section subsection reset) now lives in exactly one
% file:  C:\Users\Addst\OneDrive\Documents\MATH\preamble.tex
% Edit that file, not this one.
%
% The path below is relative to the directory these documents are COMPILED
% FROM (Differential Equations/), because TeX resolves \input against the compiler's
% working directory -- not against the location of this shim.
% ===========================================================================

\input{../preamble.tex}
