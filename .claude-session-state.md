MILESTONE 2026-07-04: feat(ui+worker): brand home link, SEO commit, Phase 4 lead-email notifications. UI (e24cadc): ode/index.html brand wordmark wrapped in <a href="/" class="brand-home-link" aria-label>; new main.css .brand-home-link rule clears underline + inherits color, hover/focus-visible opacity affordance (no competing global anchor rules exist). Pre-existing SEO working-tree changes (titles/meta, robots.txt + sitemap.xml Worker endpoints) committed separately first (bfce796). favicon.ico (1,150 B) + !/favicon.ico allowlist verified intact. PHASE 4 (380ad6a): sendLeadNotification() fires via ctx.waitUntil after the lead KV write — env.EMAIL send_email binding in wrangler.jsonc, to addstaples@gmail.com, from notifications@stapleseducation.com, replyTo = student, HTML-escaped body + CR/LF-stripped subject against header/markup injection; best-effort (env.EMAIL guard + internal try/catch) so lead capture never breaks; fetch/handleContact now accept ctx (tests pass ctx=undefined → floating self-catching promise); 22/22 tests pass. Deployed twice (46d4ff24 UI, 0fb7fffd Phase 4); post-deploy probes green both times (contact 403 / sync 401 / ode 200) — wrangler 4.107 has now bound the secret first-try on consecutive deploys. ACTIVATION PREREQ (email): wrangler email commands return Unauthorized 2036 — the stored OAuth token predates the Email Sending scopes. Human steps: `npx wrangler login` (fresh scopes), `npx wrangler email sending enable stapleseducation.com` (auto-adds SPF/DKIM, 5-15 min DNS), then one live form submit; until then sends are skipped/caught harmlessly (the binding itself deployed fine without onboarding). GEMINI AUDIT: #1 done (its router.js dynamic-banner claim is FALSE — no brand text anywhere in ode/js); #2 pending real stapler art from user; #3 SRI-on-Google-Fonts REJECTED — fonts.googleapis.com CSS is a rolling unpinnable endpoint (same class as gsi/Desmos), pinning would brick landing typography on Google's next rotation; #4 DELETE /api/sync server-side session revocation AGREED, strong Phase 5 candidate; #5 per-subject KV namespaces agreed directionally but premature before a second subject exists.

MILESTONE 2026-07-03: feat(security): Phase 3 — Cloudflare security hardening, Turnstile lead-generation API, and Web Analytics. SECRETS AUDIT: token.json / client_secrets.json / ODE-Manager-Local credentials.json confirmed never tracked (git ls-files clean, no index surgery needed); added client_secret*.json + *.pem to .gitignore and a credential hard-deny section at the END of .assetsignore (later rules win, so no future allowlist broadening can ever upload them). WORKER (src/worker.js): both API routes now sit behind an in-isolate fixed-window rate limiter keyed on CF-Connecting-IP (sync 60/min BEFORE auth so floods can't burn RS256 verifications, contact 5/min; 429 + Retry-After; 4096-bucket sweep bounds memory); POST /api/sync additionally enforces application/json (415), a declared Content-Length pre-read cap + actual-text post-read cap (413), and top-level-object shape (400) ahead of the existing whitelist sanitizer. NEW POST /api/contact: strict lead shape (name<=100/email<=254+regex/message<=4000, trimmed strings only), 16 KiB body cap, Turnstile server-side siteverify (env.TURNSTILE_SECRET_KEY Worker secret — provision with `npx wrangler secret put TURNSTILE_SECRET_KEY`, never in repo/vars; 503 unconfigured, 502 siteverify unreachable, 403 failed), verified leads stored as lead:<iso>_<rand> in ODE_PROGRESS_KV with 90-day TTL (list via `wrangler kv key list --prefix lead:`). FRONTEND (index.html + style.css): #contact inquiry section wired to /api/contact via protocol-guarded inline script (file:// disables the form and points at the booking link — SPA file:// invariant untouched), implicit-render .cf-turnstile widget (site key 0x4AAAAAADvT-1PlPvun0Xm_, dark theme), Cloudflare Web Analytics beacon (token 14c519923cd24565bcbd4b336c6bb429), nav Contact retargeted from tinyurl to #contact, new --color-success/--color-error (+ -soft) tokens driving .form-status states. TESTS: scripts/test_sync_worker.mjs extended 15 → 22 cases (contact 405/400-shapes/413/415/403-no-KV-write/200-lead-TTL-roundtrip + both 429 rate-limit edges, per-case unique IPs so buckets never bleed); all 22 pass; node --check clean; wrangler dry-run resolves all bindings (14.84 KiB bundle). Playwright verification over python http.server: form renders, both client validation branches fire with token colors, beacon+turnstile tags live; Turnstile 110200 + beacon CORS on 127.0.0.1 are environmental (domain-restricted keys — works on stapleseducation.com; optionally add localhost to the Turnstile widget's allowed hostnames for local dev). Blueprint Pillar 1 updated (hardened serverless companion + landing contact/analytics surface) per the Living Blueprint Rule. DEPLOY PREREQ: run the wrangler secret put command once before the contact form can go live.

MILESTONE 2026-07-03 (addendum): chore(deploy): Phase 3 pushed (9ca32da..5193bc2) and deployed live; TURNSTILE_SECRET_KEY provisioned and edge-verified. GOTCHA (secret/deploy race): a `wrangler secret put` (03:35 Secret Change deployment) followed ~2 min later by `wrangler deploy` produced version 14f5966f whose `wrangler versions view` metadata LISTED the secret but whose runtime env did NOT expose it — /api/contact 503'd "not configured" while /api/sync 401'd normally. A user re-put from a separate terminal printed a success line but created NO Secret Change deployment on this Worker (likely a different wrangler auth/account in that shell — always confirm with `npx wrangler deployments list | tail`, the put MUST stamp a "Secret Change" entry). FIX: simply redeploying from the repo shell (version cc922e47, then clean 1b2139ff) bound the existing secret into the runtime. RECURRENCE (2026-07-04, wrangler 4.106): the same secret-unbound 503 reappeared on a later routine deploy (30e97977) with no secret change involved; an immediate second deploy (da7112b3) rebound it. STANDING RULE: after every deploy, probe POST /api/contact with a forged turnstileToken and expect 403 (secret bound) not 503; if 503, deploy again. Consider upgrading wrangler. Diagnosed with a temporary 503-branch diag payload (undefined vs empty-string vs present — never the value), reverted before the final deploy; live worker byte-identical to committed src/worker.js. Final production sweep all green: contact forged-token 403 (full siteverify round-trip with the real secret), bad-shape 400, text/plain 415, GET 405; sync 401 unauth / 405 PUT; landing 200 with all 5 form/turnstile/beacon markers; /ode/ 200; token.json + client_secrets.json 404 (shield holds). Remaining human check: submit the live form once with a real Turnstile solve to confirm end-to-end lead capture, then read it back with `wrangler kv key list --prefix lead: --remote` (GOTCHA: wrangler v4 KV commands default to the LOCAL dev store; without --remote the production namespace looks empty). VERIFIED 2026-07-04: 9 real Turnstile-solved leads in production KV, newest record round-trips name/email/message/ip/submittedAt intact, 90-day TTLs correct.

MILESTONE 2026-07-02: security(sri): pin Subresource Integrity hashes on the immutable CDN surface. Appended integrity="sha384-..." + crossorigin="anonymous" to the four versioned CDN tags in ode/index.html (KaTeX 0.16.8 CSS/JS/auto-render via jsdelivr, Math.js 11.8.0 via cdnjs); hashes computed from the live CDN bytes and the three KaTeX values independently match the officially published 0.16.8 release hashes. Desmos v1.9/calculator.js and accounts.google.com/gsi/client are DELIBERATELY unpinned (rolling endpoints redeployed in place by their vendors — a hash would break checkpoints/sign-in on their next release; rationale documented inline and in blueprint Pillar 1). Both pinned CDNs serve Access-Control-Allow-Origin: *, so the SRI-mandated CORS fetches still succeed from the file:// null origin (Pillar 1 preserved). Verified in Playwright at 1440x900 and 375x812 (#practice-sets-2, 84 KaTeX nodes, no horizontal overflow): all four pinned assets executed (katex 0.16.8 / math 11.8.0 / Desmos globals live, KaTeX fontFamily applied), 4 integrity + 4 crossorigin attributes in DOM, zero SRI/integrity console failures — remaining console entries are environmental only (Desmos trial-key notice, GSI origin-not-allowed on localhost, favicon 404 on the throwaway static server). GOTCHA: wrangler dev reload-loops under OneDrive file churn (2860-file asset dir) and hangs requests; verified via python http.server instead. Blueprint Pillar 1 SRI contract added per the Living Blueprint Rule. Deployed live and edge-verified.

MILESTONE 2026-07-02: feat(auth): long-lived hybrid local/edge session persistence for student sign-in. Refactored the ode/js/state.js credential cache from transient sessionStorage to namespaced localStorage keys (ode_google_credential for the ~1 h GIS JWT, new ode_cloud_session for the Worker-issued session) so sign-in survives browser restarts on mobile and desktop; bootCloudSync now defensively reads either shape on parse and initializes the cloud loop immediately. src/worker.js /api/sync gained a second verification mode: a verified Google ID token is exchanged for an opaque odesess_* token (256 random bits) cached in ODE_PROGRESS_KV under session:<sha256(token)> with a 30-day expirationTtl (hash-only storage, so a KV dump is not replayable), and incoming GET/POST bearers starting odesess_ validate directly against that record before any progress:<sub> read/write; tampered/truncated/expired sessions 401 and the client falls back once to the Google credential (GIS data-auto_select="true", already present in ode/index.html from the prior sprint, re-establishes silently otherwise). The historical scratch 15-case suite is now COMMITTED as scripts/test_sync_worker.mjs (npm run test:sync — real RS256 tokens vs mocked JWKS + TTL-aware KV mock, extended with session mint/replay/tamper/expiry cases): all 15 pass. Dry-run resolved all three bindings; deployed live (version 263ecbeb) and edge-verified: no-auth 401, garbage bearer 401 "malformed token", forged odesess_ 401 "unknown or expired session", oversized session 401, PUT 405, /ode/ 200, fresh state.js serving. Blueprint Pillar 5 and CLOUD_SYNC_SETUP.md synchronized per the Living Blueprint Rule.

MILESTONE 2026-07-02: chore(deploy): production go-live on Cloudflare Workers. Bound the provisioned ODE_PROGRESS_KV namespace (id 8a39ec9fc9504b58a04a71765b20217b, discovered via wrangler kv namespace list after create reported it already existed) and the production Google Web client ID (422031597298-...apps.googleusercontent.com) into wrangler.jsonc vars and the ode/index.html g_id_onload data-client_id. Deployed via npm run deploy: 155 assets uploaded (exactly the 3 landing files plus the 152-file ode/ tree), worker version live at differential-equations-app.addstaples.workers.dev. Edge verification: / serves the landing page, /reviews.html 307-redirects to /reviews (auto-trailing-slash html_handling) and resolves 200, /ode/ serves the SPA, /api/sync returns 401 unauthenticated and 405 on PUT, and package.json / token.json / scripts all 404 (allowlist shielding confirmed in production). OPEN ITEM: stapleseducation.com apex does not respond (www does not resolve); the custom domain must be attached to the differential-equations-app Worker (dashboard: Worker -> Settings -> Domains and Routes -> Add -> Custom Domain) once the zone is active on the Cloudflare account.

MILESTONE 2026-07-01: refactor(worker): relocate sync endpoint to standardized src/worker.js entry point. Merged the JWT Web Crypto verification and ODE_PROGRESS_KV sync logic from functions/api/sync.js into src/worker.js (clean git mv, history preserved), dropped the Pages Functions dual-mode exports for a pure Worker module, and pointed wrangler.jsonc main at src/worker.js. Routing contract unchanged: GET/POST /api/sync runs the Google auth and KV save/load loops (405 on other methods), and every other request falls through seamlessly to env.ASSETS.fetch. Re-verified locally: node --check clean, the 15-case end-to-end suite (real RS256 tokens against a mocked JWKS: sanitization, tamper/audience/expiry rejection, KV round-trip) all passed against the new path, and the dry-run deploy resolves all three bindings. Updated CLOUD_SYNC_SETUP.md and blueprint Pillar 1 references; added .wrangler/ to .gitignore. Committed and pushed to origin/master. REMINDER: production deploy still requires the two TODO placeholders in wrangler.jsonc (ODE_PROGRESS_KV namespace id, GOOGLE_CLIENT_ID) plus the matching data-client_id in ode/index.html.

MILESTONE 2026-07-01: feat(sync): serverless progress synchronization loop with Google Sign-In. Built functions/api/sync.js, a dual-mode edge function (Worker main entry via wrangler.jsonc, plus exported Pages-style onRequestGet/onRequestPost handlers) that verifies Google Identity Services ID tokens entirely with the native Web Crypto API (RS256 signature against Google's live JWKS with warm-isolate caching, plus iss/aud/exp/nbf/sub claim checks) and reads/writes per-student records in the ODE_PROGRESS_KV namespace under progress:<sub> keys. POST payloads are capped at 128 KiB and sanitized against a strict whitelist of the five ode_* progress keys with per-entry caps. Injected the GIS SDK and a theme-token-styled sign-in/status control cluster into the ode/index.html header, and refactored ode/js/state.js with the cloud loop: defensive boot merge (unions for watched videos and quiz lists, local-wins for checkpoint details) plus a silent 2.5 s debounced background POST from every progress mutator, all protocol-guarded so file:// sessions stay untouched. Validation: 15-case end-to-end Node test (signed real RS256 tokens against a mocked JWKS; verified sanitization, tamper/audience/expiry rejection, KV round-trip) all passed; wrangler dev smoke test returned 401/405/404 on the auth and routing edges; node --check clean on both files; dry-run deploy resolves all three bindings. Deployment prerequisites (KV namespace id + Google OAuth client id) documented in CLOUD_SYNC_SETUP.md; blueprint Pillars 1 and 5 synchronized.

MILESTONE 2026-07-01: feat(arch): unify tutoring landing page root with nested /ode/ roadmap path. Restructured the repository into a multi-app web root for StaplesEducation.com: the tutoring business landing page (index.html, reviews.html, style.css) now serves at the site root, and the interactive ODE roadmap SPA was renamed from app/ to ode/ (clean git mv, history preserved) to serve at /ode/. Updated wrangler.jsonc assets.directory to "." and replaced the per-app ignore with a root .assetsignore deny-by-default allowlist (only index.html, reviews.html, style.css, and ode/ upload; node_modules/, scripts/, PDFs, and the Python toolchain shielded by construction, guarding the 25 MiB Cloudflare asset limit). Verified every internal reference in ode/index.html and ode/js is document-relative, so the SPA is mount-point agnostic (file:// and /ode/ both work). Wired the ODE Roadmap primary CTA (btn-primary) into both landing page navigation headers pointing to /ode/. Migrated every hardcoded app/ path to ode/ across scripts/*.py generators, all 52 .tex \input preamble lines, LATEX_STYLE_GUIDE.md, JS comments, and quizzes.json; zero app/ references remain in the toolchain. Synchronized WEBSITE_BLUEPRINT.md Pillars 1 and 2 (multi-app layout, Cloudflare deployment supersedes GitHub Pages, subpath mounting note). Wrangler dry-run verified the upload set is exactly the production surface.

MILESTONE 2026-06-29: feat: complete 10-sandbox interactive visualization library for Unit 2. Developed a comprehensive first-order linear sandbox architecture spanning integrating factor product engine builders, RK4 arc-length parameterized slope-field solution threaders, transient vs. steady-state decomposition morphers, conservative vector field exactness partial-derivative gauges, Bernoulli-to-linear transformation warps, and Picard non-Lipschitz existence/uniqueness branching engines. Concluded with applied physics engines for velocity drag limits, multi-chamber mixing ledger tracking, non-linear autonomous logistic growth thresholds, and an advanced Master First-Order Differential Matrix dashboard. Synchronized all 10 entries and u2_s1_ to u2_s10_ namespaces in WEBSITE_BLUEPRINT.md. Staged, committed as c525663, and pushed to origin/master.

MILESTONE 2026-06-29: feat: complete 10-sandbox interactive visualization library for Unit 1. Engineered a comprehensive suite of functional and analytical prerequisite tools spanning Euler limit explorations, complex rotational unit-circle vectors, log-linear scale warping observers, piecewise continuity/differentiability calculators, Taylor polynomial convergence engines, and domain-restricted inverse operator calculators. Concluded the track with a master Prerequisite Function Reference Matrix (u1_s10_) offering dynamic property cards and an interactive coordinate tangent inspector. Verified flawless Light and Dark theme execution under live theme MutationObservers and full file:// safety. Staged, committed as 2e6b7f4, and pushed to origin/master.

MILESTONE 2026-06-28: feat: deploy complete 9-sandbox interactive visualization library for Unit 0. Engineered three distinct asset clusters spanning core calculus mapping, arc-length trajectory tracers, eigenvalue topology mappers, and sub-stepped mass-spring physical simulators under the isSandbox path framework. Implemented global helper functions within app/js/router.js to minimize input boilerplate while keeping all parameters explicitly namespaced to prevent variable or DOM space collisions. Verified pixel-perfect layout symmetry and instantaneous color response under both Light and Dark theme palettes with zero runtime network dependencies. Local master branch fully synchronized to origin at Commit ce23cc7.

MILESTONE 2026-06-28: feat: deploy dynamic repo-wide HTML practice sets across all 19 units. Authored scripts/build_practice_data.py to programmatically extract and sanitize LaTeX mathematical derivations from unit data JSONs and the hand-authored Unit 12 reference exception into web-clean strings. Appended a backfilled 193-problem matrix to the global PRACTICE_DATA object within app/js/quiz-data.js, natively rendering interactive problem pools and hidden solution blocks client-side over the file:// protocol. Hardened renderPracticeMath in app/js/router.js with throwOnError:false and explicit math delimiter fallbacks to ensure robust CDN KaTeX typesetting protection across all desktop browsers. Verified all solutions stay locked behind the toggle interface and all binary PDF targets link correctly. Staged, committed as bafceee, and successfully pushed master to origin.

MILESTONE 2026-06-27: feat: initialize and build Unit 18 (Fourier Series and Partial Differential Equations) Full Asset Suite. Ingested .claude-session-state.md and PIPELINE_LEARNINGS.md; authored scripts/unit18_data.json from curriculum.json modules 18.1-18.6 (Fourier series/transform intuition, computing Fourier series, Fourier series for DEs / half-range expansions, separable PDEs, the Laplacian, heat & wave equations) following the unit17_data.json schema, with mastery quiz IDs um_18_1..um_18_30 confirmed in quizzes.json seeding the 10-problem practice set. Rendered all 3 assets via render_latex.py --unit 18 --mode cheat|practice|master (braced-title tcolorboxes via shared preamble; workpurple step tracking; accentorange conceptbox takeaways) and compiled via root-copy Tectonic --outdir app/assets/pdfs/ (Cheat 94.7 KiB, Practice 62.1 KiB, Reference 124.2 KiB, all exit 0; deep two-level TOC for 18.1-18.6 + Practice Problems + Complete Solutions verified). Capstone (18.6): full separation-of-variables heat-equation solution on a rod, u(x,0)=100, every coefficient integral expanded. Friction point: multi-clause display rows overflowed (144-275pt) because the renderer forces single non-breaking \[...\] rows; fixed in the JSON by splitting bodies into \begin{gathered}...\\...\end{gathered} and trimming prose steps, dropping all overfull below ~60pt (the Units 13-17 baseline). Title follows the data store ("Fourier Series and Partial Differential Equations") per the standing defer-to-curriculum rule. Root .tex copies and aux debris purged; 7 artifacts (1 JSON + 3 .tex + 3 .pdf) plus the two pipeline logs staged, committed, and pushed to origin/master.

MILESTONE 2026-06-26: feat: author and natively compile Unit 12 Cheat Sheet and Practice Set. Authored the two standalone Unit 12 assets to the shared 6-box harness (\input preamble.tex; braced title={#1} throughout; workpurple step tracking; accentorange conceptbox takeaways). Unit-12-Cheat-Sheet.tex = strictly Part I, 12 numbered sub-modules 12.1-12.12 via \setcounter{section}{12}, no TOC/no \clearpage (4pp). Unit-12-Practice-Set.tex = Part II (12 graded problems: definition, linearity/table, first shift, derivative-rule IVP, t-multiplication, inverse partial fractions + complete-the-square + repeated root, full five-step IVP, Heaviside shifting forward/inverse, Dirac-delta impulse-response capstone) + Part III complete solutions, \clearpage solely before Part III (3pp). Both native-compiled via TeX Live pdflatex (exit 0); aux debris (.aux/.log/.out/.toc) purged; staged & committed & pushed to origin/master.

MILESTONE 2026-06-26: Master Laplace Reference Guide complete (Unit 12 integration). Synthesized the full Unit 12 Laplace curriculum (modules 12.1-12.12 from curriculum-data.js) and the five decoupled Laplace reference PDFs (master TOC, Non-Zero-ICs/BVPs, Heaviside-Dirac-Impulse, Convolution-Duhamel-Volterra, Periodic/Fourier) into a single long-form guide app/assets/markdowns/Master-Laplace-Reference-Guide.tex, native-compiled via TeX Live pdflatex (double-passed for the two-level deep TOC, exit 0, 21pp > 20pp requirement). Six-Part hierarchy: I Foundations, II Toolkit, III Solving ODEs, IV Convolution & Systems, V Advanced (PDEs/Abel/resonance/Fourier bridge), VI Master Reference (transform + property tables, inversion playbook, completing-the-square patterns, initial/final-value theorems). Full 6-box harness via shared preamble (braced title={#1} throughout), all evaluation steps wrapped in \textcolor{workpurple}{...}, \clearpage before every Part. Aux debris (.aux/.log/.out/.toc) purged; staged & committed & pushed to origin/master.

MILESTONE 2026-06-26: Unit 11 (Mechanical Vibrations and Oscillators) decoupled assets authored and natively compiled via TeX Live pdflatex (Cheat-Sheet 4pp, Practice-Set 3pp, Reference-Guide 7pp, all exit 0); master guide double-passed for the two-level TOC (9 contentsline entries: 3 Parts + 6 sub-modules 11.1-11.6); box mapping conformed (orange formulaboxes for Hooke's law/damping discriminant/normal-mode formulas, green methodboxes for spring-mass IVP and normal-mode workflows, workpurple step tracking on all 10 solutions, crimson warningboxes for the angular-frequency-vs-frequency and resonance-vs-beats traps, accentorange-accented conceptbox takeaways); aux debris purged; committed & pushed to origin/master.

MILESTONE 2026-06-26: Unit 10 (Nonhomogeneous Equations and Forced Response) decoupled assets authored and natively compiled via TeX Live pdflatex (Cheat-Sheet 4pp, Practice-Set 3pp, Reference-Guide 7pp, all exit 0); master guide double-passed for the two-level TOC; box mapping conformed (orange formulaboxes for trial/VoP forms, green methodboxes for annihilator/Duhamel workflows, purple exampleboxes/workpurple step tracking, crimson warningboxes for resonance traps); aux debris purged; committed & pushed to origin/master.

MILESTONE 2026-06-23: Unit 7 and Unit 8 PDFs (Cheat-Sheets, Practice-Sets, Reference-Guides) natively compiled via TeX Live pdflatex (all exit 0), wide display line overflow anomalies completely resolved, aux tracking debris purged, working directories fully committed & split cleanly into atomic commits pushed to origin/master.

MILESTONE 2026-06-23: Unit 8 PDFs (Cheat-Sheet, Practice-Set, Reference-Guide) natively compiled via TeX Live pdflatex (all exit 0), wide display line overflow fixed using standard cases blocks, aux debris purged, verified pristine.

MILESTONE 2026-06-20: Unit 6 PDFs (Cheat-Sheet, Practice-Set, Reference-Guide) natively compiled via TeX Live 2026 pdflatex (all exit 0), aux debris purged, committed & pushed to origin/master.

＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
# SESSION LOG, 2026-06-20 (Unit 6 decoupled split-assets + master reference guide AUTHORED, NOT yet compiled)
＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝

NO DIRECTIVE-DISCREPANCY THIS PASS: the directive targeted "Unit 6: Multivariable
Calculus Foundations", which matches the data store exactly (curriculum-data.js:310).
(Contrast the prior Unit 9 session, where the directive's "Unit 6" label collided
with the second-order content and was retargeted to Unit 9.) Authored straight as
Unit 6.

Authored Unit 6 to the full 6-box style harness; all three sources `\input` the
shared `app/assets/markdowns/preamble.tex` (topicsbox passed
`[Unit 6 at a Glance: Core Sub-Topics]`). Content mined from the ten curriculum
sub-modules (6.1 Partial Derivatives, 6.2 Clairaut's Theorem on Mixed Partials,
6.3 Multivariable Chain Rule, 6.4 Line Integrals & FTLI, 6.5 Conservative Vector
Fields, 6.6 Potential Functions, 6.7 Level Curves, 6.8 Indifference Curves, 6.9
Contour Maps, 6.10 Exact & Inexact Differentials) and the Unit 6 QUIZ_DATA
micro-practice (um_6_1..um_6_20: partials, mixed partials/Clairaut, chain rule as
gradient dot velocity, FTLI, conservative test P_y=Q_x, potential functions,
level curves & gradient normality). Cheat Sheet consolidated into six numbered
sub-modules aligned to the directive's emphasis (partials, gradients, directional
derivatives, optimization) while still covering the exactness gateway:
  6.1 Partial Derivatives
  6.2 Higher-Order and Mixed Partials (Clairaut)
  6.3 The Gradient and Directional Derivatives
  6.4 The Multivariable Chain Rule
  6.5 Critical Points and Optimization (Hessian/second-derivative test)
  6.6 Level Curves, Gradients, and Exactness (bridge to Unit 7 exact equations)
- `Unit-6-Cheat-Sheet.tex`: strictly Part I, six numbered sub-modules 6.1-6.6 via
  `\setcounter{section}{6}` after the starred Part I `\section*`. No `\clearpage`.
- `Unit-6-Practice-Set.tex`: Part II (10 graded problems per the directive scope --
  partials, gradients, directional derivatives, optimization: f_x/f_y of x^2y+y^3 ->
  mixed partials of x^2y^3 (=6xy^2, Clairaut) -> grad(x^2+xy+y^2)=(4,5) at (1,2) ->
  directional deriv along (3,4) normalized = 32/5 = 6.4 -> steepest ascent dir (4,5),
  rate sqrt(41) -> chain rule z=x^2+y^2 on unit circle dz/dt=0 -> chain-rule tree
  z=x^2y, x=s+t, y=st => z_s=t(s+t)(3s+t) -> crit pt of x^2+y^2-4x-6y+13 => (2,3)
  min, value 0 -> x^2-y^2 saddle at origin -> capstone x^3-3x+y^2 => (1,0) min f=-2,
  (-1,0) saddle) + Part III (full solutions, every live step in
  `\textcolor{workpurple}{...}`). `\clearpage` ONLY before Part III.
- `Unit-6-Reference-Guide.tex`: unified master, Parts I/II/III with deep two-level
  `\tableofcontents` (Parts + 6.1-6.6) and `\clearpage` before Parts II and III.
- Box mapping per directive: definitions/standard/geometric forms (partial-deriv
  defn, Clairaut, gradient/directional-deriv, second-derivative test, level-curve &
  exact-differential test) -> orange formulabox; multivariable optimization
  workflow & chain-rule trees -> green methodbox; worked numeric partials steps ->
  purple examplebox; unit-vector-normalization, sign-boundary (D<0/D=0), and
  path-dependence/wrong-cross-partials traps -> crimson warningbox; final takeaway
  -> conceptbox accented with `\textcolor{accentorange}`. All box titles brace-
  protected via `title={#1}` in the shared preamble.
- Balance verified programmatically (no compile): begin/end 24/24, 4/4, 25/25;
  true display `\[`/`\]` 11/11, 14/14, 25/25 (the +2 apparent excess per file is
  the `\\[6pt]`/`\\[4pt]` title line-spacing, not display math); inline `$` even in
  all three (194, 182, 364).
- STATUS: source `.tex` written to disk only. NOT compiled this session per
  directive (compile deferred to clear context). Next step: pdflatex from repo
  root with `-output-directory=app/assets/pdfs` so the repo-root-relative
  `\input{app/assets/markdowns/preamble.tex}` path resolves; run RG twice for TOC.

---
---

MILESTONE 2026-06-19: Natively compiled Unit 9 Cheat Sheet, Practice Set, and Reference Guide to app/assets/pdfs/ via local TeX Live 2026 pdflatex (all exit 0, aux debris purged); staged, committed, and pushed to origin master.

＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
# SESSION LOG, 2026-06-19 (Unit 9 decoupled split-assets + master reference guide AUTHORED, NOT yet compiled)
＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝

DIRECTIVE-DISCREPANCY RESOLVED FIRST: the token-optimized directive asked for
"Unit 6: Second-Order Linear Equations", but the codebase data store says Unit 6
is "Multivariable Calculus Foundations" (curriculum-data.js:310) and the
second-order content (characteristic roots, Wronskian, Abel's identity, reduction
of order, Cauchy-Euler) is actually Unit 9 ("Second-Order Linear Equations, Theory
and Structure", curriculum-data.js:520). Surfaced via AskUserQuestion; user chose
to follow the data store and author as Unit 9. Filenames/titles/counter all retargeted
6 -> 9.

Authored Unit 9 to the full 6-box style harness; all three sources `\input` the
shared `app/assets/markdowns/preamble.tex` (topicsbox passed
`[Unit 9 at a Glance: Core Sub-Topics]`). Content mined from the eight curriculum
sub-modules (9.1 Introduction to Second-Order Equations, 9.2 Existence/Uniqueness/
Superposition/Independence, 9.3 Linear Independence & the Wronskian, 9.4 Abel's
Identity, 9.5 Reduction of Order, 9.6 Constant Coefficient Homogeneous Equations,
9.7 The Cauchy-Euler Equation, 9.8 Higher-Order Linear Equations) and the Unit 9
QUIZ_DATA micro-practice (second-order form/linearity/homogeneity, superposition,
Wronskian independence, variable-vs-constant coefficients).
- `Unit-9-Cheat-Sheet.tex`: strictly Part I, eight numbered sub-modules 9.1-9.8 via
  `\setcounter{section}{9}` after the starred Part I `\section*`. No `\clearpage`.
- `Unit-9-Practice-Set.tex`: Part II (10 graded problems across the three root cases
  -> real-distinct y''-5y'+6y=0 (r=2,3) -> repeated y''-6y'+9y=0 (r=3 double) ->
  complex y''+4y'+13y=0 (-2+/-3i) -> real-root IVP y''-y'-2y=0 -> Wronskian of
  e^2x,e^3x = e^5x -> Abel for y''+3y'+2y=0 -> reduction of order x^2y''-3xy'+4y=0
  y1=x^2 => y2=x^2 ln x -> Cauchy-Euler x^2y''+2xy'-6y=0 (m=2,-3) -> repeated IVP
  y''-4y'+4y=0 => (1-x)e^2x -> higher-order capstone y'''-3y''+3y'-y=0 =>
  (c1+c2 x+c3 x^2)e^x) + Part III (full solutions, every live step in
  `\textcolor{workpurple}{...}`). `\clearpage` ONLY before Part III.
- `Unit-9-Reference-Guide.tex`: unified master, Parts I/II/III with deep two-level
  `\tableofcontents` (Parts + 9.1-9.8) and `\clearpage` before Parts II and III.
- Box mapping per directive: three characteristic root cases & standard forms ->
  orange formulabox; reduction of order & Abel's identity workflows -> green
  methodbox; numeric worked steps -> purple examplebox; repeated-root/sign boundary
  trap -> crimson warningbox; final takeaway -> conceptbox accented with
  `\textcolor{accentorange}`.
- Balance verified programmatically (no compile): begin/end 17/17, 4/4, 19/19; true
  display `\[`/`\]` 11/11, 18/18, 29/29 (the +2 apparent excess per file is the
  `\\[6pt]`/`\\[4pt]` title line-spacing, not display math); inline `$` even in all
  three (160, 122, 282).
- STATUS: source `.tex` written to disk only. NOT compiled this session per
  directive (compile deferred to clear context). Next step: pdflatex from repo
  root with `-output-directory=app/assets/pdfs` so the repo-root-relative
  `\input{app/assets/markdowns/preamble.tex}` path resolves; run RG twice for TOC.

---
---

- MILESTONE 2026-06-19: Natively compiled Unit 5 trio via TeX Live 2026 pdflatex (Cheat Sheet 4pp, Practice Set 3pp, Reference Guide 8pp, all exit 0), purged aux debris, staged/committed/pushed the published PDFs to origin master.

＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
# SESSION LOG, 2026-06-19 (Unit 5 decoupled split-assets + master reference guide AUTHORED, NOT yet compiled)
＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝

Authored Unit 5 ("Numerical Methods") to the full 6-box style harness; all three
sources `\input` the shared `app/assets/markdowns/preamble.tex` (topicsbox passed
`[Unit 5 at a Glance: Core Sub-Topics]`). Content mined from the six curriculum
sub-modules (5.1 Euler's Method, 5.2 Improved & Modified Euler, 5.3 Heun's Method,
5.4 The RK4 Method, 5.5 The Runge-Kutta Family, 5.6 Adaptive Step Size & Method
Comparison) and the Unit 5 QUIZ_DATA micro-practice (Euler update/order, improved
& modified Euler two-slope steps, Heun predictor-corrector, RK4 4-stage blend).
- `Unit-5-Cheat-Sheet.tex`: strictly Part I, six numbered sub-modules 5.1-5.6 via
  `\setcounter{section}{5}` after the starred Part I `\section*`. No `\clearpage`.
- `Unit-5-Practice-Set.tex`: Part II (10 graded problems: Euler 1-step y'=t+y ->
  Euler 2nd step -> Euler error orders -> improved Euler -> modified/midpoint ->
  Heun predictor-corrector -> RK4 stage formulas -> RK4 step y'=y vs e^0.1 ->
  method-order ranking -> adaptive stepping -> RK4 capstone y'=t+y h=0.2 = 1.242805
  vs exact 2e^t-t-1) + Part III (full solutions, every live step in
  `\textcolor{workpurple}{...}`). `\clearpage` ONLY before Part III.
- `Unit-5-Reference-Guide.tex`: unified master, Parts I/II/III with deep two-level
  `\tableofcontents` (Parts + 5.1-5.6) and `\clearpage` before Parts II and III.
- Box mapping per directive: update rules/standard forms -> orange formulabox;
  Heun predictor-corrector & RK4 adaptive-error workflow -> green methodbox;
  numeric worked steps -> purple examplebox; equal-vs-unequal slope-weight trap
  and order-vs-step-count trap -> crimson warningbox; final takeaway ->
  conceptbox accented with `\textcolor{accentorange}`.
- Balance verified programmatically (no compile): begin/end 20/20, 5/5, 23/23;
  braces net 0 in all three; true display `\[`/`\]` 16/16, 20/20, 36/36 (the only
  apparent excess was the `\\[6pt]`/`\\[4pt]` title line-spacing, not display math).
- STATUS: source `.tex` written to disk only. NOT compiled this session per
  directive (compile deferred to clear context). Next step: pdflatex from repo
  root with `-output-directory=app/assets/pdfs` so the repo-root-relative
  `\input{app/assets/markdowns/preamble.tex}` path resolves; run RG twice for TOC.

---
---

- 2026-06-19: Natively compiled Unit 4 (Cheat-Sheet, Practice-Set, Reference-Guide) to PDF via local TeX Live 2026 pdflatex from repo root (shared preamble `\input` resolved); all three exit 0, aux debris purged, published to `app/assets/pdfs/`.

＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
# SESSION LOG, 2026-06-19 (Unit 4 decoupled split-assets + master reference guide AUTHORED, NOT yet compiled)
＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝

Authored Unit 4 ("Autonomous Equations, Equilibrium Solutions, and Stability")
to the full 6-box style harness, all three sources `\input` the shared
`app/assets/markdowns/preamble.tex` (topicsbox passed `[Unit 4 at a Glance: Core
Sub-Topics]`). Content mined from the six curriculum sub-modules (4.1 Equilibrium
Solutions & Stability, 4.2 Autonomous Equations & the Phase Line, 4.3 The
Logistic Equation, 4.4 Bernoulli Differential Equations, 4.5 Solving Bernoulli
Equations, 4.6 Applications of Bernoulli Equations) and the Unit 4 QUIZ_DATA
micro-practice (um_4_1 .. um_4_30).
- `Unit-4-Cheat-Sheet.tex`: strictly Part I, six numbered sub-modules 4.1-4.6 via
  `\setcounter{section}{4}` after the starred Part I `\section*`. No `\clearpage`.
- `Unit-4-Practice-Set.tex`: Part II (10 graded problems: equilibria roots ->
  f'-sign stability -> semi-stable sign test -> phase line -> logistic roots/
  classification -> logistic numeric -> Bernoulli n/v identify -> Bernoulli
  reduction -> full Bernoulli solve capstone y'+y=y^2 => y=1/(1+Ce^x)) + Part III
  (full solutions, every live step in `\textcolor{workpurple}{...}`). `\clearpage`
  ONLY before Part III.
- `Unit-4-Reference-Guide.tex`: unified master, Parts I/II/III with deep two-level
  `\tableofcontents` (Parts + 4.1-4.6) and `\clearpage` before Parts II and III.
- Box mapping per directive: stability states/standard forms -> orange formulabox;
  Bernoulli change-of-variable workflow -> green methodbox; coordinate-tracking
  worked examples -> purple examplebox; carrying-capacity/sign traps -> crimson
  warningbox; final takeaway -> conceptbox accented with `\textcolor{accentorange}`.
- STATUS: source `.tex` written to disk only. NOT compiled this session per
  directive (compile deferred to clear context). Next step: pdflatex from repo
  root with `-output-directory=app/assets/pdfs` so the repo-root-relative
  `\input{app/assets/markdowns/preamble.tex}` path resolves; run RG twice for TOC.

＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
# SESSION LOG, 2026-06-19 (Unit 3 decoupled split-assets, shared preamble, and master reference guide authored and compiled)
＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝

Authored Unit 3 ("Existence, Uniqueness, and Geometry") to the full 6-box style
harness. NEW this pass: a unified `app/assets/markdowns/preamble.tex` was
extracted from `Unit-2-Reference-Guide.tex` (documentclass -> palette ->
titlesec -> six boxes -> setlist/secnumdepth/tocdepth); all three Unit 3 sources
`\input` it instead of duplicating the package stack. `topicsbox` was
parameterized with an optional title argument in the shared preamble so one
preamble serves every unit (pass `[Unit 3 at a Glance: Core Sub-Topics]`).
Content mined from the six curriculum sub-modules (3.1 Slope Fields & Vector
Fields, 3.2 Phase Plane & Phase Portraits, 3.3 Existence & Uniqueness, 3.4 Fixed
Points & Iteration, 3.5 Banach Fixed Point Theorem, 3.6 Picard Iteration) and the
Unit 3 QUIZ_DATA micro-practice. NOTE: directive said "3.1-3.5" but the
curriculum has SIX modules; authored all 3.1-3.6 to stay faithful to content.
- `Unit-3-Cheat-Sheet.tex`: strictly Part I, six numbered sub-modules 3.1-3.6
  via `\setcounter{section}{3}` after the starred Part I `\section*`. No
  `\clearpage`. 5 pp.
- `Unit-3-Practice-Set.tex`: Part II (10 graded problems: slope field -> phase
  plane -> E&U test -> fixed points -> contraction -> Picard capstone) + Part III
  (full solutions, every live step in `\textcolor{workpurple}{...}`). `\clearpage`
  ONLY before Part III; opens straight on problems. 3 pp.
- `Unit-3-Reference-Guide.tex`: unified master, Parts I/II/III with deep two-level
  `\tableofcontents` (Parts + 3.1-3.6) and `\clearpage` before Parts II and III.
  8 pp.
- Compiled from repo root with `-output-directory=app/assets/pdfs` so the
  repo-root-relative `\input` path resolves; pdflatex TeX Live 2026, RG run twice
  for TOC, all exit 0, zero `^!` errors. Fixed one `]`-in-optional-title break in
  the 3.5 examplebox by brace-wrapping the title. Aux (.aux/.log/.out/.toc)
  purged. 7 assets staged: preamble.tex + 3 .tex + 3 .pdf.

＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
# SESSION LOG, 2026-06-19 (Unit 1 decoupled split-assets and master reference guide authored and compiled)
＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝

Branch: `master`. Session start HEAD: `649a2c4`. Pushed as
`feat: re-author and compile conformed decoupled assets and master reference guide for Unit 1`.

### 1.1 Three Unit 1 LaTeX sources authored (`app/assets/markdowns/`)
- Authored Unit 1 ("Foundations and Prerequisites") to the full 6-box style
  harness, matching the Unit 0/2 reference implementations. No `.tex` sources for
  Unit 1 existed before this pass (only a stale `Unit-1-Cheat-Sheet.pdf` and an
  untracked `Unit-1-Solutions.pdf`); the Cheat Sheet PDF was overwritten by the
  fresh conformed build. Content mined from the six curriculum sub-modules (1.1
  Calculus Review, 1.2 The Number e, 1.3 Euler's Formula and Identity, 1.4 Basic
  Definitions / Order, Degree, Linearity, 1.5 What It Means to Be a Solution, 1.6
  Forming DEs by Eliminating Constants) and the Unit 1 `QUIZ_DATA` micro-practice
  (13 video keys) plus the unit-mastery quiz. The micro-practice is rich with
  explicit equations, so the worked content uses the app's own items: chain/power/
  product rules, $\int e^{kx}dx$, $d/dt\,e^{kt}=ke^{kt}$, $dy/dt=y\to Ce^t$, Euler
  ($e^{i\pi}=-1$, $e^{i\pi/2}=i$, polar form), order vs degree vs linearity,
  solution verification ($y=e^{2x}$ on $y''-4y=0$, $y=\cos x$ on $y''+y=0$,
  $y=x^2$ on $xy'=2y$), and eliminating constants ($y=Cx\to y'=y/x$,
  $y=A\cos x+B\sin x\to y''+y=0$, circle $\to y'=-x/y$).
- `Unit-1-Cheat-Sheet.tex`: strictly Part I. Title block, blue `topicsbox`, six
  numbered sub-modules 1.1-1.6 with `\setcounter{section}{1}` directly after the
  starred Part I `\section*` so subsections auto-number 1.1-1.6. No `\clearpage`.
- `Unit-1-Practice-Set.tex`: strictly Part II (10 progressively graded problems,
  calculus review -> e -> Euler -> classification -> verification -> capstone
  eliminate-constants) and Part III (full solutions, every live step wrapped in
  `\textcolor{workpurple}{...}`). Conditional `\clearpage` ONLY before Part III,
  so it opens straight on the problems with no leading ghost page.
- `Unit-1-Reference-Guide.tex`: the unified master, Parts I/II/III in sequence
  with the deep two-level `\tableofcontents` (Parts + sub-modules 1.1-1.6) and
  `\clearpage` boundaries before Part II and Part III.

### 1.2 Style harness conformance
- Identical preamble across all three: same package set, the 5-color HTML hex map
  (`headerblue` #1C569E, `accentgreen` #2E7D32, `accentorange` #E27A1E,
  `workpurple` #A020F0, `warnred` #C0392B), `titlesec` headerblue subsections, and
  all six unbreakable `title={#1}`-protected boxes. Content mapped per the guard-
  rails: definitions/rules into orange `formulabox`, the verify-a-solution and
  eliminate-constants algorithms into green `methodbox`es, worked examples into
  purple `examplebox`es, three core traps (missing inner derivative / lost $+C$,
  order-is-not-degree, "works at one point") into crimson `warningbox`es, and each
  document closes in a thin orange `conceptbox` with `\textcolor{accentorange}`
  highlighted terms. Structural `---` em-dashes preserved in all prose.

### 1.3 Compile + verification
- Local pdflatex, two passes each, all exit 0, zero `^!` errors: Cheat Sheet 5 pp,
  Practice Set 3 pp, Reference Guide 8 pp, all `%PDF` header. `pdftotext` confirmed
  the two-level TOC (Part I p1, 1.1-1.6, Part II p6, Part III p7), clean "1.1-1.6"
  headings with all three warning boxes and the takeaway in the Cheat Sheet, and
  the Practice Set opening directly on Part II (no Part I, no leading blank).
  Debris (.aux/.log/.out/.toc) purged from disk.

### 1.4 Immediate next actions
- Units 0, 1, and 2 now all carry the full conformed three-asset set, so their
  Practice-Set and Reference-Guide app links resolve. Backfill Units 3+ the same
  way. Author `PRACTICE_DATA` for Units 0 and 1. Pre-existing UNRELATED working-
  tree changes (reorganized PDFs, `ODE-Manager-Local` notes) remain unstaged.

---
---

＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
# SESSION LOG, 2026-06-19 (Unit 0 decoupled split-assets and master reference guide authored and compiled)
＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝

Branch: `master`. Session start HEAD: `3483991`. Pushed as
`feat: author and natively compile decoupled assets and master reference guide for Unit 0`.

### 1.1 Three new Unit 0 LaTeX sources (`app/assets/markdowns/`)
- Authored Unit 0 ("What are Differential Equations") to the full 6-box style
  harness, mirroring the Unit 2 reference implementation. Content mined from the
  four curriculum sub-modules (0.1 What are Differential Equations, 0.2 How
  Differential Equations Work, 0.3 A Tourist's Guide, 0.4 Why You Are Learning
  Them) and the Unit 0 `QUIZ_DATA` micro-practice (8 video keys) plus the 30-item
  unit-mastery quiz. Unit 0 is conceptual, so the math content is reading/order/
  linearity/ODE-vs-PDE classification, solution verification, initial conditions,
  and the standard exponential models (growth, decay, cooling, Newton's 2nd law).
- `Unit-0-Cheat-Sheet.tex`: strictly Part I. Title block, blue `topicsbox`, four
  numbered sub-modules 0.1-0.4. Per the directive, `\setcounter{section}{0}` is
  applied right after the starred Part I `\section*`, so the subsections
  auto-number 0.1-0.4 (not 0.1 = first because the counter starts at 0 already;
  empirically confirmed clean in the PDF). No internal `\clearpage`.
- `Unit-0-Practice-Set.tex`: strictly Part II (10 progressively graded problems:
  verify-solution, order, linear/nonlinear, IVP application of the constant,
  second-order verify, ODE/PDE, a `y = Ce^x - x - 1` verify+IVP, and two physical
  models) and Part III (complete solutions, every live step wrapped in
  `\textcolor{workpurple}{...}`). Conditional `\clearpage` ONLY before Part III, so
  the document opens directly on the problems with no leading ghost page.
- `Unit-0-Reference-Guide.tex`: the unified master compilation, Parts I/II/III in
  sequence with the deep two-level `\tableofcontents` (Parts + sub-modules 0.1-0.4)
  and `\clearpage` markers before Part II and Part III for clean print bounds.

### 1.2 Style harness conformance
- Identical preamble across all three: same package set, the 5-color HTML hex map
  (`headerblue` #1C569E, `accentgreen` #2E7D32, `accentorange` #E27A1E,
  `workpurple` #A020F0, `warnred` #C0392B), `titlesec` headerblue subsections, and
  all six unbreakable `title={#1}`-protected boxes. Content mapped per the guard-
  rails: core definitions/rules into orange `formulabox`, the verify-a-solution
  algorithm into a green `methodbox`, worked examples into purple `examplebox`,
  two core pitfalls (unknown-is-a-function, order-is-not-degree) into crimson
  `warningbox`, and each document closes in a thin-bordered orange `conceptbox`
  with `\textcolor{accentorange}` highlighted terms. Structural `---` em-dashes
  preserved in all prose.

### 1.3 Compile + verification
- Local pdflatex, two passes each, all exit 0, zero `^!` errors: Cheat Sheet 4 pp,
  Practice Set 3 pp, Reference Guide 7 pp, all `%PDF` magic header. `pdftotext`
  confirmed: the two-level TOC lists Part I (p1), 0.1-0.4, Part II (p5), Part III
  (p6); the Cheat Sheet renders clean "0.1-0.4" headings with both warning boxes
  and the takeaway; the Practice Set opens directly on Part II (no Part I, no
  leading blank). Debris (.aux/.log/.out/.toc) purged from disk.

### 1.4 Immediate next actions
- The Unit 0 Practice-Set and Reference-Guide PDFs now exist, so those two app
  links resolve for Unit 0 alongside Unit 2. Backfill the remaining units the same
  way. Author `PRACTICE_DATA` for Units 0 and 2. Pre-existing UNRELATED working-
  tree changes (reorganized PDFs, `ODE-Manager-Local` notes) remain unstaged.

---
---

＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
# SESSION LOG, 2026-06-19 (Crimson warningbox added to the box system and applied to Unit 2 pitfalls)
＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝

Branch: `master`. Session start HEAD: `d2b233c`. Pushed as
`style/feat: integrate crimson warning boxes, enforce headerblue subsection typography, and finalize dynamic SPA link routing`.

### 1.1 Scope note (overlap with prior commit d2b233c)
- This directive re-issued two items already shipped in `d2b233c`: the `titlesec` headerblue subsection
  rule (all three preambles) and the router hrefs (`renderPracticeSetDetail` -> Unit-N-Practice-Set.pdf,
  `renderUnitDetail` footer -> Unit-N-Reference-Guide.pdf, both target=_blank rel=noopener no download).
  Verified still correct; `app/js/router.js` needed NO change this pass and is not in this commit.
  The genuinely new work was the crimson warningbox. titlesec was NOT re-injected (avoids a duplicate).

### 1.2 Crimson warningbox (`warnred` + box, all three sources + style guide)
- Added `\definecolor{warnred}{HTML}{C0392B}` to the palette and a 6th unbreakable box
  `\newtcolorbox{warningbox}[1][Common Pitfall]{...}` (colback red!3!white, colframe warnred, white title,
  3pt crimson west border, sharp corners, title={#1}) to all three preambles.
- Wrapped the Part I 2.1 `\noindent\textbf{Pitfall.}` paragraph in
  `\begin{warningbox}[Common Pitfall: Lost Equilibrium Solutions] ... \end{warningbox}` in the Cheat
  Sheet and Reference Guide (the Practice Set has no Part I, so no pitfall to wrap; it still carries the
  box definition for an identical preamble).
- Style guide: added the `warnred` palette row + definecolor, the warningbox code listing + table row,
  and bumped the box-system count from five to six.

### 1.3 Compile + verification
- Local pdflatex, two passes each, all exit 0, zero errors: Cheat Sheet 5 pp, Practice Set 4 pp,
  Reference Guide 9 pp. `pdftotext` confirmed the warningbox title renders in the Cheat Sheet and
  Reference Guide (1 each) and is absent from the Practice Set (0). Debris (.aux/.log/.out/.toc) purged.
- `node --check app/js/router.js`: clean (unchanged). Acrobat lock cleared before compiling, as before.

### 1.4 Immediate next actions
- Author `PRACTICE_DATA` for Unit 2; apply this style guide (now 6 boxes) to future units. Only Unit 2
  has its Practice-Set / Reference-Guide PDFs compiled, so those two app links 404 for other units
  until backfilled. Pre-existing UNRELATED working-tree changes remain unstaged, as before.

---
---

＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
# SESSION LOG, 2026-06-19 (Headerblue subsection typography + Practice-Set/Reference-Guide link routing)
＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝

Branch: `master`. Session start HEAD: `1cd3d60`. Pushed as
`style/feat: apply headerblue subsection typography formatting and complete dynamic app link routing loops`.

### 1.1 Subsection typography (all three Unit 2 sources + style guide)
- Added `\usepackage{titlesec}` and, after the palette, the rule
  `\titleformat{\subsection}{\normalfont\large\bfseries\color{headerblue}}{\thesubsection}{1em}{}`
  to all three preambles, so the numbered sub-module headings (2.1-2.5) render in headerblue, large,
  bold. `titlesec` was missing from the minimal TeX Live install; pulled it via tlmgr.
- Style guide: added `\usepackage{titlesec}` to the documented package list (Section 1) and a new
  Section 1.2 "Subsection Typography" documenting the rule as a permanent requirement.

### 1.2 App link routing (`app/js/router.js`)
- `renderPracticeSetDetail`: the action button now targets the dedicated practice-set binary,
  `href = "assets/pdfs/Unit-" + unitIndex + "-Practice-Set.pdf"`, with the label updated to
  "Open Unit N Practice Set (PDF)" (was the cheat sheet). Removed the now-orphaned
  `const materials` in this function.
- `renderUnitDetail` master-guide footer: the primary reference button now targets
  `href = "assets/pdfs/Unit-" + unitIndex + "-Reference-Guide.pdf"` (was the cheat sheet file). The
  `if (materials && materials.file)` guard is retained. Both anchors keep `target="_blank"` +
  `rel="noopener"` and carry NO `download` attribute (verified: zero `.download` assignments in file).

### 1.3 Compile + verification
- Local pdflatex, two passes each, all exit 0, zero errors: Cheat Sheet 5 pp, Practice Set 4 pp,
  Reference Guide 9 pp. Debris (.aux/.log/.out/.toc) purged.
- `node --check app/js/router.js`: clean. New hrefs and attribute state grep-verified. The router copy
  changes carry no ampersands or em-dashes.
- Acrobat lock cleared before compiling, as before.

### 1.4 Routing note for content backfill
- The Unit 2 Practice-Set and Reference-Guide PDFs exist, so those two links resolve for Unit 2. For
  all other units these binaries are not compiled yet, so the Practice-Set button and the master-guide
  footer link will 404 until each unit's PDFs are produced. Pre-existing UNRELATED working-tree
  changes remain unstaged, as before.

---
---

＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
# SESSION LOG, 2026-06-19 (Two-level subsection TOC nesting and vibrant-purple execution color)
＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝

Branch: `master`. Session start HEAD: `d3c585f`. Pushed as
`style/refactor: drive deep subsection TOC nesting and update math text execution to vibrant purple`.

### 1.1 Style guide (`app/assets/markdowns/LATEX_STYLE_GUIDE.md`)
- TOC section rewritten: master guides set `\setcounter{secnumdepth}{2}` and `\setcounter{tocdepth}{2}`,
  the index is two levels deep (Parts + sub-modules 2.1-2.5), sub-modules are numbered `\subsection`
  (not starred), and the section counter is forced to the unit number so they read 2.1..2.5 rather
  than 0.1..0.5. Palette: `workpurple` updated `#744AA8` -> `#A020F0` (vibrant). Typography rule
  updated: live solution tracking now uses `\textcolor{workpurple}` (the vibrant purple), superseding
  the old `\textcolor{purple}` rule, unifying the example-box frame and the tracking text on one color.

### 1.2 Unit 2 sources (all three preambles, identical)
- `workpurple` definecolor -> `#A020F0`; added `\setcounter{secnumdepth}{2}` + `\setcounter{tocdepth}{2}`
  after `\setlist`. (Practice Set carries the counters too for an identical preamble, though it has no
  subsections/TOC.)

### 1.3 Structural refactor (Cheat Sheet + Reference Guide)
- Converted the five Part I `\subsection*{2.X Title}` to numbered `\subsection{Title}` (manual "2.X"
  text prefix dropped) and added `\setcounter{section}{2}` right after the Part I `\section*` so the
  auto-numbers come out as the unit modules 2.1-2.5. EMPIRICALLY verified first: the literal directive
  form (`\subsection{2.1 ...}` under a starred Part) renders the broken double prefix "0.1 2.1 ..."
  because the starred section leaves the section counter at 0; forcing the counter to 2 + dropping the
  manual prefix yields clean "2.1 Separable Equations" in both heading and TOC. This is the exact
  mechanism flagged in the prior clarifying question and accepted via the directive.

### 1.4 Vibrant-purple execution color
- The directive's objective ("update the solution tracking text color to vibrant purple") could not be
  met by only changing the `workpurple` value, because the live tracking math used built-in
  `\textcolor{purple}`, not `workpurple`. Switched all 44 tracking wrappers in each of Part III
  (Reference Guide + Practice Set) from `\textcolor{purple}` to `\textcolor{workpurple}` (= `#A020F0`).
  The conceptbox `\textcolor{accentorange}` accents (3 per file) were left intact.

### 1.5 Compile + verification
- Local pdflatex, two passes each, all exit 0, zero `^!` errors: Cheat Sheet 5 pp, Practice Set 4 pp,
  Reference Guide 9 pp. `pdftotext` confirmed the two-level TOC (Part I, then 2.1-2.5, then Part II p6,
  Part III p7) and clean "2.1 Separable Equations" headings. Debris (.aux/.log/.out/.toc) purged.
- Constraint: no web-app UI code files (app/js, app/css, index.html, app/data) touched; zero raw
  ampersands in the staged .tex/.md. (Acrobat lock cleared before compiling, as before.)

### 1.6 Immediate next actions
- Author `PRACTICE_DATA` for Unit 2; apply this style guide to future units. No UI link targets the
  Practice Set / Reference Guide PDFs yet.
- Pre-existing UNRELATED working-tree changes (reorganized PDFs, `ODE-Manager-Local` notes, untracked
  `app/assets/pdfs/Unit-1-Solutions.pdf`) remain unstaged, as before.

---
---

＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
# SESSION LOG, 2026-06-19 (Unified LaTeX style guide established and Unit 2 assets overhauled to it)
＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝

Branch: `master`. Session start HEAD: `97f5c6c`. Pushed this session as
`style/feat: establish unified LaTeX style guide and overhaul Unit 2 assets`.

### 1.1 New style guide (`app/assets/markdowns/LATEX_STYLE_GUIDE.md`)
- Authored the definitive project-scoped spec: preamble/package set, the 4-color HTML palette
  (`headerblue` #1C569E, `accentgreen` #2E7D32, `accentorange` #E27A1E, `workpurple` #744AA8),
  hyperref mapped to `headerblue`, the master-guide `\tableofcontents` rule, the 5 unbreakable
  `title={#1}`-protected boxes, inline typography (`\textcolor{purple}` retained for live solution
  tracking; conceptbox conclusions use `\textcolor{accentorange}`), and the Part II/III `\clearpage`
  print-split rule (with the standalone-Practice-Set exception: no break before Part II).

### 1.2 Preamble overhaul (all three Unit 2 sources, identical)
- Replaced the old 3-box block with: 4 `\definecolor{...}{HTML}{...}` palette entries, hyperref ->
  `headerblue`, `\tcbuselibrary{skins}` (dropped the now-unused `breakable` library), and the 5 boxes
  remapped to named colors: topicsbox (headerblue), formulabox (accentorange), NEW methodbox
  (accentgreen, green algorithm block), examplebox (workpurple west border), NEW conceptbox
  (accentorange thin 0.3pt border). All unbreakable, all `title={#1}` where parameterized.

### 1.3 Structural refactor
- Reference Guide: `\tableofcontents` beneath the title block; each starred Part registers a crisp TOC
  line via `\phantomsection\addcontentsline{toc}{section}{...}`. Renders "Contents" + Part I (p1),
  Part II (p6), Part III (p7).
- methodbox: a green "Algorithm: Solving a First-Order Linear Equation" 4-step block injected in
  Part I 2.2 (Cheat Sheet + Reference Guide). The Practice Set lays out no standalone algorithm, so it
  receives none (the directive's conditional not met there).
- conceptbox: each document's raw `\begin{center}\textit{End of...}` line replaced with a
  `conceptbox` "Unit 2 Key Takeaway" carrying orange `\textcolor{accentorange}` accents. All Part III
  `\textcolor{purple}` live-tracking math left verbatim.

### 1.4 Compile + verification
- Local pdflatex, two passes each (TOC needs the second), all exit 0, zero `^!` errors:
  Cheat Sheet 5 pp, Practice Set 4 pp, Reference Guide 9 pp. `pdftotext` confirmed the 3-entry TOC,
  the methodbox titles, and the conceptbox in the right files. Debris (.aux/.log/.out/.toc) purged.
- Format constraint: no web-app UI code files (app/js, app/css, index.html, app/data) were touched
  this pass; staged diff carries zero raw ampersands. The `---` em dashes live only in the authored
  .tex/.md content (house style), never in app UI copy.
- NOTE: Adobe Acrobat again locked the PDFs at first; cleared before compiling. Keep the Unit 2 PDFs
  closed during compile passes.

### 1.5 Immediate next actions
- Apply this style guide to future units. Author `PRACTICE_DATA` for Unit 2. No UI link points at the
  Practice Set / Reference Guide PDFs yet (master-guide link still targets `Unit-[N]-Cheat-Sheet.pdf`).
- Pre-existing UNRELATED working-tree changes (reorganized PDFs, `ODE-Manager-Local` notes, untracked
  `app/assets/pdfs/Unit-1-Solutions.pdf`) remain unstaged, as before.

---
---

＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
# SESSION LOG, 2026-06-18 (Local TeX Live compile of the three Unit 2 PDFs into the asset directory)
＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝

Branch: `master`. Session start HEAD: `d4fe1c8`. A local TeX Live 2026 engine is now installed at
`C:/texlive/2026` (`pdflatex` is `/c/texlive/2026/bin/windows/pdflatex`), reversing the prior
"no local LaTeX engine" constraint recorded in every earlier log.

### 1.1 Engine bring-up and missing-package installs
- The base install was minimal: it lacked the `tcolorbox` dependency chain. `tlmgr` (run as
  `C:\texlive\2026\bin\windows\tlmgr.bat`, not on PATH) pulled the missing packages from CTAN in this
  order as the compile cascade surfaced them: `xcolor`, `tcolorbox`, `pgf`, `environ`, `fp`,
  `trimspaces`, `enumitem` (then `tikzfill`, then `pdfcol`, then `listings`/`listingsutf8`).
  `geometry`, `hyperref`, `tools`, `amsfonts` were already present.

### 1.2 Source bug found and fixed (`title=#1` -> `title={#1}`)
- First clean-package compile still threw 9 "Missing $ inserted" errors per file. Root cause isolated
  with minimal reproductions: the custom box definitions used `title=#1` (unbraced). When a box title
  carries inline math containing an `=` (e.g. `Solve $\frac{dy}{dx} = 6x^{2}y$`), tcolorbox's pgfkeys
  parser treats that `=` as a key/value separator and tears the math apart, so `$` never opens math
  mode. Confirmed the fix is to brace the value in the DEFINITION only: `title={#1}` makes `#1` arrive
  as one group, immune to re-parsing, with no edit needed at any call site (verified even with titles
  carrying both `=` and `,`).
- Applied to the `formulabox` and `examplebox` definitions in all three sources (6 lines total). The
  `topicsbox` literal title has no math/`=` and was left as-is. These source fixes are committed
  alongside the PDFs so the repo can reproduce the binaries (a deliberate, minimal expansion of the
  directive's PDF-only stage list, since the committed sources otherwise still held the bug).

### 1.3 Clean build + verification
- Two `pdflatex` passes per file, all exit 0, zero `^!` errors: `Unit-2-Cheat-Sheet.pdf` (4 pp,
  176 KB), `Unit-2-Practice-Set.pdf` (4 pp, 154 KB), `Unit-2-Reference-Guide.pdf` (7 pp, 189 KB).
  Each begins with the `%PDF` magic header. Build debris (`.aux`, `.log`, `.out`) removed from
  `app/assets/pdfs/`; only the three PDFs, `Unit-1-*.pdf`, and `.gitkeep` remain.
- Committed `assets: compile and publish Unit 2 split-asset PDFs and master reference guide` (the 3
  PDFs + the 3 `title={#1}` source fixes) and pushed to `origin master`.

### 1.4 Immediate next actions
- The unit detail master guide link resolves to `Unit-[N]-Cheat-Sheet.pdf`; the Unit 2 Practice Set
  and Reference Guide PDFs now exist but no UI link points at them yet (wire if separate targets are
  wanted). Author `PRACTICE_DATA` for Unit 2. Compile remaining units the same way.
- Pre-existing UNRELATED working-tree changes (reorganized PDFs, `ODE-Manager-Local` notes, the
  untracked `app/assets/pdfs/Unit-1-Solutions.pdf`) remain unstaged, as before.

---
---

＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
# SESSION LOG, 2026-06-18 (Unit 2 unified master reference guide restored alongside the split assets)
＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝

Branch: `master`. Session start HEAD: `4ed6dcf`. Session end HEAD: pushed this session as
`feat: introduce master unified reference guide source for Unit 2 landing page rewards loop`.
Two files staged together: the re-introduced `app/assets/markdowns/Unit-2-Reference-Guide.tex` (new)
and `.claude-session-state.md` (this log).

### 1.1 Master guide re-introduced (`app/assets/markdowns/Unit-2-Reference-Guide.tex`)
- Recreated the unified Unit 2 master reference guide that was split out and deleted last session, so
  it coexists with the two decoupled sources. It is the full compilation: shared preamble and the
  three custom tcolorbox definitions, the title block, the blue overview `topicsbox`, then Parts I,
  II, and III in sequence (cheat sheet, the 10 graded problems, and the full step by step solutions
  with every live math step wrapped in `\textcolor{purple}{...}`). This is the document the unit
  detail page master reference guide link points at via `Unit-[N]-Cheat-Sheet.pdf` once compiled.
- The three assets now in `app/assets/markdowns/`:
  - `Unit-2-Cheat-Sheet.tex`   --- strictly Part I.
  - `Unit-2-Practice-Set.tex`  --- strictly Parts II and III.
  - `Unit-2-Reference-Guide.tex` --- the master compilation, Parts I, II, and III.

### 1.2 Integrity check
- No local LaTeX engine, so structure verified programmatically (temp checker written, run, removed):
  display math 63 `\[` / 63 `\]`, inline `$` 194 (even), envs balanced (document, topicsbox,
  formulabox times 5, examplebox times 5, enumerate, itemize, center), braces net 0 min 0, one
  `\end{document}`, 44 `\textcolor{purple}` wrappers, zero non-escaped ampersands. The structural em
  dashes (`---`) are preserved throughout.

### 1.3 Immediate next actions
- Compile the three `.tex` sources to their PDFs under `app/assets/pdfs/` so the cheat sheet, practice
  set, and master reference guide links resolve. Author `PRACTICE_DATA` for Unit 2.
- Pre-existing UNRELATED working-tree changes (reorganized PDFs, `ODE-Manager-Local` notes, the
  untracked `app/assets/pdfs/Unit-1-Solutions.pdf`) remain unstaged, as before.

---
---

＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
# SESSION LOG, 2026-06-18 (Unit 2 LaTeX split into decoupled Cheat Sheet and Practice Set sources)
＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝

Branch: `master`. Session start HEAD: `900ee23`. Session end HEAD: pushed this session as
`feat: split Unit 2 materials into separate decoupled Cheat Sheet and Practice Set TeX files`.
Files staged together: the legacy `Unit-2-Reference-Guide.tex` DELETION, the two new TeX sources,
and `.claude-session-state.md` (this log).

### 1.1 Decoupled split (`app/assets/markdowns/`)
- Restructured the unified Unit 2 LaTeX guide into the split asset model. The legacy
  `Unit-2-Reference-Guide.tex` was removed from disk via `git rm`, and its content was divided into
  two standalone, independently compilable documents.
- `Unit-2-Cheat-Sheet.tex`: the shared preamble, package setup, and the three custom tcolorbox
  definitions (`topicsbox`, `formulabox`, `examplebox`), the title block, the blue overview
  `topicsbox`, and strictly Part I (the condensed cheat sheet, modules 2.1 through 2.5 with the five
  orange formula boxes and five purple worked examples). Title line set to
  "Unit 2: First Order Linear Differential Equations \\--- Cheat Sheet". Terminates with its own
  `\end{document}` right after Worked Example 2.5.
- `Unit-2-Practice-Set.tex`: the identical preamble, packages, and custom box definitions, a new
  title block reading "Unit 2: First Order Linear Differential Equations \\--- Practice Problems and
  Solutions", then directly Part II (the 10 graded problems) and Part III (the full step by step
  solutions, every live math step wrapped in `\textcolor{purple}{...}`). The blue topicsbox and all
  of Part I are omitted from the body, though the topicsbox definition stays in the shared preamble
  per the identical definitions requirement. Terminates with its own `\end{document}`.

### 1.2 Integrity check
- No local LaTeX engine, so structure verified programmatically (temp checker written, run, removed):
  - Cheat Sheet: display math 23 `\[` / 23 `\]`, inline `$` 106 (even), envs balanced (document,
    topicsbox, formulabox times 5, examplebox times 5, itemize, center), braces net 0 min 0, one
    `\end{document}`, zero non-escaped ampersands.
  - Practice Set: display math 40 / 40, inline `$` 88 (even), envs balanced (document, enumerate,
    center), braces net 0 min 0, 44 `\textcolor{purple}` wrappers, one `\end{document}`, zero
    non-escaped ampersands.
  - Copy rules honored: no stray `&`, and the structural em dashes (`---`) are preserved in the prose
    of both documents.

### 1.3 Immediate next actions
- Compile both `.tex` to `app/assets/pdfs/Unit-2-Cheat-Sheet.pdf` and a practice set PDF, and wire the
  Practice Set link if a separate target is wanted. Author `PRACTICE_DATA` for Unit 2 from the
  practice set source.
- Pre-existing UNRELATED working-tree changes (reorganized PDFs, `ODE-Manager-Local` notes, the
  untracked `app/assets/pdfs/Unit-1-Solutions.pdf`) remain unstaged, as before.

---
---

＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
# SESSION LOG, 2026-06-18 (Unit 2 LaTeX master reference guide source authored)
＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝

Branch: `master`. Session start HEAD: `46d2af8`. Session end HEAD: pushed this session as
`feat: author comprehensive Unit 2 LaTeX master reference guide source`. Two files staged together:
`app/assets/markdowns/Unit-2-Reference-Guide.tex` (new) and `.claude-session-state.md` (this log).

### 1.1 New LaTeX source (`app/assets/markdowns/Unit-2-Reference-Guide.tex`, 16.5 KB)
- Authored a complete, self contained LaTeX reference guide for Unit 2, First Order Linear
  Differential Equations. New directory `app/assets/markdowns/` created to hold it.
- Content derived from the local data: the five Unit 2 curriculum modules (2.1 Separable, 2.2 Linear
  and Integrating Factors, 2.3 Initial Value Problems, 2.4 Homogeneous and Substitution, 2.5
  Applications) from `curriculum-data.js`, and the concepts, equations, and pitfalls pulled from the
  Unit 2 `QUIZ_DATA` micro practice (10 video keys) and the 30 item unit mastery quiz.
- Structure per the directive: title block ("Unit 2: First Order Linear Differential Equations",
  "Prepared by Staples Education"), a blue `topicsbox`, Part I cheat sheet (orange `formulabox` for
  standard forms and master formulas, purple `examplebox` with a thick left border via
  `borderline west` for five worked examples on distinct numbers), Part II of exactly 10 progressively
  graded problems with no inline answers, and Part III with full step by step solutions where every
  live math execution step is wrapped in `\textcolor{purple}{...}` (44 wrappers).
- Punctuation house style for this document only: em dashes used for conceptual emphasis in prose,
  standard commas for lists. This is authored source content, distinct from the app UI copy rule.

### 1.2 Integrity check
- No local LaTeX engine (`pdflatex`, `latexmk`, `xelatex` all absent), so structure was verified
  programmatically: display math 63 `\[` and 63 `\]`, inline `$` count 194 (even), all environments
  balanced (`document`, `topicsbox`, `formulabox` times 5, `examplebox` times 5, `enumerate`,
  `itemize`, `center`), brace net 0 with min depth 0 (no premature closes). The temp checker script
  was removed. The file is ready to compile in Overleaf or any TeX engine.

### 1.3 Immediate next actions
- Compile the `.tex` to `app/assets/pdfs/Unit-2-Cheat-Sheet.pdf` so the existing Unit 2 cheat sheet
  and master reference guide links resolve. Author `PRACTICE_DATA` for Unit 2 from this guide.
- Pre-existing UNRELATED working-tree changes (reorganized PDFs, `ODE-Manager-Local` notes, the
  untracked `app/assets/pdfs/Unit-1-Solutions.pdf`) remain unstaged, as before.

---
---

＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
# SESSION LOG, 2026-06-18 (Master reference guide link at the foot of every unit detail page)
＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝

Branch: `master`. Session start HEAD: `892485a`. Session end HEAD: pushed this session as
`feat: inject native master reference guide links at the conclusion of all unit detail views`.
Two files staged together: `app/js/router.js` and `.claude-session-state.md` (this log).

### 1.1 Master reference guide link (`app/js/router.js`, `renderUnitDetail`)
- After the Unit Mastery quiz host is appended, `renderUnitDetail` now appends a
  `.unit-master-resource-row` as the final child of `.unit-section`, so the link is the closing
  action on every unit detail page. Inside it, a native anchor styled with the existing
  `.pdf-download-btn` class reads exactly "View Full Unit Reference Guide (Cheat Sheet, Problems, and
  Solutions)". The href is built dynamically from `AVAILABLE_MATERIALS[unitIndex].file` as
  `assets/pdfs/Unit-[N]-Cheat-Sheet.pdf`, with `target="_blank"` and `rel="noopener"` and no
  `download` attribute, so it opens the PDF inline in a new tab. The block is guarded on the unit
  having a materials file.
- No CSS change. `.unit-section` is already a flex column with a 1.25rem gap, so the new row inherits
  clean separation, and `.unit-master-resource-row` serves as a structural hook only.

### 1.2 Verification performed
- `node --check app/js/router.js`: parse clean.
- Browser (static server on `app/`, fresh port 8797, browser test MCP), unit detail `#unit-1`, via
  DOM inspection: the row is present and is the last child of `.unit-section`; the anchor text matches
  the spec exactly; href is `assets/pdfs/Unit-1-Cheat-Sheet.pdf`; `target` is `_blank`; `rel` is
  `noopener`; no `download` attribute; the label contains no em or en dash and no ampersand. Server
  stopped and all `.playwright-mcp` artifacts removed.

### 1.3 Immediate next actions
- Content work unchanged: author `PRACTICE_DATA` for Units 2 through 18, and compile and commit the
  remaining `Unit-[N]-Cheat-Sheet.pdf` and topic guide PDFs so every new tab link resolves.
- Pre-existing UNRELATED working-tree changes (reorganized PDFs, `ODE-Manager-Local` notes, the
  untracked `app/assets/pdfs/Unit-1-Solutions.pdf`) remain unstaged, as before.

---
---

＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
# SESSION LOG, 2026-06-18 (Materials card spacing follow-up, topic guides anchored beneath buttons)
＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝

Branch: `master`. Session start HEAD: `5681ec8` (the prior UI polish commit). Session end HEAD:
pushed this session as
`ui/refactor: anchor topic guides beneath buttons and push empty space to card floor`.
Two files staged together: `app/css/main.css` and `.claude-session-state.md` (this log). This commit
also carries the `.claude-session-state.md` prepend from the prior commit, which had been left
unstaged by the stage exactly the code files directive last time.

Scope: a one file CSS spacing correction that reverses the approach taken in the prior log section 1.5.
The previous pass used `margin-top: auto` on `.materials-subtopics` to float the topic guides to the
card bottom, which opened an artificial gap between the primary button and the topic guides. This pass
removes that, so the topic guides sit directly beneath the button, and the card itself collects the
leftover slack at its floor.

---

## 1. Completed task and file modification

### 1.1 Materials card spacing (`app/css/main.css`)
- Removed `margin-top: auto` from `.materials-subtopics`. The Topic guides block now sits naturally
  right below the primary action button, separated only by the card flex gap (0.75rem) plus its own
  top border and `padding-top`.
- Made the column packing explicit on `.materials-card` with `justify-content: flex-start`. With the
  grid stretching cards to a shared row height, flex-start packs every content block (title,
  description, button, topic guides) flush from the top and collects the leftover empty space at the
  bottom of the card, beneath the last subtopic, so the row keeps a clean baseline with no artificial
  gaps between the text blocks and the list.
- Updated both block comments to describe the new behavior, superseding the prior log section 1.5
  description (which documented the now removed `margin-top: auto` anchor).

## 2. Verification performed
- Browser (static server on `app/`, fresh port 8795 to dodge the MCP subresource cache, browser test
  MCP), Cheat Sheets `#cheat-sheets`, via DOM inspection on a card that carries curated topic guides
  (Unit 9):
  - `.materials-subtopics` computed `margin-top` is `0px` (the auto rule is gone).
  - `.materials-card` computed `justify-content` is `flex-start`.
  - The gap between the primary button bottom and the topic guides top is 12px, the card flex gap
    only, confirming the guides sit tucked beneath the button with no artificial gap.
  - The slack from the last subtopic bottom to the card content floor is 85px on this short card,
    confirming the leftover empty space collects at the bottom of the card.
  - Server stopped and all `.playwright-mcp` artifacts removed.

## 3. Immediate next actions
- Content work unchanged: author `PRACTICE_DATA` for Units 2 through 18, and compile and commit the
  remaining `Unit-[N]-Cheat-Sheet.pdf` and topic guide PDFs so the new tab links resolve for everyone.
- Pre-existing UNRELATED working-tree changes (deleted and reorganized PDFs and cheat sheets, the
  untracked `ODE-Manager-Local` notes, and the untracked `app/assets/pdfs/Unit-1-Solutions.pdf`)
  remain unstaged, exactly as before. They were not bundled into this commit.

---
---

＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
# SESSION LOG, 2026-06-18 (Three way theme dropdown, cheat sheet labels, card spacing repair)
＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝

Branch: `master`. Session start HEAD: `7de72df` (prior session log append), in sync with
`origin/master`. Session end HEAD: pushed this session as
`ui/refactor: implement three-way theme dropdown, update cheat sheet labels, and fix card spacing alignment`.
Four app files staged together: `app/index.html`, `app/js/theme.js`, `app/js/router.js`,
`app/css/main.css`. `app/js/quiz-data.js` was re-verified only (no change needed).

Scope: a four part UI polish pass. The light and dark toggle button became a third themed dropdown
with a new system option, the Cheat Sheets primary buttons and subhead moved to the open in a new tab
narrative, the Practice Set pipeline was re-verified clean of any review guide copy, and the materials
card flex layout was fixed so the primary button hugs the description on every card.

---

## 1. Completed tasks and file modifications

### 1.1 Three way theme dropdown (`app/index.html`, `app/js/theme.js`, `app/css/main.css`)
- `index.html`: replaced the single `#theme-toggle` button with a `.nav-dropdown theme-dropdown`
  block matching the exact structure of the Browse and Learning Mode dropdowns. The trigger holds a
  `#theme-dropdown-label` span; the menu has three `role="menuitem"` buttons with ids `#theme-light`,
  `#theme-dark`, `#theme-system` and a `data-theme-choice` attribute, with Light Mode marked active by
  default.
- `theme.js`: rewritten to manage three explicit preferences, `light`, `dark`, `system`, persisted to
  `localStorage["ode_theme_preference"]` with a `light` default. `effectiveTheme` resolves `system`
  through `window.matchMedia("(prefers-color-scheme: dark)")` (captured once as `SYSTEM_MEDIA`).
  `applyTheme` sets `data-theme="dark"` or removes the attribute (light is the absence, matching
  theme.css). `updateThemeButtons` writes the trigger label (Light Mode, Dark Mode, System Default)
  and toggles the `.active` menu item, mirroring how `modes.js` syncs the learning mode. A
  `change` listener on `SYSTEM_MEDIA` re-applies the effective theme on the fly whenever the OS
  preference toggles, but only while the stored preference is `system` (with an `addListener`
  fallback for older engines).
- `main.css`: removed the dead `.theme-toggle-btn` rules (the control now reuses the shared
  `.nav-dropdown` component, including the existing `.nav-dropdown-menu button.active` accent state).

### 1.2 Hardcoded contrast hex converted to theme variables (`app/css/main.css`)
- Converted every remaining `#ffffff` and `#000000` to theme variables so they invert natively:
  `.checkpoint-begin-btn`/`.check-btn`, `.pair-badge`, `.quiz-launch-btn`, `.quiz-next-btn` text now
  use `var(--bg-color)` (dark text on the lavender accent in dark mode, white on purple in light),
  and `.video-embed`, `.desmos-frame`, `.math-canvas` backgrounds now use `var(--bg-color)`. A hex
  scan of `main.css` afterward returns zero hardcoded colors.

### 1.3 Cheat Sheets link text (`app/js/router.js`)
- `renderCheatSheets`: the primary button string changed from "Download Unit [N] Cheat Sheet" to
  "Open Unit [N] Cheat Sheet", matching the open in a new tab behavior the anchors already use, and
  the subhead changed from the download a one page cheat sheet narrative to "Open the reference
  materials for each unit, each in a new tab. Some units add focused topic guides."

### 1.4 Practice Set review guide removal, re-verified (`app/js/quiz-data.js`, `app/js/router.js`)
- Confirmed `PRACTICE_DATA` outputs only clean `problems` and `solutions` (Unit 1 authored, 10 each),
  and the `#practice-sets-[N]` pipeline renders only the "Practice Problems" and "Solutions" section
  headers. No review guide section header or prose appears anywhere in the rendered body. The single
  "review guide" mention left in the tree is a provenance comment above `PRACTICE_DATA`, not user
  facing copy. No code change was required for this task.

### 1.5 Materials card flex spacing repair (`app/css/main.css`)
- The buttons were sitting at uneven heights because `.materials-card-desc` carried `flex-grow: 1`,
  which stretched the description and pushed the primary button down by a variable amount. Removed
  that `flex-grow` so the button now hugs the description (measured gap is exactly the 0.75rem flex
  gap, 12px, on every card). Moved the vertical distribution onto `.materials-subtopics` via
  `margin-top: auto`, so the topic guides block anchors to the card bottom padding boundary and cards
  of differing description lengths keep an even baseline. There were no `margin-top: auto` rules on
  the buttons to remove.

## 2. Architecture notes (delta this session)
- The header now hosts three instances of the single `.nav-dropdown` idiom: Browse (links), Learning
  Mode (action buttons), and Color theme (action buttons). The theme and mode dropdowns share the
  identical label plus `.active` sync pattern, the only difference being theme.js persists to
  localStorage while modes.js persists through ODEState.
- Theme state is now a three way preference, not a boolean. The effective `data-theme` is still only
  `light` (no attribute) or `dark`; `system` is a preference that resolves to one of those live.
- No engine, state, schema, `QUIZ_DATA`, or `PRACTICE_DATA` changes.

## 3. Verification performed
- `node --check app/js/router.js` and `app/js/theme.js`: parse clean.
- Copy rule scan on the four touched files: the only em or en dash in `app/` is the curated
  Picard Lindelof video title in `curriculum-data.js` (source data), and the only ampersands are
  `&&` operators in code. A hex scan of `main.css` returns zero hardcoded colors.
- Browser (static server on `app/`, fresh port 8793 to dodge the MCP subresource cache, browser
  test MCP), via DOM inspection:
  - Default load: no `data-theme` attribute, no stored preference, trigger reads "Light Mode",
    `#theme-light` is the active item, the old `#theme-toggle` is gone, menu items are Light Mode,
    Dark Mode, System Default.
  - Clicking Dark sets `data-theme="dark"`, pref `dark`, label "Dark Mode", active `#theme-dark`;
    Light reverts all four; System sets pref `system`, label "System Default", active `#theme-system`
    and resolves `data-theme` to the live OS value (light in the test env).
  - Dark mode contrast: a `.quiz-launch-btn` resolves its text to `rgb(18,18,18)`, that is
    `--bg-color`, confirming the converted hex inverts natively.
  - Cheat Sheets: subhead is the new open in a new tab copy, first button reads "Open Unit 0 Cheat
    Sheet", zero buttons contain the word Download, `.materials-card-desc` computed `flex-grow` is 0,
    and `.materials-subtopics` computed `margin-top` resolves to a positive auto value (84px on the
    first card), confirming the bottom anchor.
  - Card layout: the gap between the description bottom and the primary button top is 12px (the flex
    gap only), confirming the button hugs the description.
  - Practice Set `#practice-sets-1`: section headers are exactly "Practice Problems" and "Solutions",
    the action button reads "Open Unit 1 Cheat Sheet (PDF)", the rendered body contains no "review
    guide" text, and 10 problems render.
  - The only console output is the pre existing Desmos trial key warning and a favicon 404, neither
    from this refactor.
  - Server stopped and all `.playwright-mcp` artifacts removed.

## 4. Immediate next actions
- Remaining content work is unchanged from prior logs: author `PRACTICE_DATA` for Units 2 through 18,
  and compile and commit the remaining `Unit-[N]-Cheat-Sheet.pdf` and topic guide PDFs so the new tab
  links resolve for everyone.
- Pre-existing UNRELATED working-tree changes (deleted and reorganized PDFs and cheat sheets, the
  untracked `ODE-Manager-Local` notes, and the untracked `app/assets/pdfs/Unit-1-Solutions.pdf`)
  remain unstaged, exactly as at session start. They were not bundled into this commit.

---
---

＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
# SESSION LOG, 2026-06-18 (Materials hub UI/UX refactor, plus the practice set Open label)
＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝

Branch: `master`. Session start HEAD: `4bcd79b` (compiled Unit 1 cheat sheet PDF commit).
Session end HEAD: `75de1e6`, in sync with `origin/master`. One commit this session, all four app
files staged together and pushed:
`75de1e6` ui/refactor: update practice set anchors to Open label and finalize materials hub navigation.

Scope: a five part UI/UX refactor of the learning mode control, PDF anchor behavior, Practice Set
scope, and universal subtopic rendering, finished with a one word label change on the practice set
action button. Files: `app/index.html`, `app/js/modes.js`, `app/js/router.js`, `app/css/main.css`.

---

## 1. Completed tasks and file modifications

### 1.1 Learning mode is now a dropdown (`app/index.html`, `app/js/modes.js`, `app/css/main.css`)
- Replaced the two button `.mode-switch` group with a `.nav-dropdown mode-dropdown`, reusing the
  same CSS only Browse component (the menu opens on `:hover` and `:focus-within`, no new JS for the
  open and close behavior). The trigger holds a `#mode-dropdown-label` span; the menu has two
  `role="menuitem"` buttons carrying the original ids `#mode-exploration` and `#mode-guided` plus a
  `data-mode` attribute, so the existing `modes.js` click wiring kept working untouched.
- `modes.js`: `updateModeButtons` now also writes the active mode name into `#mode-dropdown-label`,
  so the closed trigger always shows the current mode. Exploration Mode stays the default.
- `main.css`: removed the now dead `.mode-switch` and `.mode-btn` rules (which also deleted a
  hardcoded `#ffffff`), extended `.nav-dropdown-menu a` styling to cover `button` children
  (full width, left aligned, transparent background, inherited font), and added a
  `.nav-dropdown-menu button.active` accent state for the selected mode.

### 1.2 PDF anchors open natively in a new tab (`app/js/router.js`)
- Every cheat sheet anchor had its `download` attribute stripped and `target="_blank"` plus
  `rel="noopener"` added, so the browser opens the PDF inline in a new tab rather than forcing a
  download. This covers all three surfaces, per the user's directive to leave the buttons where
  they already render: the Practice Set detail action button (`renderPracticeSetDetail`), the Cheat
  Sheets index primary buttons (`renderCheatSheets`), and the curated topic guide links. The hrefs
  still resolve to `assets/pdfs/Unit-[N]-Cheat-Sheet.pdf` and the per guide files.

### 1.3 Practice Set detail scope isolation (`app/js/router.js`)
- The `#practice-sets-[N]` body outputs only the `PRACTICE_DATA` problems and solutions. Confirmed
  there is no "review guide" or "read the cheat sheet" prose in the rendered body. The cheat sheet
  download button stays (per the user's revised instruction), now with the Open label below.

### 1.4 The action button label (`app/js/router.js`)
- Final pass of this session: the practice set detail action button text changed from
  "Download Unit [N] Cheat Sheet (PDF)" to "Open Unit [N] Cheat Sheet (PDF)", matching the new
  open in a new tab behavior. Single string change, no layout or data touched.

### 1.5 Universal subtopic guide rendering (`app/js/router.js`, `app/css/main.css`)
- Added two helpers. `getUnitSubtopics(unitIndex)` returns the curated `AVAILABLE_MATERIALS`
  subtopics where a unit has them (each carries a PDF `file`), and otherwise derives the list from
  that unit's `CURRICULUM` modules, with `stripModuleNumber` removing the leading "N.M " prefix so
  the topic reads cleanly. `appendSubtopics(card, unitIndex, asLinks)` renders the shared
  "Topic guides" sub-list: a `pdf-download-link` (new tab) when `asLinks` is true and the entry has
  a file, a plain `materials-subtopic-text` span otherwise.
- Wired the helper into all three hub pages so every one of the 19 unit cards shows a Topic guides
  list: `renderCheatSheets` calls it with `asLinks` true (curated PDF links where present, plain
  text otherwise), `renderPracticeSets` and the new `renderInteractives` call it with `asLinks`
  false (plain text only, so neither hub carries PDF links).
- `renderInteractives` is a new data driven hub that replaces the old "Content coming soon."
  placeholder for `#interactives`: one `materials-card` per unit linking to `#unit-[N]` where the
  checkpoints live, plus the plain text Topic guides. The now orphaned `renderStaticPage` helper was
  removed; `.static-page-placeholder` is still used by the practice set coming soon notes, so that
  CSS stayed.
- `main.css`: added `.materials-subtopic-text` (and its `::before` accent square) to style the non
  link topic entries to match the link spacing without the interactive affordance.

## 2. Architecture notes (delta this session)
- The mode control now shares the exact `.nav-dropdown` component with Browse, so there is a single
  dropdown idiom in the header. The menu items are buttons (actions) rather than links (navigation),
  the only structural difference from Browse.
- `getUnitSubtopics` is the single source for a unit's topic guides across the three hubs. Curated
  guides remain the override; the curriculum modules are the universal fallback, so adding curated
  guides to a unit in `AVAILABLE_MATERIALS` automatically upgrades its plain text list to PDF links
  on the Cheat Sheets hub with no other change.
- No engine, state, schema, or `QUIZ_DATA` and `PRACTICE_DATA` changes. The Quizzes Index was out of
  scope and is untouched.

## 3. Verification performed
- `node --check app/js/router.js` and `app/js/modes.js`: parse clean.
- Copy rule scan on all four files: no em or en dashes, and the only ampersands are `&&` operators
  in code. The single em or en dash anywhere in `app/` is a curated video title in
  `curriculum-data.js` (Picard Lindelof), which is source data, not authored prose.
- Browser (static server on `app/`, fresh port 8791 to dodge the MCP subresource cache, browser
  test MCP), all checks via DOM inspection:
  - Cheat Sheets: 19 cards; primary button has no `download` attribute, `target="_blank"`, href
    `assets/pdfs/Unit-0-Cheat-Sheet.pdf`; every card has a Topic guides list; Unit 0 renders module
    derived plain text, Unit 9 renders curated PDF links (target blank, no download).
  - Practice Sets hub: 19 cards, every card has Topic guides, zero PDF links anywhere (Unit 9 curated
    topics render as plain text); Open buttons resolve to `#practice-sets-[N]`.
  - Practice Set detail `#practice-sets-1`: action button reads "Open Unit 1 Cheat Sheet (PDF)" (the
    pre commit screen still showed Download; the final string commit fixed it), no download attribute,
    target blank, correct href; 10 problems and 10 solutions; no review guide or read the cheat sheet
    prose.
  - Interactives `#interactives`: now a 19 card hub, not the placeholder; every card has plain text
    Topic guides, zero PDF links, Open buttons resolve to `#unit-[N]`.
  - Mode dropdown: default Exploration Mode with the Exploration option active; invoking the Guided
    handler flips the mode, the trigger label, and the active class, and re-renders the curriculum
    for Guided locking; invoking Exploration reverts all three.
  - The only console output is a pre existing favicon 404 and the Desmos trial key warning, neither
    from this refactor.
  - Server stopped and all `.playwright-mcp` artifacts removed.

## 4. Immediate next actions
- Push done this session at `75de1e6`. Remaining content work is unchanged from prior logs: author
  `PRACTICE_DATA` for Units 2 through 18, and compile and commit the remaining
  `Unit-[N]-Cheat-Sheet.pdf` and topic guide PDFs so the now new tab links resolve for everyone.
- Pre-existing UNRELATED working-tree changes (deleted and reorganized PDFs and cheat sheets, the
  untracked `ODE-Manager-Local` notes, and a new untracked `app/assets/pdfs/Unit-1-Solutions.pdf`)
  remain unstaged, exactly as at session start. They were not bundled into this commit.

---
---

＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
# SESSION SUMMARY, 2026-06-18 (Student materials hub, end to end)
＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝

This is a consolidated overview of the entire 2026-06-18 session. The detailed per-task logs follow
below this summary, in reverse chronological order, and remain the authoritative record for each
step. Everything described here is already COMMITTED and PUSHED across eight commits on `master`.

Branch: `master`. Session start HEAD: `7342b17` (interactive checkpoints wired for Units 13 to 18).
Session end HEAD: `4bcd79b`, in sync with `origin/master`.

---

## 1. Arc of the session

The session built, in sequence, a complete student materials hub on top of the existing curriculum
single page app: a global navigation dropdown, four hash routes behind it, data driven index pages
for Cheat Sheets, Practice Sets, and Quizzes, two two-level directories (Quizzes and Practice Sets),
and finally a real Practice Set environment with a PDF download and rendered problems and solutions.
A standalone LaTeX cheat sheet for Unit 1 was generated mid session (terminal output only), and the
user's compiled PDF of it was committed at the end so the Unit 1 download link is live.

## 2. Completed tasks, in order, with commit hashes

1. `ef13179` feat/docs: global navigation header dropdown. A CSS only dropdown ("Browse") in the
   header, requiring zero JavaScript, expanding on `:hover` and `:focus-within`, with full ARIA
   (`aria-haspopup`, `aria-expanded`, `role="menu"`, `role="none"`, `role="menuitem"`) and four
   links: Practice Sets, Cheat Sheets (PDFs), Quizzes Index, Interactives. Files: `app/index.html`,
   `app/css/main.css`.

2. `d79f199` feat/docs: route handling and PDF scaffolding. Added the four static routes to
   `renderCurriculum`, a shared `renderStaticPage` scaffold, `renderPracticeSets` and
   `renderCheatSheets` placeholders, the `app/assets/pdfs/` directory (with `.gitkeep`), and the
   reusable `.pdf-download-btn` component. Files: `app/js/router.js`, `app/css/main.css`,
   `app/assets/pdfs/.gitkeep`.

3. `387bd94` feat/docs: dynamic UI indices. Turned the three placeholder routes into data driven
   Tables of Contents over `CURRICULUM`: `AVAILABLE_MATERIALS` config, `renderCheatSheets` (per unit
   primary PDF plus optional topic guides), `renderPracticeSets`, and `renderQuizzesIndex` (mastery
   launch cards). Files: `app/js/router.js`, `app/css/main.css`.

4. `a299a1c` refactor/docs: Quizzes Index becomes a two-level directory. Top level is 19 unit cards
   with "View Unit Quizzes" linking to `#quizzes-index-[N]`; the new `renderUnitQuizzesDetail`
   renders every video's micro practice launch card plus the unit mastery card, reusing the
   `micro::<video_id>` and `mastery::<unit title>` ids so progress stays in sync. File:
   `app/js/router.js`.

5. `7594527` refactor/docs: dedicated Practice Sets sub-view. Practice cards route to
   `#practice-sets-[N]` instead of the `#unit-[N]` fallback; new `renderPracticeSetDetail` with a
   back button, unit header, a Practice Problems section, and a Solutions section hidden behind a
   "View Solutions" toggle (placeholder content at this stage). Files: `app/js/router.js`,
   `app/css/main.css`.

6. (no commit) Generated a complete, compilable LaTeX cheat sheet and review guide for Unit 1
   (Foundations and Prerequisites), with `tcolorbox` color coding (orange for core logic and
   standard form, purple for worked examples, red for pitfalls), a title page, table of contents,
   and three paginated parts. Output to the terminal only, per the directive.

7. `b4dc167` feat/docs: Practice Set views with PDF download and rendered content. Expanded
   `AVAILABLE_MATERIALS` so every unit (0 to 18) has a primary `file`; added the `PRACTICE_DATA`
   global to `app/js/quiz-data.js` (Unit 1 fully authored, 10 problems and worked solutions in
   KaTeX); rebuilt `renderPracticeSetDetail` to render a "Download Unit [N] Cheat Sheet (PDF)"
   action button, the problems and solutions from `PRACTICE_DATA`, and a `renderPracticeMath` KaTeX
   pass. Files: `app/js/router.js`, `app/js/quiz-data.js`, `app/css/main.css`.

8. `4bcd79b` assets: add the compiled Unit 1 cheat sheet PDF. The user's Overleaf compile of the
   LaTeX from step 6 (245 KB, 9 pages) committed at `app/assets/pdfs/Unit-1-Cheat-Sheet.pdf`, so the
   Unit 1 download link now resolves for everyone. File: `app/assets/pdfs/Unit-1-Cheat-Sheet.pdf`.

## 3. Architectural decisions

- Routing is hash based in one `renderCurriculum` switch. Exact static routes (`#practice-sets`,
  `#cheat-sheets`, `#quizzes-index`, `#interactives`) are matched first, then the numeric sub-routes
  by regex (`/^#practice-sets-\d+$/`, `/^#quizzes-index-\d+$/`), each parsing a unit index with an
  out-of-range fallback to its index page, then the existing `#unit-N` and Table of Contents views.
  The order guarantees the exact and the numeric forms never collide.
- Two-level directory pattern, applied to both Quizzes and Practice Sets: a top level grid of unit
  cards, each linking to a per unit detail route. Reused across both features for consistency.
- Progress sync by shared quiz id. Surfaces that show the same quiz mount it with the same engine id
  (`micro::<video_id>`, `mastery::<unit title>`), so a student's solved questions are identical
  whether opened from a unit, the Quizzes Index, or a sub-view. The launch card versus inline
  distinction is controlled only by the `inline` flag in the mount config, no engine change.
- Data lives in plain script globals (file:// constraint). `AVAILABLE_MATERIALS` (in `router.js`)
  is the source for per unit PDF file names. `PRACTICE_DATA` (new global in `quiz-data.js`, loaded
  before `router.js`) holds practice problems and solutions, keyed by unit index, with KaTeX double
  escaped in source so it resolves to a single backslash at runtime. `PRACTICE_DATA` is NOT mirrored
  into `app/data/quizzes.json` (that mirror tracks `QUIZ_DATA` only).
- CSS reuse over new classes. New views lean on existing classes (`toc-grid`, `materials-card`,
  toc-intro and toc-heading, module-title, back-to-toc-btn, pdf-download-btn, video-quiz-host,
  unit-mastery-host, static-page-placeholder). New CSS this session was the `.pdf-download-btn`
  component, `.nav-dropdown` block, the `.materials-card` family, the `.pdf-download-link` topic
  guide style, the `.practice-set-section` layout, and the practice problem and solution list
  styling. Every new rule maps colors to theme variables, so light and dark both inherit cleanly.
- Theme contrast trick. `.pdf-download-btn` uses `color: var(--bg-color)` on an accent background,
  which gives white text on purple in light mode and dark text on lavender in dark mode, correct in
  both without any hardcoded hex.

## 4. Exact codebase state at session end

- New routes live and verified: `#practice-sets`, `#practice-sets-[N]`, `#cheat-sheets`,
  `#quizzes-index`, `#quizzes-index-[N]`. `#interactives` is still the "Content coming soon."
  placeholder (never in scope this session).
- `app/index.html`: the `.nav-dropdown` nav block sits first inside `.header-controls`.
- `app/js/router.js`: holds `AVAILABLE_MATERIALS` (19 units, each with a `file`, Units 9 to 18 also
  with `subtopics`), the route switch with the two numeric sub-route branches, `renderStaticPage`,
  `buildIndexShell`, `renderCheatSheets`, `renderPracticeSets`, `renderPracticeSetDetail`,
  `renderQuizzesIndex`, `renderUnitQuizzesDetail`, and the `renderPracticeMath` helper.
- `app/js/quiz-data.js`: `QUIZ_DATA` unchanged (250 micro_practice keys, 19 unit_mastery keys), plus
  the new `PRACTICE_DATA` global. PRACTICE_DATA currently has Unit 1 only (10 problems and
  solutions); all other units degrade to a coming soon note in the sub-view.
- `app/css/main.css`: all the new component and view styling described above.
- `app/assets/pdfs/`: `.gitkeep` plus the committed `Unit-1-Cheat-Sheet.pdf`. Every other per unit
  PDF link (`Unit-[N]-Cheat-Sheet.pdf` and the topic guide files) is a scaffolded target whose file
  is not present yet.
- Verification standard used throughout: `node --check` on touched JS, a copy-rule scan (no em or en
  dashes, no ampersands outside `&&` operators), and live browser-test MCP checks served from a
  FRESH PORT each time (the MCP caches subresources by URL, so reusing a port serves stale JS after
  an edit). Artifacts and servers were cleaned up after each task.

## 5. Outstanding and next steps

- Author `PRACTICE_DATA` for the remaining units (2 to 18) from their review guides, and compile and
  commit the rest of the `Unit-[N]-Cheat-Sheet.pdf` files and the topic guide PDFs so the download
  links go live. The renderer and CSS already handle any unit generically.
- The Practice Sets primary button text still reads "Open Unit [N] Practice Set" on the index, which
  is fine. The `#interactives` route could later get the same data driven treatment.
- Pre-existing UNRELATED working-tree changes (deleted and reorganized PDFs and cheat sheets, plus
  untracked `ODE-Manager-Local` notes) remain unstaged, exactly as at session start. They were never
  bundled into any commit this session.

＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
# END SESSION SUMMARY. Per-task detail logs follow below.
＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝

---
---

# SESSION LOG, 2026-06-18 (Practice Set views with PDF download and rendered problems and solutions)

Branch: `master`. Session start HEAD: `7594527` (prior dedicated-Practice-Sets-sub-view commit).
Scope: upgraded the Practice Set sub-view into a real practice environment with a cheat sheet PDF
download, problems and solutions rendered from a new `PRACTICE_DATA` store, and a solutions toggle.
Three app files touched (`app/js/router.js`, `app/js/quiz-data.js`, `app/css/main.css`) plus this
session-state append, in one feature/docs commit, pushed to `origin master`.

---

## 1. Completed Tasks and File Modifications

### 1.1 `AVAILABLE_MATERIALS` now carries a primary file per unit (`app/js/router.js`)
- Added a `file` field ("Unit-[N]-Cheat-Sheet.pdf") to every unit entry (0 through 18), and created
  entries for Units 0 through 8 which previously had none. Units 9 through 18 keep their existing
  `subtopics` arrays alongside the new `file`. This makes `AVAILABLE_MATERIALS[unitIndex].file` the
  source for the cheat sheet download path. No regression to `renderCheatSheets`, which still builds
  its primary href inline and only reads `subtopics`; the new Units 0 through 8 entries have no
  `subtopics`, so its guard still skips the topic-guide list for them.

### 1.2 New `PRACTICE_DATA` store (`app/js/quiz-data.js`)
- Appended a `PRACTICE_DATA` global after `QUIZ_DATA`, keyed by unit array index. Each unit holds a
  `problems` array, where every problem has a stable `id` ("ps_[unit]_[n]"), a `problem` prompt, and
  a worked `solution`. Both fields may contain KaTeX (inline single dollar, display double dollar),
  double escaped in source so they resolve to a single backslash at runtime, matching the quiz data
  convention. Unit 1 is fully authored with all 10 problems and solutions, drawn from the Unit 1
  cheat sheet and review guide content (calculus toolkit, the number e, Euler's formula, and ODE
  vocabulary). Other units have no entry yet and fall back to a coming soon note.

### 1.3 `renderPracticeSetDetail` rebuilt (`app/js/router.js`)
- Added a `renderPracticeMath(el)` helper that calls `renderMathInElement` with the same delimiters
  as the quiz engine, guarded for when the auto render script is absent.
- The sub-view now renders, in order: the "Back to Practice Sets" button, the unit title header, an
  action row with a `pdf-download-btn` anchor reading "Download Unit [N] Cheat Sheet (PDF)" pointing
  to `assets/pdfs/` plus `AVAILABLE_MATERIALS[unitIndex].file` (with the `download` attribute), a
  Practice Problems section that renders the `PRACTICE_DATA` problems as an ordered list, and a
  Solutions section hidden behind the "View Solutions" toggle that renders the solutions as an
  ordered list. After building the DOM it calls `renderPracticeMath(container)` once to typeset all
  the KaTeX. Units without practice data show "Practice problems for this unit are coming soon." and
  "Solutions for this unit are coming soon." while still offering the PDF download.

### 1.4 Styling (`app/css/main.css`)
- Added `.practice-action-row` (flex, wraps on narrow screens), `.practice-problem-list` (plain
  numbered list with spacing), an accent top border and padding on `.practice-set-solutions` so the
  revealed solutions read as a clearly separate block, and `.practice-solution-list li` styled as
  soft panels with an accent left bar (`var(--accent-color)`, `var(--panel-bg)`) so each solution is
  visually distinct from the problems. The "View Solutions" toggle keeps the reused `pdf-download-btn`
  styling. All colors are theme variables, so light and dark inherit correctly.

## 2. Architecture notes (delta this session)
- `PRACTICE_DATA` is a new runtime global in `quiz-data.js`, loaded before `router.js` in
  `index.html` like `QUIZ_DATA`, so it is available to the renderer with no extra wiring. It is NOT
  mirrored into `app/data/quizzes.json` (that mirror tracks `QUIZ_DATA` only).
- The Practice Set detail is now the content surface that the standalone LaTeX review guides feed
  into. Authoring more units means adding their key to `PRACTICE_DATA` only; the renderer and CSS
  already handle any unit generically, and missing units degrade to the coming soon notes.

## 3. Verification performed
- `node --check` on both `app/js/router.js` and `app/js/quiz-data.js`: parse clean.
- A temporary node script (written, run, then deleted) loaded `PRACTICE_DATA` via a Function eval
  and confirmed: 10 Unit 1 problems, 0 runtime double-backslash strings (so KaTeX is single
  backslash at runtime), 0 en or em dashes, 0 ampersands, and ids correctly sequenced ps_1_1 through
  ps_1_10. Copy-rule scan on the changed files: the only ampersands are `&&` operators in code.
- Browser (static server on `app/`, fresh port to dodge the JS cache, browser-test MCP):
  - `#practice-sets-1`: download button reads "Download Unit 1 Cheat Sheet (PDF)" pointing to
    `assets/pdfs/Unit-1-Cheat-Sheet.pdf` with the download attribute; 10 problems and 10 solutions
    render; all 10 problems and all 10 solutions contain rendered `.katex` spans (math typeset, not
    raw TeX); solutions hidden by default and revealed by the toggle, which swaps to "Hide
    Solutions". A full page screenshot confirmed the numbered problems, the accent separated
    solutions panels, and clean math.
  - `#practice-sets-0` (no practice data): the download button still shows ("Download Unit 0 Cheat
    Sheet (PDF)" to `assets/pdfs/Unit-0-Cheat-Sheet.pdf"), and both sections show their coming soon
    notes with no problem or solution list.
  - Dark mode: the solutions accent top border resolves to the dark accent `rgb(187,134,252)`,
    confirming theme inheritance (the download button background was caught mid transition on its
    0.2s animation, a known and harmless timing artifact).
  - Server stopped and all `.playwright-mcp` and screenshot artifacts removed.

## 4. Immediate next actions
- Push done this session. A real compiled `app/assets/pdfs/Unit-1-Cheat-Sheet.pdf` (245 KB, 9 pages)
  is present in the working tree, evidently the user's Overleaf compile of the Unit 1 LaTeX guide
  generated last turn. It was LEFT UNSTAGED because the directive enumerated only the three code
  files to stage. To make the Unit 1 cheat sheet download work for everyone, that PDF (and future
  per unit PDFs) should be committed deliberately. Next content step: author `PRACTICE_DATA` for the
  remaining units from their review guides.
- Pre-existing UNRELATED working-tree changes (deleted/reorganized PDFs and cheat sheets, plus some
  untracked `ODE-Manager-Local` notes) remain unstaged, as in prior sessions.

---
---

# SESSION LOG, 2026-06-18 (Dedicated Practice Sets sub-view with solutions toggle)

Branch: `master`. Session start HEAD: `a299a1c` (prior Quizzes Index two-level-directory commit).
Scope: refactored the Practice Sets UI so the unit buttons no longer fall back to `#unit-[N]`, and
built a dedicated `#practice-sets-[N]` sub-view holding a Practice Problems section and a
solutions toggle. Two app files touched (`app/js/router.js`, `app/css/main.css`) plus this
session-state append, in one refactor/docs commit, pushed to `origin master`.

---

## 1. Completed Tasks and File Modifications

### 1.1 Routing (`app/js/router.js`)
- `renderPracticeSets` cards now link to `#practice-sets-[N]` instead of the temporary `#unit-[N]`
  fallback. The card button label is unchanged ("Open Unit [N] Practice Set"), and the index
  subhead was reworded to describe the problems-and-solutions sub-view.
- Added a router branch for `/^#practice-sets-\d+$/` directly after the exact `#practice-sets`
  case (so the two never collide): it parses the unit index and calls
  `renderPracticeSetDetail(container, pi)` for a valid unit index (0 through 18), falling back to
  `renderPracticeSets` for an out-of-range index.

### 1.2 New sub-view `renderPracticeSetDetail(container, unitIndex)`
- Renders a "Back to Practice Sets" button (sets the hash to `practice-sets`), a `.toc-intro`
  header with the unit title and a subhead, then two sections:
  - A Practice Problems section: a `module-title` header and a `.practice-set-problems` body with
    the placeholder "Practice problems loading...".
  - A Solutions section: a "View Solutions" toggle button (the reused `pdf-download-btn`), and a
    `.practice-set-solutions` body that is `hidden` by default and contains its own "Solutions"
    header plus the placeholder "Solutions loading...". The toggle flips `body.hidden` and swaps
    its own label between "View Solutions" and "Hide Solutions", so the answers never show until
    the student asks for them.

### 1.3 Styling (`app/css/main.css`)
- Added a small layout-only `.practice-set-section` block (flex column, `align-items: flex-start`,
  `gap`) so the toggle button hugs its content while `.practice-set-problems` and
  `.practice-set-solutions` stretch to full width via `align-self: stretch`. No colors are set
  here; every themed visual comes from the reused classes (`back-to-toc-btn`, `toc-intro`,
  `toc-heading`, `toc-subhead`, `module-title`, `static-page-placeholder`, `pdf-download-btn`),
  so light and dark theme inheritance and responsiveness are automatic.

## 2. Architecture notes (delta this session)
- No engine/state/schema changes. The Practice Sets directory now matches the Quizzes Index
  pattern: a top-level grid route plus a per-unit detail route guarded by a numeric-hash regex
  with an out-of-range fallback. The solutions visibility uses the native `hidden` attribute, so
  the default browser `display: none` applies with no extra CSS.
- The problem and solution content is placeholder text only. Filling each unit's practice set with
  real problems and worked solutions (the LaTeX review guides being authored separately are the
  natural source) is future work.

## 3. Verification performed
- `node --check app/js/router.js`: parse clean. Copy-rule scan: no em-dashes or en-dashes in the
  file; the only ampersands are `&&` operators in code. The placeholders use three plain periods,
  not an ellipsis or any forbidden character.
- Browser (static server on `app/`, fresh port to dodge the JS cache, browser-test MCP):
  - Practice Sets index: all 19 cards link to `#practice-sets-[N]` (Unit 3 button reads "Open Unit
    3 Practice Set" with href `#practice-sets-3`).
  - Sub-view `#practice-sets-3`: "Back to Practice Sets" button, heading "Unit 3: Existence,
    Uniqueness, and Geometry", section headers "Practice Problems" and "Solutions", and the
    placeholders "Practice problems loading..." and "Solutions loading...".
  - Solutions toggle: hidden by default (`display: none`, button "View Solutions"); one click
    reveals (`display: block`, button "Hide Solutions"); a second click hides again. Confirmed
    across both clicks.
  - Back button returns to `#practice-sets` (19 cards); an out-of-range `#practice-sets-99` falls
    back to the Practice Sets index.
  - Dark mode: after the body color transition settles, the section headers and the unit heading
    resolve to the dark text color `rgb(224,224,224)`, the placeholder to the dark secondary
    `rgb(154,160,176)`, and the toggle to the dark accent `rgb(187,134,252)`, confirming theme
    inheritance through the reused classes.
  - Server stopped and all `.playwright-mcp` and screenshot artifacts removed.

## 4. Immediate next actions
- Push done this session. Next: author the actual practice problems and worked solutions per unit
  and inject them into the two containers (the standalone LaTeX cheat sheet and review guides are a
  natural content source). Remaining prior open items are unchanged: drop the real PDFs into
  `app/assets/pdfs/`, and optionally give `#interactives` the same data-driven treatment.
- Pre-existing UNRELATED working-tree changes (deleted/reorganized PDFs and cheat sheets, plus some
  untracked `ODE-Manager-Local` notes) remain unstaged, as in prior sessions.

---
---

# SESSION LOG, 2026-06-18 (Quizzes Index upgraded to a two-level directory)

Branch: `master`. Session start HEAD: `387bd94` (prior dynamic-UI-indices commit).
Scope: refactored the `#quizzes-index` route into a two-level directory so a student can reach
both the Micro Practice and the Unit Mastery quizzes for any unit from one hub. One app file
touched (`app/js/router.js`) plus this session-state append, in one refactor/docs commit, pushed
to `origin master`. No CSS change: the sub-view reuses existing classes.

---

## 1. Completed Tasks and File Modifications

### 1.1 New nested route `#quizzes-index-[N]` (`app/js/router.js`)
- Added a router branch after the exact `#quizzes-index` case: a `/^#quizzes-index-\d+$/` test
  parses the unit index from the hash, calls `renderUnitQuizzesDetail(container, qi)` when it is a
  valid unit index (0 through 18), and falls back to `renderQuizzesIndex` for an out-of-range index.
  The exact `#quizzes-index` case is checked first so the two never collide.

### 1.2 `renderQuizzesIndex` is now the top level of the directory
- Replaced the flat dashboard of mastery launch cards with a grid of 19 unit cards (reusing
  `.materials-card` in a `.toc-grid`), each with the unit title, description, and a primary
  `.pdf-download-btn` whose label changed from "Launch Practice" to "View Unit Quizzes" and whose
  href is `#quizzes-index-[N]`. The intro subhead was reworded to explain the new hub behavior.

### 1.3 New sub-view `renderUnitQuizzesDetail(container, unitIndex)`
- Renders a "Back to Quizzes Index" button (sets the hash to `quizzes-index`), a `.toc-intro`
  header with the unit title and a subhead, then a responsive `.toc-grid` of Micro Practice launch
  cards, one per video that has a `QUIZ_DATA.micro_practice[video_id]` entry, iterating the unit's
  modules and videos from `CURRICULUM`. Each micro card is mounted with
  `QuizEngine.mount(host, { id: "micro::" + video.video_id, title: video.title, ... })` and the
  `.video-quiz-host` wrapper. The inline flag is intentionally omitted so the surface is a launch
  card that opens the modal runner, matching the mastery card. Below the grid, the unit mastery
  quiz is mounted in a `.unit-mastery-host` with id `"mastery::" + unitData.unit`, anchored at the
  bottom exactly like the unit detail view.
- Progress sync: because the micro cards reuse the same `micro::<video_id>` ids as the unit
  detail inline runner, and the mastery card reuses `mastery::<unit title>`, a student's solved
  questions appear identically on both surfaces.

## 2. Architecture notes (delta this session)
- No engine/state/schema/CSS changes. The two-level directory is pure router logic reusing the
  existing quiz engine and existing classes (`.toc-grid`, `.materials-card`, `.video-quiz-host`,
  `.unit-mastery-host`, `.toc-intro`). The launch-card-versus-inline distinction is controlled
  solely by the presence of the `inline` flag in the mount config, so the same micro quiz id
  renders inline in a unit and as a launch card in the quizzes sub-view without any engine change.
- Video titles shown on the micro cards come straight from `CURRICULUM_DATA` (the curated YouTube
  titles), some of which contain ampersands. These are source data rendered the same way the unit
  detail view already renders them, not UI prose authored this session, so the copy rule (which
  governs prose and comments I generate) is not violated.

## 3. Verification performed
- `node --check app/js/router.js`: parse clean. Copy-rule scan: no em-dashes or en-dashes in the
  file; the only ampersands are `&&` operators in code.
- Browser (static server on `app/`, fresh port to dodge the JS cache, browser-test MCP):
  - Top level `#quizzes-index`: 19 unit cards, every primary button reads "View Unit Quizzes", and
    the Unit 9 card links to `#quizzes-index-9`.
  - Sub-view `#quizzes-index-9`: "Back to Quizzes Index" button, heading "Unit 9: ...", 15 Micro
    Practice launch cards (matching the 15 Unit 9 videos that have quizzes), each a real launch
    card (first reads "Second Order Equations", "Progress: 0 of 5 complete"), and the Unit Mastery
    launch card at the bottom ("Progress: 0 of 30 complete").
  - Progress sync proven: marking one question correct under `micro::xCCeV-glFdM` via
    `ODEState.setQuizAnswerCorrect` and re-rendering flipped the first micro card to "Progress: 1
    of 5 complete" and its button to "Resume Practice"; the temporary state was cleared afterward.
  - Launch works: clicking a micro card's button opened the modal runner titled with the video and
    "Question 1 of 5". The Back button returned to `#quizzes-index` (19 cards). An out-of-range
    `#quizzes-index-99` fell back to the top-level index.
  - Dark mode: micro launch cards resolve to `rgb(30,30,30)` background with the dark panel border,
    and the mastery card keeps its dark accent border `rgb(187,134,252)`, confirming theme
    inheritance through the reused classes.
  - Server stopped and all `.playwright-mcp` and screenshot artifacts removed.

## 4. Immediate next actions
- Push done this session. The Quizzes Index is now a complete two-level hub. Remaining open items
  from prior logs are unchanged: drop the real PDFs into `app/assets/pdfs/` to make the cheat-sheet
  and topic-guide links live, build the dedicated HTML review guides the Practice Sets buttons will
  mount, and optionally give `#interactives` the same data-driven treatment.
- Pre-existing UNRELATED working-tree changes (deleted/reorganized PDFs and cheat sheets, plus some
  untracked `ODE-Manager-Local` notes) remain unstaged, as in prior sessions.

---
---

# SESSION LOG, 2026-06-18 (Dynamic UI indices for cheat sheets, practice sets, and quizzes)

Branch: `master`. Session start HEAD: `d79f199` (prior route-handling and PDF-scaffolding commit).
Scope: replaced the placeholder scaffolding for `#cheat-sheets`, `#practice-sets`, and
`#quizzes-index` with dynamic, data-driven Tables of Contents that mirror the main curriculum UI.
Two app files touched (`app/js/router.js`, `app/css/main.css`) plus this session-state append, in
one feature/docs commit, pushed to `origin master`. `#interactives` is unchanged (still the
placeholder; not in scope this session).

---

## 1. Completed Tasks and File Modifications

### 1.1 `AVAILABLE_MATERIALS` config (`app/js/router.js`)
- Added a module-level `AVAILABLE_MATERIALS` object keyed by unit array index (0 through 18). Each
  present key holds `{ subtopics: [ { title, file } ] }`, where `file` is a basename resolved under
  `assets/pdfs/`. Populated focused topic guides for Units 9 through 18 (for example "Abel's
  Identity" under Unit 9 and "Exponential Response Formula" under Unit 10); Units 0 through 8 carry
  no subtopics and render the primary cheat sheet only. Every primary unit cheat sheet is derived
  from the index by the renderer, so only the optional guides live in the map.

### 1.2 `renderCheatSheets(container)` (`app/js/router.js`)
- Now data-driven over `CURRICULUM`. Builds a back button and a Table-of-Contents-style intro
  (reusing `.toc-intro`, `.toc-heading`, `.toc-subhead`) via a new shared `buildIndexShell` helper,
  then a responsive `.toc-grid` of one `.materials-card` per unit. Each card shows the unit title
  and description, a primary `.pdf-download-btn` linking to `assets/pdfs/Unit-[N]-Cheat-Sheet.pdf`
  with the `download` attribute, and, when `AVAILABLE_MATERIALS[index]` lists subtopics, a nested
  "Topic guides" list of `.pdf-download-link` anchors with their own distinct download targets.

### 1.3 `renderPracticeSets(container)` (`app/js/router.js`)
- Same per-unit card iteration as Cheat Sheets, tailored for web-based practice. Each card carries a
  primary action button labeled "Open Unit [N] Practice Set". INTERIM TARGET: the button links to
  `#unit-N`, whose detail view already hosts the micro practice and the mastery quiz; a dedicated
  HTML review guide will replace this target once those guides are built. No subtopic lists here.

### 1.4 `renderQuizzesIndex(container)` and the `#quizzes-index` route (`app/js/router.js`)
- New `renderQuizzesIndex` builds the same shell and a `.toc-grid`, then for each unit that has a
  `QUIZ_DATA.unit_mastery[unitData.unit]` entry mounts a `.unit-mastery-host` through
  `QuizEngine.mount` with id `"mastery::" + unitData.unit`. This reuses the existing quiz engine
  launch card (title, progress line, and Launch/Resume/Review button) verbatim, and the shared
  mastery id keeps progress in sync with the unit detail view. The router branch for
  `#quizzes-index` now calls this instead of the placeholder. All 19 units have a mastery quiz, so
  the dashboard shows 19 launch cards.

### 1.5 Styling (`app/css/main.css`)
- Added a `.materials-card` block after the `.pdf-download-btn` rules: a static panel container
  mirroring the unit card look (`var(--panel-bg)`, `var(--panel-border)`, radius, shadow) but as a
  div that holds its own action buttons. Added `.materials-card-title`, `.materials-card-desc`, a
  `.materials-card .pdf-download-btn { align-self: flex-start }` rule so the primary button hugs its
  content, `.materials-subtopics` (top border separator), `.materials-subtopics-label`, and a reset
  `.materials-subtopics-list`. Added `.pdf-download-link`: a lighter accent-colored topic-guide link
  with a small `::before` accent square marker, hover `var(--accent-soft)` background, and a
  focus-visible outline, all theme-variable driven. The grid itself reuses `.toc-grid`, so the
  responsive `auto-fill minmax(300px, 1fr)` behavior comes for free.

## 2. Architecture notes (delta this session)
- No engine/state/schema changes. `renderStaticPage` remains (still used by `#interactives`). The
  three new index pages follow the same DOM-building idiom as `renderTableOfContents` and reuse its
  grid and intro classes, so they match the curriculum styling and responsiveness automatically.
- `AVAILABLE_MATERIALS` is the new single source for optional per-unit topic-guide PDFs. The primary
  per-unit cheat sheet name is a convention (`Unit-[N]-Cheat-Sheet.pdf`), not stored in the map.
- The Quizzes Index shares the `mastery::<unit title>` quiz id with the unit detail view by design,
  so a student's mastery progress is identical whether opened from a unit or from the index.
- All referenced PDF targets under `assets/pdfs/` are scaffolded links; the actual PDF files are not
  yet present (populating them is future work). Only `.gitkeep` exists in that directory so far.

## 3. Verification performed
- `node --check app/js/router.js`: parse clean.
- Copy-rule scan on both changed files: no em-dashes, en-dashes, or standalone ampersands; the only
  `&` are `&&` operators in code.
- Browser (static server on `app/`, fresh port to dodge the JS cache noted last session,
  browser-test MCP):
  - Cheat Sheets: 19 cards; Unit 9 shows its primary "Download Unit 9 Cheat Sheet" plus 4 topic
    guides (first is "Abel's Identity" to `assets/pdfs/Unit-9-Abels-Identity.pdf`); Unit 0 has the
    primary only and no subtopics. Screenshots confirm the 3-column responsive grid and the nested
    topic-guide lists with accent markers.
  - Practice Sets: 19 cards; Unit 3 button reads "Open Unit 3 Practice Set" and links to `#unit-3`;
    no subtopic lists.
  - Quizzes Index: 19 mastery launch cards; first reads "Unit 0: What are Differential Equations"
    with "Progress: 0 of 30 complete" and a "Launch Practice" button. Clicking it opens the full
    quiz modal ("Question 1 of 30"), confirming the reused engine launch logic works from the index.
  - Dark mode: `.materials-card` resolves to `rgb(30,30,30)` background with the dark panel border,
    and `.pdf-download-link` to the dark accent `rgb(187,134,252)`, confirming theme inheritance.
  - Server stopped and all `.playwright-mcp` and screenshot artifacts removed.

## 4. Immediate next actions
- Push done this session. Next natural steps: produce and drop the actual PDFs into
  `app/assets/pdfs/` to make the cheat-sheet and topic-guide links live, and build the dedicated
  HTML review guides that the Practice Sets buttons will eventually mount (currently they route to
  the unit detail view). The `#interactives` route is still the placeholder and could get the same
  data-driven treatment (an index of the per-module interactive checkpoints).
- Pre-existing UNRELATED working-tree changes (deleted/reorganized PDFs and cheat sheets, plus some
  untracked `ODE-Manager-Local` notes) remain unstaged, as in prior sessions.

---
---

# SESSION LOG, 2026-06-18 (Route handling for global navigation and PDF scaffolding)

Branch: `master`. Session start HEAD: `ef13179` (prior global-nav-dropdown commit).
Scope: wired the four global navigation dropdown hashes into the router, added scaffolded
placeholder pages for them, created the PDF storage directory, and added a reusable
`.pdf-download-btn` component. Three tracked files touched (`app/js/router.js`,
`app/css/main.css`, new `app/assets/pdfs/.gitkeep`) plus this session-state append, in one
feature/docs commit, pushed to `origin master`.

---

## 1. Completed Tasks and File Modifications

### 1.1 PDF storage directory (`app/assets/pdfs/`)
- Created `app/assets/pdfs/` to hold compiled downloadable curriculum PDFs. Git does not track
  empty directories, so a `.gitkeep` placeholder (a short comment explaining the directory's
  purpose) was added to persist it in the repo.

### 1.2 Route handling (`app/js/router.js`)
- `renderCurriculum()` now checks the hash FIRST for the four static navigation routes before
  falling through to the existing unit/Table-of-Contents logic: `#practice-sets` to
  `renderPracticeSets`, `#cheat-sheets` to `renderCheatSheets`, `#quizzes-index` and
  `#interactives` to `renderStaticPage` directly. Unmatched hashes keep the prior behavior
  (`#unit-N` detail view, anything else the Table of Contents), so unit routing is unchanged.
- Added a shared `renderStaticPage(container, title, message)` helper that clears the container,
  renders a "Back to Table of Contents" button (reusing the existing `.back-to-toc-btn` and
  `.unit-detail-nav` styles and the hash-clear navigation), a themed `<h1 class="static-page-title">`,
  and a `<p class="static-page-placeholder">` message inside a `<section class="static-page">`.
- Added `renderPracticeSets` and `renderCheatSheets` wrappers that call `renderStaticPage` with
  their titles ("Practice Sets" and "Cheat Sheets (PDFs)") and the placeholder "Content coming
  soon." The `renderCheatSheets` comment notes the page will later host PDF links from
  `app/assets/pdfs/` via the `.pdf-download-btn` component.
- The existing async `hashchange` listener already calls `renderCurriculum`, so the dropdown
  links activate the new routes with no extra wiring.

### 1.3 Styling (`app/css/main.css`)
- Added a `.static-page` / `.static-page-title` / `.static-page-placeholder` block after the
  nav-dropdown rules. The title uses `var(--text-color)` with a `2px` `var(--accent-color)`
  bottom border (matching the Table-of-Contents heading treatment); the placeholder uses
  `var(--text-secondary)`. The section inherits the page width from the `.app-container` wrapper.
- Added the reusable `.pdf-download-btn` component: an `inline-flex` anchor styled as a primary
  action button, background `var(--accent-color)`, text `var(--bg-color)` (which gives correct
  contrast in BOTH themes, white text on purple in light, dark text on lavender in dark), border
  `var(--accent-color)`, bold, no underline. Hover swaps to `var(--accent-soft)` background with
  `var(--accent-color)` text and a `var(--shadow-color)` lift; `:focus-visible` draws a
  `var(--accent-color)` outline; `:active` nudges down 1px. No hardcoded hex.

## 2. Architecture notes (delta this session)
- The router gains a static-page branch in front of the unit/TOC branch. No engine/state/schema
  changes; the quiz subsystem and checkpoint registry are untouched. `renderStaticPage` follows
  the same DOM-building idiom as `renderUnitDetail` and reuses existing nav/back-button classes.
- The four hash targets now resolve to real scaffolded pages. Their actual content (a Practice
  Sets index, a Cheat Sheets PDF list reading from `app/assets/pdfs/`, a Quizzes Index, and an
  Interactives gallery) is future work.

## 3. Verification performed
- `node --check app/js/router.js`: parse clean.
- Copy-rule scan on both changed files: no em-dashes, en-dashes, or standalone ampersands; the
  only `&` are `&&` operators in code, which the copy rule permits.
- Browser (static server on `app/`, browser-test MCP): all four routes render the scaffolding
  (correct `<h1>` titles "Practice Sets", "Cheat Sheets (PDFs)", "Quizzes Index", "Interactives",
  each with "Content coming soon." and a working Back button); the four dropdown links carry the
  matching hrefs; unit detail (`#unit-0`) and the Table of Contents still render. The injected
  `.pdf-download-btn` resolves to `inline-flex`, hugs its content (184px), and inherits theme
  colors in both light (bg `rgb(98,0,238)`, text white) and dark (bg `rgb(187,134,252)`, text
  `rgb(18,18,18)`). Title border tracks the accent in both themes. Servers stopped and all
  `.playwright-mcp` and screenshot artifacts removed.
- CACHE NOTE for next session: the browser-test MCP aggressively caches subresources by URL.
  Editing a `.js` file and re-navigating to the same `index.html` serves the STALE script (the
  `<script src>` URL is unchanged, so a cache-bust query on the HTML alone does not refresh it).
  Serve on a FRESH PORT (a new origin gives a clean cache) to verify JS edits.

## 4. Immediate next actions
- Push done this session. Next natural steps: populate `app/assets/pdfs/` and build the real
  Cheat Sheets list using `.pdf-download-btn`, then flesh out the Practice Sets, Quizzes Index,
  and Interactives pages (none requested yet).
- Pre-existing UNRELATED working-tree changes (deleted/reorganized PDFs and cheat sheets, plus
  some untracked `ODE-Manager-Local` notes) remain unstaged, as in prior sessions.

---
---

# SESSION LOG, 2026-06-18 (CSS-only global navigation header dropdown)

Branch: `master`. Session start HEAD: `7342b17` (prior interactive-checkpoint wiring commit).
Scope: added a CSS-only global navigation dropdown to the header, requiring zero JavaScript.
Two app files touched (`app/index.html`, `app/css/main.css`) plus this session-state append, in
one feature/docs commit, pushed to `origin master`.

---

## 1. Completed Tasks and File Modifications

### 1.1 Markup (`app/index.html`)
- Injected a `<nav class="nav-dropdown">` block as the FIRST child of `.header-controls`, before
  the `.mode-switch` group, so it sits to the left of the mode buttons and theme toggle.
- Structure: a `<button class="nav-dropdown-trigger">` carrying `aria-haspopup="true"` and
  `aria-expanded="false"`, with a "Browse" label span and an `aria-hidden` arrow span, followed
  by a `<ul class="nav-dropdown-menu" role="menu">` of four `<li role="none">` items each
  wrapping an `<a role="menuitem">`.
- The four links (text to href): "Practice Sets" to `#practice-sets`, "Cheat Sheets (PDFs)" to
  `#cheat-sheets`, "Quizzes Index" to `#quizzes-index`, "Interactives" to `#interactives`. The
  target sections are placeholder hashes; no in-page anchors with those ids exist yet (future work).

### 1.2 Styling (`app/css/main.css`)
- Added one cohesive block after the `.theme-toggle-btn:hover` rule, before the progress banner.
- Expansion is purely CSS: the menu is hidden by default (`opacity: 0`, `visibility: hidden`,
  `pointer-events: none`, `transform: translateY(-6px)`) and revealed by `.nav-dropdown:hover`
  and `.nav-dropdown:focus-within` (keyboard path), which set `opacity: 1`, `visibility: visible`,
  `pointer-events: auto`, `transform: translateY(0)`. The visibility transition is delayed on
  close and immediate on open so the fade-out animates cleanly.
- The indicator arrow is a CSS triangle that rotates 180 degrees on hover and focus-within, with
  a `transform` transition. Opacity and transform transitions are 0.2s ease.
- Color mapping is variable-only, NO hardcoded hex: trigger and panel use `var(--panel-bg)`,
  `var(--text-color)`, `var(--panel-border)`; the panel shadow uses `var(--shadow-color)`; hover
  and focus states use `var(--accent-soft)` and `var(--accent-color)`. Verified to inherit Light
  and Dark theme values from `theme.css`.

## 2. Architecture notes (delta this session)
- No JavaScript, no engine/router/state/schema changes. The dropdown is a self-contained markup
  plus CSS addition; it does not touch the quiz subsystem or checkpoint registry.
- The theme is applied via `data-theme` on the document element (`light`/`dark`); the new styles
  inherit through the same CSS custom properties as the existing header controls.

## 3. Verification performed
- Copy-rule scan on both changed files: zero em-dashes, en-dashes, or ampersands ("Cheat Sheets
  (PDFs)" uses parentheses, not an ampersand).
- Browser (static server on `app/` port 8765, browser-test MCP): default state hidden
  (`opacity 0`, `visibility hidden`, `pointer-events none`, arrow not rotated); opens on hover
  and on keyboard `:focus-within`; four links resolve to the correct hrefs and carry
  `role="menuitem"`; trigger carries `aria-haspopup="true"` and `aria-expanded="false"`; menu
  carries `role="menu"`. Light and dark screenshots confirm theme inheritance (dark trigger text
  resolves to `rgb(224,224,224)`). At 414px width the header wraps with no horizontal overflow and
  the open panel stays within the viewport (left 32, right 224 inside a 399px client width).
  Server stopped and `app/.playwright-mcp/` plus screenshot artifacts removed.

## 4. Immediate next actions
- Push done this session. The four hash targets are placeholders; building the actual
  `#practice-sets`, `#cheat-sheets`, `#quizzes-index`, and `#interactives` landing sections is
  open future work (none requested).
- Pre-existing UNRELATED working-tree changes (deleted/reorganized PDFs and cheat sheets) remain
  unstaged, as in prior sessions. They were not bundled with this commit.

---
---

# SESSION LOG, 2026-06-17 (Interactive checkpoints wired for Units 13 through 18)

Branch: `master`. Session start HEAD: `9ac1bac` (prior Units 17/18 quiz-suite docs commit).
Scope: wired and authored the interactive checkpoint widgets for Units 13 through 18. Units 13 and
14 already had complete widget files (`widgets-unit13.js`, `widgets-unit14.js`) that were never
loaded by `index.html`, so their checkpoints showed the "under construction" placeholder; Units 15
through 18 had no widget files at all. After this session every interactive checkpoint from Unit 13
onward resolves to a real, first-principles widget. One feature commit (the five touched app files
plus this session-state append). NOT yet pushed at the time of writing (push when asked).

---

## 1. Completed Tasks and File Modifications

### 1.1 Wired the pre-existing Unit 13 and Unit 14 widgets
- `app/index.html`: added `<script src="js/checkpoints/widgets-unit13.js">` and
  `widgets-unit14.js` after `widgets-unit12.js`, before `quiz-data.js`. No content changes; both
  files were already complete (5 and 4 registered renderers) and only needed loading.

### 1.2 Authored four new widget files (22 net-new renderers)
All follow the exact `widgets-unit13/14.js` pattern: an IIFE calling
`CheckpointRegistry.register(id, fn)`, building via `CheckpointCore` helpers (`shell`,
`buildExpressionTask`, `buildLogicGate`, `buildCategorySorter`, `desmosGraph`) and the checkers
`expressionsMatch`/`numberEquals`. Math drawn from the established per-unit quiz suites.
- `app/js/checkpoints/widgets-unit15.js` (7): `system_conversion_stepper`,
  `eigenvalue_method_stepper`, `complex_eigenvalue_spiral_visualizer` (Desmos spiral sink),
  `matrix_exponential_calculator` (expressionsMatch `e^(2*t)`), `fundamental_matrix_builder`,
  `system_solution_verifier`, `system_particular_solution_builder`.
- `app/js/checkpoints/widgets-unit16.js` (4): `phase_portrait_classifier` (category sorter, shared by
  modules 16.1 and 16.2), `linearization_jacobian_checker`, `nullcline_sketcher` (Desmos nullclines),
  `nonlinear_phase_portrait_matcher`. `desmos_predator_prey_simulator` NOT re-authored (already
  registered in Unit 4).
- `app/js/checkpoints/widgets-unit17.js` (6): `ivp_bvp_classifier`, `two_point_bvp_solver`
  (expressionsMatch `2*x`, `x^3`), `eigenfunction_matcher`, `bvp_shooting_visualizer`,
  `orthogonality_integral_checker`, `sturm_liouville_form_transformer`.
- `app/js/checkpoints/widgets-unit18.js` (5): `fourier_series_builder` (Desmos square-wave partial
  sum), `fourier_coefficient_calculator`, `sine_cosine_series_selector`,
  `separation_of_variables_stepper`, `heat_equation_simulator` (Desmos heat decay).
  `contour_map_reader` NOT re-authored (already registered in Unit 6).

### 1.3 Wired Units 15 through 18 into the page
- `app/index.html`: added four `<script>` tags for `widgets-unit15.js`..`widgets-unit18.js` after
  `widgets-unit14.js`, before `quiz-data.js`.

## 2. Architecture notes (delta this session)
- No engine/registry/core changes. The `CheckpointRegistry` (id-keyed `RENDERERS`, placeholder
  fallback) and the `CheckpointCore` builder toolkit documented in earlier logs are unchanged and
  remain accurate. This session is pure widget content plus six `<script>` tags.
- Shared checkpoint ids (`desmos_predator_prey_simulator`, `contour_map_reader`,
  `phase_portrait_classifier` across two modules) are reused via id-keyed registration, not
  duplicated.
- Copy rule honored (no em/en-dashes, no ampersands); matrices described by rows in words and
  vectors as tuples; KaTeX only inside Desmos `latex` strings (double-backslashed in source).

## 3. Verification performed
- `node --check` on all six widget files: parse clean.
- Registry coverage probe (stub `CheckpointCore.shell` + minimal `document`, call
  `CheckpointRegistry.render` for every Unit 13-18 checkpoint id from `curriculum-data.js`): all 25
  Unit 15-18 instances resolve to a real renderer, zero placeholders.
- Copy-rule ripgrep scan on the four new files: clean.
- Browser (static server on `app/` port 8765, browser-test MCP): page loads with only the benign
  favicon 404; all 24 new/shared ids render a real widget (Begin button, no placeholder) and build
  their body on click with zero exceptions; `CheckpointCore` checkers accept the intended correct
  answers (`e^(2t)`, `2x`, `x^3`, `x`, `1/2`, `-1`) and reject wrong ones; all six Desmos LaTeX
  strings render with no expression error. Server stopped and `app/.playwright-mcp/` removed.

## 4. Immediate next actions
- Push `origin master` when asked.
- Possible future work (none requested): Units 0-12 already have full checkpoint coverage; the
  interactive-checkpoint backlog is now complete for all units. Remaining optional items from prior
  logs (quizzes index/landing UI, spaced-repetition review, cross-unit cumulative final) are
  untouched.

---
---

# SESSION LOG, 2026-06-17 (Units 17 and 18 quiz suite generation) -- FINAL BATCH, CURRICULUM COMPLETE

Branch: `master`. Session start HEAD: `42ed992` (prior Units 15 and 16 quiz-suite docs commit).
Session end HEAD (pushed): this docs/feature commit
(`feat/docs: generate Units 17 and 18 quiz suites and append session state save`) advances HEAD
one further and is pushed at session close.
Scope of this session: generated the complete quiz suites for BOTH Unit 17 ("Boundary Value
Problems and Sturm-Liouville Theory") and Unit 18 ("Fourier Series and Partial Differential
Equations") in a single batch, the FINAL content batch. Twenty-four Micro-Practice quizzes (one
per video across all 12 modules, 5 items each = 120 items) plus two 30-question Unit Mastery
quizzes (60 items), all appended to `app/js/quiz-data.js` and synced to the
`app/data/quizzes.json` mirror. Authored via a new retained combined generator at
`scripts/gen_units17_18_quizzes.py`, following the workflow in the `quiz-authoring-workflow`
project memory. One feature/docs commit, COMMITTED and PUSHED. ALL 19 UNITS (Unit 0 through Unit
18) ARE NOW DONE; the curriculum quiz content is COMPLETE.

---

## 1. Completed Tasks and File Modifications

### 1.1 Unit 17 Micro-Practice quizzes (12 videos, 60 items)
- Appended 12 `micro_practice` keys to `app/js/quiz-data.js` (5 items each, IDs
  `mp_<video_id>_<n>`), inserted via the unique splice anchor before `unit_mastery: {`.
  No existing data overwritten; the prior final micro array picked up a trailing comma only.
- video_ids by module (from `app/js/curriculum-data.js`, authoritative):
  - 17.1 Initial versus Boundary Value Problems: `Izmkk1Ry3Wc`, `E97SZm2ZrBo`
  - 17.2 Two-Point Boundary Value Problems: `AnyGw-gOY0U`, `8y0IcYqPIgA`
  - 17.3 Eigenvalues and Eigenfunctions of BVPs: `vGwyyXWGR3Y`, `Elk0gjSd4c8`
  - 17.4 The Shooting Method: `qIfxydBEdzg`, `tMO28AakkZ8`
  - 17.5 Orthogonal Functions: `J3S4lBADQVw`, `ZYf0tz9oVz8`
  - 17.6 Sturm-Liouville Theory: `Oz_cnaz9zSo`, `l9Dc1ab9RaU`
- Pedagogy: theory-and-technique unit, conceptual classification balanced with the eigenvalue and
  inner-product computations the topics admit. Items require the IVP-versus-BVP distinction
  (conditions at one point versus two), the two-conditions-for-second-order count, the BVP
  no/one/infinitely-many outcome (versus the IVP uniqueness guarantee), Dirichlet (`y`) versus
  Neumann (`y'`) conditions, worked two-point solves (`y'' = 0`, `y(0)=0,y(1)=2 -> 2x`; `y'' = 6x`,
  `y(0)=0,y(1)=1 -> x^3`; `y'' + y = 0`, `y(0)=0,y(pi/2)=1 -> sin x`; the `y(0)=y(pi)=0` infinitely
  many `B sin x` case), the homogeneous trivial solution, the Dirichlet eigenvalues
  `lambda_n = (n pi / L)^2` and eigenfunctions `sin(n pi x / L)` with the worked `L = pi -> n^2` and
  `L = 2 -> (n pi / 2)^2`, the Neumann `cos(n pi x / L)` family with the `n = 0` constant mode, the
  nonpositive-lambda-gives-trivial fact, the eigenvalues-increase-without-bound fact, the shooting
  method (BVP to IVP via a guessed initial slope, tune until the far condition is met,
  root-finding on `F(s) = y(b;s) - beta`, two-shots-and-interpolate for linear problems,
  Newton/bisection for nonlinear, reuse of Runge-Kutta integrators), the function inner product
  `int_a^b f g dx` and orthogonality and the norm, the worked
  `int sin(n pi x / L) sin(m pi x / L) = 0` (m != n), `L/2` (m = n), the cosine and sine-cosine
  orthogonality on `[-pi, pi]`, the orthogonal trig set and orthonormal definition, and
  Sturm-Liouville theory (the self-adjoint form `(p y')' + (q + lambda w)y = 0`, regular problem
  with separated BCs, real eigenvalues, weight-`w` orthogonality, the increasing unbounded
  spectrum, the `p = 1, q = 0, w = 1` reduction of `y'' + lambda y = 0`, and the complete
  eigenfunction basis).

### 1.2 Unit 18 Micro-Practice quizzes (12 videos, 60 items)
- Appended 12 `micro_practice` keys (5 items each) in the same micro splice, immediately after the
  Unit 17 keys. Module 18.5 correctly handled as a single-video module (one micro key); module
  18.6 correctly handled as a three-video module.
- video_ids by module:
  - 18.1 Fourier Series and Transform Intuition: `r6sGWTCMz2k`, `spUNpyF58BY`
  - 18.2 Computing Fourier Series: `wmCIrpLBFds`, `ijQaTAT3kOg`
  - 18.3 Fourier Series for Differential Equations: `JaiSBThC6wM`, `jDLCxErtwTs`
  - 18.4 Separable Partial Differential Equations: `dIsxevQhzic`, `17WkgjkENV0`
  - 18.5 The Laplacian: `EW08rD-GFh0` (single video)
  - 18.6 The Heat and Wave Equations: `yK-xFXATzyQ`, `6lbSrBmGUik`, `BNULJwNPmFk`
- Pedagogy: Fourier-and-PDE unit, the visual 3Blue1Brown intuition balanced with the coefficient
  integrals and PDE classifications the topics demand. Items require the Fourier-series-as-sum-of-
  harmonics idea and the rotating-vector picture and the square-wave-needs-infinitely-many fact and
  the heat-flow origin, the Fourier transform as a continuous spectrum with the winding center-of-
  mass detector and the time-versus-frequency domains and series-for-periodic-transform-for-
  nonperiodic and the present-frequency peak, the coefficient formulas
  (`a_0 = (1/L) int_{-L}^L f`, `a_n = (1/L) int f cos(n pi x / L)`, `b_n = (1/L) int f sin`), the
  period `2L`, the orthogonality basis of the formulas, the even-gives-cosines and
  odd-gives-sines parity rules (worked `x^2` even), the average-equals-`a_0/2` fact, the
  jump-converges-to-midpoint rule, the full series and periodic extension and piecewise-smooth
  convergence and the Gibbs overshoot and periodic-forcing use, half-range sine (odd extension,
  fixed-zero ends) and cosine (even extension, insulated ends) series with the `b_n = (2/L) int_0^L`
  coefficient, separation of variables (`u = X(x)T(t)`, the common separation constant, two ODEs,
  the spatial eigenvalue problem), the classical PDEs (heat `u_t = alpha u_xx`, wave
  `u_tt = c^2 u_xx`, Laplace `u_xx + u_yy = 0`, Laplace as the steady state, superposition-plus-
  Fourier for initial data), the Laplacian (`u_xx + u_yy`, the neighbor-average comparison, the
  positive-Laplacian-below-average sign, harmonic functions, the shared spatial operator), the heat
  solution (`sum b_n sin(n pi x / L) e^{-k(n pi / L)^2 t}`, high modes decay fastest, decay to
  zero, sine-series initial data), the wave solution (oscillatory `cos`/`sin(n pi c t / L)` time
  factors, `c` as wave speed, two initial conditions for second-order-in-time, d'Alembert
  `f(x - ct) + g(x + ct)`), and Duhamel's principle (forced wave equation, superposing impulse
  responses, homogeneous-plus-particular, `f(x,t)` as the source, linearity as the basis).

### 1.3 Unit 17 and Unit 18 Mastery quizzes (30 items each)
- Appended 2 `unit_mastery` keys, `"Unit 17: Boundary Value Problems and Sturm-Liouville Theory"`
  (IDs `um_17_1` .. `um_17_30`) and `"Unit 18: Fourier Series and Partial Differential Equations"`
  (IDs `um_18_1` .. `um_18_30`), exact unit titles from `curriculum-data.js`, via the end-of-file
  splice anchor. Both blocks emitted in one splice joined by a comma; the prior final array
  (Unit 16) picked up a trailing comma only.
- Unit 17 mastery spans all six modules: the IVP/BVP distinction and two-conditions count and the
  no/one/infinitely-many outcome and Dirichlet/Neumann conditions and the steady-heat application,
  worked two-point solves (`y'' = 0`, `y(0)=1,y(2)=5 -> 1 + 2x`; `y'' = 2`, `y(0)=y(1)=0 -> x^2 - x`;
  the `y(0)=y(pi)=0` infinitely-many case), the general-solution-then-apply-conditions method, the
  trivial solution, the Dirichlet eigenvalues `(n pi / L)^2` and `sin(n pi x / L)` and worked
  `L = pi -> n^2`, the Neumann `cos(n pi x / L)` family, the unbounded spectrum, the shooting
  method (BVP to IVP, tune the slope, two-shots interpolation, the `F(s) = 0` root, Runge-Kutta
  reuse), the function inner product and orthogonality and the worked sine orthogonality (`0` and
  `L/2`) and the orthogonal trig set and the coefficient-decoupling payoff, and Sturm-Liouville
  theory (the self-adjoint form, real eigenvalues, weight-`w` orthogonality, the
  `p = 1, q = 0, w = 1` reduction, the complete basis).
- Unit 18 mastery spans all six modules: the Fourier-series harmonic sum and the square-wave
  infinite-series fact, the transform continuous spectrum and series-versus-transform and the
  present-frequency peak, the coefficient formula `a_n` and the average-equals-`a_0/2` and the
  odd-gives-sines parity and the worked `x^2` cosine series and the jump-midpoint rule, the
  half-range sine (odd extension, fixed-zero ends) and cosine (insulated ends) series and the
  Gibbs overshoot and the `b_n = (2/L) int_0^L` coefficient, separation of variables (`u = XT`, the
  common constant, two ODEs, the spatial eigenvalue problem, superposition-plus-Fourier for initial
  data), the Laplacian (`u_xx + u_yy`, the neighbor-average comparison, harmonic functions, the
  positive-below-average sign, the shared operator), and the heat/wave equations (the heat and wave
  forms, exponential decay versus oscillation, two initial conditions for the wave equation, and
  d'Alembert's traveling-wave solution).

### 1.4 Combined single-source-of-truth generator `scripts/gen_units17_18_quizzes.py` (RETAINED)
- Modeled on the retained `scripts/gen_units15_16_quizzes.py`, authoring two units at once.
  Content authored once as Python dicts with single-backslash KaTeX raw strings; `json.dumps`
  then emits both (i) the JS object-literal splices patched into `quiz-data.js` via the unique
  anchors, and (ii) the `app/data/quizzes.json` mirror via `json.dump(indent=2)`, so JS-source
  and JSON escaping stay identical (`\\frac` -> single backslash at runtime).
- Two-unit design: separate `MASTERY_17` and `MASTERY_18` lists (populated by `m17`/`m18`
  helpers); `update_js()` emits both `unit_mastery` blocks in a single end-of-file splice joined
  by `",\n\n"`, and `update_json()` assigns both unit keys. The 24 micro blocks emit in one micro
  splice.
- Built-in validation: exactly 24 micro videos, 5-items-per-video, two 30-item mastery lists,
  exactly one `correct` option and 4 options per item, a global duplicate-ID guard across all
  items, and a hard reject of any em-dash, en-dash, or ampersand in user-facing strings. All
  asserts passed (`24 micro videos, 30 + 30 mastery items, copy rules OK`).
- COPY-RULE CAVEAT honored: no KaTeX matrix environments (whose `&` column separator is forbidden);
  this unit pair needed none. Intervals are written as `[a, b]` and ranges with the word "to", and
  "Sturm-Liouville" and "two-point" use plain hyphen-minus, never en-dashes.

### 1.5 Verification (clean KaTeX and structural integrity)
- A validate-only dry run (importing the module and calling `validate()` without splicing) passed
  before any file mutation, confirming counts and copy rules up front.
- `node`/`vm` eval of `quiz-data.js`: file parses cleanly; both new unit keys present with 30 items
  each; all 24 Unit 17/18 video keys resolve with 5 items each (120 total); exactly one correct
  + 4 options per item across all 1820 items in the file. micro keys 226 -> 250 (+24),
  unit_mastery 17 -> 19 (+2). Zero structural problems. THIS COMPLETES ALL 19 UNITS (Unit 0
  through Unit 18) and the curriculum's 250 video micro-practice keys.
- Runtime escaping: scanned the actual runtime string VALUES (not the JSON.stringify
  representation, which re-escapes a single backslash to `\\`): 0 real double-backslash occurrences
  across 1800 new strings, confirming single-backslash KaTeX at runtime. Copy-rule scan on the same
  runtime strings: 0 em-dash/en-dash/ampersand. Doubled-word scan: 0 hits.
- JS<->JSON mirror: 0 mismatches across all keys; 250 micro keys and 19 mastery keys in both.
  `difflib.SequenceMatcher` confirmed both files changed by PURE INSERTS (quiz-data.js +2106
  inserts / 0 deletes / 1 boundary replace-op for the trailing comma; quizzes.json +4372 inserts /
  0 deletes / 0 replaces).
- Runtime KaTeX confirmed by installing `katex@0.16.8` (the exact CDN version pinned in
  `app/index.html`) in a temp dir and rendering every extracted `$...$` and `$$...$$` segment from
  the new content with `throwOnError: true`, `$$`-before-`$` precedence: 947 segments rendered, 0
  failures, 0 unbalanced `$` strings. Temp install removed afterward.

---

## 2. Architecture State (delta this session)

- No engine/router/state/schema changes. The quiz subsystem and `QUIZ_DATA` schema documented in
  earlier logs remain current and accurate. This session was pure content authoring.
- CONTENT NOW COMPLETE: all 19 units (Unit 0 through Unit 18) have full Micro-Practice (one quiz
  per video, 5 items each) and Unit Mastery (30 items each) coverage. Source totals after this
  session: `micro_practice` 250 keys, `unit_mastery` 19 keys; `app/data/quizzes.json` deep-equals
  source across all keys.
- Five retained combined generators now exist alongside the single-unit ones:
  `gen_unit3_quizzes.py`, `gen_unit4_quizzes.py`, `gen_units5_6_quizzes.py`,
  `gen_units7_8_quizzes.py`, `gen_units9_10_quizzes.py`, `gen_units11_12_quizzes.py`,
  `gen_units13_14_quizzes.py`, `gen_units15_16_quizzes.py`, and now
  `gen_units17_18_quizzes.py` (the final one).

---

## 3. Immediate Next Actions

### 3.1 Content backlog -- CLEARED
- micro_practice: 0 videos remain (all 250 done across Units 0 through 18).
- unit_mastery: 0 units remain (all 19 done). The per-unit quiz-authoring backlog is COMPLETE.
- Possible future work (none requested): a polished quizzes index/landing UI, spaced-repetition
  review, a cross-unit cumulative final, or wiring the interactive checkpoints listed per module in
  `curriculum-data.js` that are not yet implemented. None of these are started.

### 3.2 Per-batch checklist (kept for reference even though the backlog is cleared)
1. Append to `app/js/quiz-data.js` only; do not overwrite existing keys. Unique IDs
   `mp_<video_id>_<n>` and `um_<unit>_<n>`. Exactly one `correct`; rationale on every option;
   `hint` per item; Math Confidence Reset (no incorrect rationale reveals the answer).
2. Enforce copy rule (no em-dashes, en-dashes, ampersands); double-backslash KaTeX in JS source.
3. Validate-only dry run, then `node`-eval `quiz-data.js`, then assert JS<->JSON mirror deep-equal.
4. Confirm pure-insert via `difflib`; render every KaTeX segment against `katex@0.16.8`.
5. Commit staging ONLY the intended files. Push `origin master` when asked.

### 3.3 Git State at session close
- HEAD advances by this docs/feature commit on `master`, pushed to `origin/master`.
- Pre-existing, UNRELATED, still-unstaged working-tree changes (do NOT bundle with app work):
  deleted tracked PDFs/images and reorganized untracked PDF folders and cheat sheets (see
  `git status`). Ampersands in those PDF filenames are unrelated to the app copy rule. These were
  deliberately left unstaged throughout.
- Git warns LF -> CRLF on edited files (Windows autocrlf); harmless.

---
---

# SESSION LOG, 2026-06-17 (Units 15 and 16 quiz suite generation)

Branch: `master`. Session start HEAD: `9603fa9` (prior Units 13 and 14 quiz-suite docs commit).
Session end HEAD (pushed): this docs/feature commit
(`feat/docs: generate Units 15 and 16 quiz suites and append session state save`) advances HEAD
one further and is pushed at session close.
Scope of this session: generated the complete quiz suites for BOTH Unit 15 ("Systems of Linear
Differential Equations") and Unit 16 ("Phase Plane Analysis and Nonlinear Dynamics") in a single
batch. Twenty-six Micro-Practice quizzes (one per video across all 13 modules, 5 items each = 130
items) plus two 30-question Unit Mastery quizzes (60 items), all appended to `app/js/quiz-data.js`
and synced to the `app/data/quizzes.json` mirror. Authored via a new retained combined generator
at `scripts/gen_units15_16_quizzes.py`, following the workflow in the `quiz-authoring-workflow`
project memory. One feature/docs commit, COMMITTED and PUSHED. Units 0 through 16 are now done;
Units 17 through 18 remain (the curriculum runs Unit 0 through Unit 18, 19 units total).

---

## 1. Completed Tasks and File Modifications

### 1.1 Unit 15 Micro-Practice quizzes (14 videos, 70 items)
- Appended 14 `micro_practice` keys to `app/js/quiz-data.js` (5 items each, IDs
  `mp_<video_id>_<n>`), inserted via the unique splice anchor before `unit_mastery: {`.
  No existing data overwritten; the prior final micro array picked up a trailing comma only.
- video_ids by module (from `app/js/curriculum-data.js`, authoritative):
  - 15.1 Converting Higher-Order Equations to Systems: `cq3bPBePE8E`, `_yXKdyjzMBI`
  - 15.2 The Eigenvalue Method for Linear Systems: `Pkw8QEt0Nm4`, `Zwb5eiYcL8w`
  - 15.3 Complex Eigenvalues: `N8bpFOndEQU`, `TRVS5Wo9LoM`
  - 15.4 The Matrix Exponential: `O85OWBJ2ayo`, `LwSk9M5lJx4`
  - 15.5 Fundamental Matrices: `eI8k_j7v0Nk`, `Ybmb0BJ0GPc`
  - 15.6 Homogeneous Systems, Worked Examples: `RUpJUNjGmFI`, `qpWV8Pd9tao`
  - 15.7 Nonhomogeneous Systems: `eKG47g-qbDw`, `C5SO-XAIG_g`
- Pedagogy: systems unit, computation-heavy where the techniques allow. Items require the
  higher-order-to-system conversion (`x_1 = y`, `x_2 = y'`, worked `y'' + 3y' + 2y = 0` companion
  rows `(0,1)` and `(-2,-3)`, the n-th-order-gives-n-equations count, worked `y'' = -y` and
  `y''' = y` systems), the eigenvalue trial `e^{lambda t} v` reducing to `A v = lambda v` and
  `det(A - lambda I) = 0`, the superposition general solution, worked characteristic equations and
  eigenvalues (rows `(0,1),(-2,-3) -> -1,-2`; rows `(2,1),(1,2) -> 3,1` with eigenvector `(1,1)`;
  triangular rows `(1,1),(0,2) -> 1,2`), complex eigenvalues (rows `(0,1),(-1,0) -> +/-i`,
  conjugate pairs, Euler split into `e^{at} cos bt` and `e^{at} sin bt`, real/imaginary parts as
  two real solutions, `alpha < 0` spiral sink, `alpha = 0` center, worked rows `(-1,-1),(1,-1) ->
  -1 +/- i`), the matrix exponential (series `I + At + (At)^2/2! + ...`, `x = e^{At} x_0`,
  `e^{A0} = I`, `d/dt e^{At} = A e^{At}`, diagonal `x'=2x,y'=3y -> e^{2t},e^{3t}`, `A = PDP^{-1}
  -> P e^{Dt} P^{-1}`, `e^{(A+B)t} = e^{At} e^{Bt}` iff `AB = BA`), fundamental matrices (columns
  are independent solutions, `Phi' = A Phi`, general solution `Phi c`, `e^{At} = Phi(t) Phi(0)^{-1}`,
  IVP propagator `Phi(t) Phi(t_0)^{-1} x_0`, nonzero Wronskian, non-uniqueness up to `Phi C`),
  worked homogeneous systems (rows `(1,1),(4,1) -> 3,-1` saddle with eigenvector `(1,2)`; rows
  `(3,1),(1,3) -> 4,2` source with orthogonal eigenvectors `(1,1)`,`(1,-1)`), and nonhomogeneous
  systems (`x = x_h + x_p`, undetermined-coefficients constant-forcing guess and resonance
  `t e^{lambda t}` fix, the steady state `x_p = -A^{-1} g`, variation of parameters
  `x_p = Phi int Phi^{-1} g dt` with its general-forcing advantage and the `int_{t_0}^t` IVP form).

### 1.2 Unit 16 Micro-Practice quizzes (12 videos, 60 items)
- Appended 12 `micro_practice` keys (5 items each) in the same micro splice, immediately after the
  Unit 15 keys.
- video_ids by module:
  - 16.1 Phase Portraits of Linear Systems: `UO_dgXa5szg`, `fsnEIYEZc3Y`
  - 16.2 Sources, Sinks, Saddles, Spirals, and Centers: `VqXKa11IA6A`, `n9H-6TQIEJc`
  - 16.3 Linearization Near Equilibria: `RCWkzzLgwf0`, `UCpMao94iFg`
  - 16.4 Nullclines and Stability for Nonlinear Systems: `ifkvwzkm2Qc`, `qocE0phJ4t8`
  - 16.5 Nonlinear Phase Portraits: `BaVfVVMcxQ4`, `vBwyD4JJlSs`
  - 16.6 Predator-Prey Dynamics in the Phase Plane: `ccIl0clxJpE`, `Zg9k9ijiYPA`
- Pedagogy: geometric/dynamics unit, conceptual classification balanced with the determinant and
  Jacobian computations the topics admit. Items require the phase-portrait definition (trajectories
  in the state plane, origin equilibrium for invertible `A`, straight-line trajectories along
  eigenvectors, slow-eigenvalue tangency at a node), the full equilibrium taxonomy by eigenvalue
  sign (source = both positive, sink = both negative, saddle = opposite signs with `det A < 0`,
  spiral = complex with nonzero real part, center = purely imaginary with `tr A = 0` and `det A >
  0`) with worked classifications (rows `(-1,0),(0,-2)` sink; rows `(1,0),(0,-1)` saddle; rows
  `(2,0),(0,3)` source; rows `(0,1),(-1,0)` center), separatrices along the saddle eigenvectors,
  linearization (the Jacobian rows `(f_x, f_y),(g_x, g_y)`, equilibria where `f = g = 0`, worked
  pendulum Jacobian rows `(0,1),(-1,0)` at the origin, stability from Jacobian eigenvalues,
  Hartman-Grobman hyperbolicity and the borderline imaginary-eigenvalue center failure, worked
  `-2,-5` node and `3,-1` saddle), nullclines (the `x`-nullcline `f = 0`, equilibria at
  nullcline intersections, vertical flow on the `x`-nullcline, worked `x' = x - y -> y = x`,
  multiple equilibria, the qualitative global portrait), nonlinear phase portraits (autonomy as no
  explicit `t`, non-crossing of trajectories by uniqueness, find-and-linearize-each-equilibrium,
  isolated limit cycles as a nonlinear phenomenon, separatrices as basin boundaries, sketching
  nullclines first then local Jacobian structure), and predator-prey (the `y` predator with
  `-cy` decline and `+dxy` gain, prey `x' = ax` exponential growth without predators, predator
  `y' = -cy` decay without prey, coexistence equilibrium `x = c/d`, `y = a/b`, the encounter-term
  meaning of `xy`, the center closed-orbit oscillation, the lagging predator peak, the origin
  saddle from eigenvalues `a` and `-c`, and the logistic refinement stabilizing coexistence).

### 1.3 Unit 15 and Unit 16 Mastery quizzes (30 items each)
- Appended 2 `unit_mastery` keys, `"Unit 15: Systems of Linear Differential Equations"` (IDs
  `um_15_1` .. `um_15_30`) and `"Unit 16: Phase Plane Analysis and Nonlinear Dynamics"` (IDs
  `um_16_1` .. `um_16_30`), exact unit titles from `curriculum-data.js`, via the end-of-file
  splice anchor. Both blocks emitted in one splice joined by a comma; the prior final array
  (Unit 14) picked up a trailing comma only.
- Unit 15 mastery spans all seven modules: the conversion (`y'' + 5y' + 6y` companion form,
  fourth-order-gives-four-equations, worked `y'' + 4y` rows `(0,1),(-4,0)`, the rationale), the
  eigenvalue method (trial, `A v = lambda v`, `det(A - lambda I) = 0`, superposition, worked rows
  `(2,1),(1,2) -> 3,1`), complex eigenvalues (rows `(0,1),(-1,0) -> +/-i`, conjugate pairs, the
  `alpha`/`beta` roles, the `alpha = 0` center), the matrix exponential (series, `e^{At} x_0`,
  `d/dt e^{At} = A e^{At}`, `P e^{Dt} P^{-1}`, diagonal `x'=-x,y'=4y -> e^{-t},e^{4t}`),
  fundamental matrices (independent-solution columns, `Phi' = A Phi`, `Phi(0) = I` normalization,
  general solution `Phi c`), worked homogeneous systems (rows `(1,1),(4,1) -> 3,-1` saddle with
  eigenvector `(1,2)`; rows `(3,1),(1,3) -> 4,2` source), and nonhomogeneous systems (`x_h + x_p`,
  steady state `-A^{-1} g`, variation of parameters formula, resonance `t e^{lambda t}` fix).
- Unit 16 mastery spans all six modules: phase portraits (definition, origin equilibrium,
  eigenvector straight lines, slow-direction tangency, decay to origin), the taxonomy (source,
  saddle via `det A < 0`, spiral, center via `tr A = 0` and `det A > 0`, worked rows
  `(-1,0),(0,-3)` sink), linearization (the Jacobian, equilibria `f = g = 0`, rows
  `(f_x,f_y),(g_x,g_y)`, worked pendulum Jacobian, Hartman-Grobman hyperbolicity), nullclines
  (the `x`-nullcline `f = 0`, intersection equilibria, vertical flow, worked `x' = x - y -> y = x`,
  evaluate-Jacobian-at-the-equilibrium), nonlinear phase portraits (autonomy, non-crossing,
  isolated limit cycles, find-and-linearize strategy, separatrices), and predator-prey (the `y`
  predator identification, prey exponential growth without predators, coexistence `x = c/d`,
  `y = a/b`, center closed orbits, the origin saddle from `a` and `-c`).

### 1.4 Combined single-source-of-truth generator `scripts/gen_units15_16_quizzes.py` (RETAINED)
- Modeled on the retained `scripts/gen_units13_14_quizzes.py`, authoring two units at once.
  Content authored once as Python dicts with single-backslash KaTeX raw strings; `json.dumps`
  then emits both (i) the JS object-literal splices patched into `quiz-data.js` via the unique
  anchors, and (ii) the `app/data/quizzes.json` mirror via `json.dump(indent=2)`, so JS-source
  and JSON escaping stay identical (`\\frac` -> single backslash at runtime).
- Two-unit design: separate `MASTERY_15` and `MASTERY_16` lists (populated by `m15`/`m16`
  helpers); `update_js()` emits both `unit_mastery` blocks in a single end-of-file splice joined
  by `",\n\n"`, and `update_json()` assigns both unit keys. The 26 micro blocks emit in one micro
  splice.
- Built-in validation: exactly 26 micro videos, 5-items-per-video, two 30-item mastery lists,
  exactly one `correct` option and 4 options per item, a global duplicate-ID guard across all
  items, and a hard reject of any em-dash, en-dash, or ampersand in user-facing strings. All
  asserts passed (`26 micro videos, 30 + 30 mastery items, copy rules OK`).
- COPY-RULE / MATRIX CAVEAT enforced for both units: KaTeX matrix environments use `&` as a column
  separator, which is forbidden, so all matrices are described by their rows in words
  (e.g. "the matrix with rows `(2, 1)` and `(1, 2)`") and vectors as tuples `(x, y)`, never via
  `\begin{bmatrix}`/`\begin{pmatrix}`. Eigenvalue and Jacobian questions give matrices this way,
  and decoupled systems (`x' = 2x`, `y' = 3y`) describe diagonal cases.
- Anti-truncation: the generator was assembled incrementally via the editor in module-sized
  chunks behind a `# <<<CONTENT_INSERTION_POINT>>>` sentinel (helper functions `C`/`W`/`item`
  keep the dictionaries compact), well within output limits.

### 1.5 Verification (clean KaTeX and structural integrity)
- A validate-only dry run (importing the module and calling `validate()` without splicing) passed
  before any file mutation, confirming counts and copy rules up front. A pre-splice `$`-balance
  scan (0 unbalanced), doubled-word scan (0 hits), and ID-consistency check (all micro IDs prefixed
  `mp_<video_id>_`, mastery IDs `um_15_1..30` and `um_16_1..30` in sequence) all passed.
- `node`/`vm` eval of `quiz-data.js`: file parses cleanly; both new unit keys present with 30 items
  each; all 26 Unit 15/16 video keys resolve with 5 items each (130 total); exactly one correct
  + 4 options per item across all 1640 items in the file. micro keys 200 -> 226 (+26),
  unit_mastery 15 -> 17 (+2). Zero structural problems.
- Runtime escaping: 0 double-backslash occurrences in any new runtime string, confirming
  single-backslash KaTeX (`"\\frac"` in source -> `\frac` at runtime).
- JS<->JSON mirror: 0 mismatches across all keys; 226 micro keys and 17 mastery keys in both.
  `difflib.SequenceMatcher` confirmed the mirror change is PURE INSERTS (35237 equal lines, 4616
  inserts, 0 deletes, 0 replaces); the `git diff --numstat` figure (9609/4993) was Myers
  brace-matching noise, not real deletions. `quiz-data.js` is +2226 with 0 deletions.
- Runtime KaTeX confirmed by installing `katex@0.16.8` (the exact CDN version pinned in
  `app/index.html`) in a temp dir and rendering every extracted `$...$` and `$$...$$` segment
  from the new content with `throwOnError: true`, `$$`-before-`$` precedence: 1504 segments
  rendered, 0 failures. Temp install removed afterward.

---
---

# SESSION LOG, 2026-06-17 (Units 13 and 14 quiz suite generation)

Branch: `master`. Session start HEAD: `1834b53` (prior Units 11 and 12 quiz-suite docs commit).
Session end HEAD (pushed): this docs/feature commit
(`feat/docs: generate Units 13 and 14 quiz suites and append session state save`) advances HEAD
one further and is pushed at session close.
Scope of this session: generated the complete quiz suites for BOTH Unit 13 ("Series Solutions")
and Unit 14 ("Linear Algebra Foundations for Systems") in a single batch. Eighteen Micro-Practice
quizzes (one per video across all 9 modules, 5 items each = 90 items) plus two 30-question Unit
Mastery quizzes (60 items), all appended to `app/js/quiz-data.js` and synced to the
`app/data/quizzes.json` mirror. Authored via a new retained combined generator at
`scripts/gen_units13_14_quizzes.py`, following the workflow in the `quiz-authoring-workflow`
project memory. One feature/docs commit, COMMITTED and PUSHED. Units 0 through 14 are now done;
Units 15 through 18 remain (the curriculum runs Unit 0 through Unit 18, 19 units total).

---

## 1. Completed Tasks and File Modifications

### 1.1 Unit 13 Micro-Practice quizzes (10 videos, 50 items)
- Appended 10 `micro_practice` keys to `app/js/quiz-data.js` (5 items each, IDs
  `mp_<video_id>_<n>`), inserted via the unique splice anchor before `unit_mastery: {`.
  No existing data overwritten; the prior final micro array picked up a trailing comma only.
- video_ids by module (from `app/js/curriculum-data.js`, authoritative):
  - 13.1 Taylor Series and the Exponential Function: `ZcvxlNKKY_Q`, `ebfOSDj4j3I`
  - 13.2 Power Series Solutions: `xeeM3TT4Zgg`, `6csP7dw0XTY`
  - 13.3 The Frobenius Method: `_qQLuxYClA4`, `58_qJyfVl-Y`
  - 13.4 Legendre and Bessel Equations: `3e5BUrtUKZc`, `uLORiAWe63A`
  - 13.5 Hermite, Laguerre, and Chebyshev Polynomials: `1WlTe-DzO5E`, `sHlIF-YZ9Yw`
- Pedagogy: series unit, computation-heavy where the techniques allow. Items require the
  Maclaurin series of `e^x` (`sum x^n/n!`) and the coefficient drills (`x^4 -> 1/24`,
  `x^3 -> 1/6`), the Taylor coefficient formula `f^{(n)}(a)/n!`, the Maclaurin-is-center-zero
  fact, the geometric radius `R = 1` and the worked `sum x^n/2^n -> R = 2`, the `cos x` and
  `sin x` series, the power-series ansatz `y = sum a_n x^n` and the `y'` and `y''` index shifts,
  the recurrence from `y' = y` (`a_{n+1} = a_n/(n+1)`) with worked `a_2 = 1/2` and `a_3 = 1/6`
  rebuilding `e^x`, the ordinary-point definition and the `y'' + y = 0` recurrence
  (`a_{n+2} = -a_n/((n+2)(n+1))`) giving `cos`/`sin`, the radius-bounded-by-nearest-singular-point
  rule, the regular-singular-point definition, the Frobenius form `sum a_n x^{n+r}`, the indicial
  equation `r(r-1) + p_0 r + q_0 = 0` with worked roots (`x^2 y'' + x y' - y -> r = +/-1` and
  `2r^2 + r - 1 -> r = 1/2, -1`), the repeated-root log term and integer-difference case, the
  `a_0 x^r` leading behavior, Legendre's equation and `P_2 = (3x^2-1)/2` and the `x = +/-1`
  singular points and orthogonality, Bessel's equation and its regular singular point and
  indicial roots `r = +/-nu` and the `J_nu ~ x^nu` behavior and cylindrical applications, Hermite
  (`H_2 = 4x^2 - 2`, weight `e^{-x^2}`, quantum harmonic oscillator), Laguerre (the `x y'' +
  (1-x)y' + n y = 0` form, weight `e^{-x}`), and Chebyshev (`T_2 = 2x^2 - 1`,
  `T_n(cos theta) = cos(n theta)`, weight `1/sqrt(1-x^2)`, the equioscillation property).

### 1.2 Unit 14 Micro-Practice quizzes (8 videos, 40 items)
- Appended 8 `micro_practice` keys (5 items each) in the same micro splice, immediately after the
  Unit 13 keys.
- video_ids by module:
  - 14.1 Vectors, Span, and Linear Transformations: `k7RM-ot2NWY`, `kYB8IZa5AuE`
  - 14.2 The Determinant and Change of Basis: `Ip3X9LOh2dk`, `P2LTAUO1TdA`
  - 14.3 Eigenvalues and Eigenvectors: `PFDu9oVAE-g`, `e50Bj7jn9IQ`
  - 14.4 Eigenvalues Through Differential Equations: `QYS-ML_vn4k`, `Cose44Lgssw`
- Pedagogy: geometric linear-algebra unit, computation balanced with the conceptual content the
  3Blue1Brown-style modules demand. Items require the linear-combination definition and the
  worked `2(1,0) + 3(0,1) = (2,3)`, span and basis (independence plus spanning), the span of two
  independent plane vectors being all of `R^2`, the linear-transformation identity
  `T(au + bv) = aT(u) + bT(v)`, columns as basis images, matrix-vector products by row dotting
  (worked rows `(2,0),(0,3)` on `(1,1) -> (2,3)` and rows `(1,2),(3,4)` on `(1,1) -> (3,7)`),
  composition as matrix product, the determinant as area scaling and the `ad - bc` formula with
  worked `rows (3,1),(2,4) -> 10`, the `det = 0` collapse and dependent columns, the negative-det
  orientation flip, change of basis (columns as new basis in old coordinates, the `P^{-1} A P`
  similarity, eigenbasis diagonalization), the eigenvector definition `A v = lambda v`, the
  characteristic equation `det(A - lambda I) = 0`, diagonal and triangular eigenvalues read off
  the diagonal, the `2x2` characteristic quadratic `lambda^2 - (tr)lambda + det = 0`, the
  trace/determinant trick `lambda = m +/- sqrt(m^2 - p)` with worked `rows (2,1),(1,2) -> 3, 1`
  and `rows (3,1),(1,3) -> 4, 2`, trace as eigenvalue sum, the `x' = Ax` trial `e^{lambda t} v`
  reducing to `A v = lambda v`, the decoupling along an eigenvector into `x' = lambda x`, the
  superposition general solution `c_1 e^{lambda_1 t} v_1 + c_2 e^{lambda_2 t} v_2` with worked
  diagonal system, the sign-of-lambda stability reading, and complex eigenvalues giving
  oscillation.

### 1.3 Unit 13 and Unit 14 Mastery quizzes (30 items each)
- Appended 2 `unit_mastery` keys, `"Unit 13: Series Solutions"` (IDs `um_13_1` .. `um_13_30`) and
  `"Unit 14: Linear Algebra Foundations for Systems"` (IDs `um_14_1` .. `um_14_30`), exact unit
  titles from `curriculum-data.js`, via the end-of-file splice anchor. Both blocks emitted in one
  splice joined by a comma; the prior final array (Unit 12) picked up a trailing comma only.
- Unit 13 mastery spans all five modules: the Taylor coefficient formula and the `e^x`/`sin`
  series and coefficient drills, Maclaurin center, the geometric radius `R = 1`, the power-series
  ansatz and the `y' = y` recurrence with worked `a_3 = 1/6`, ordinary points and the `y'' + y`
  recurrence, the radius-to-nearest-singularity rule, the regular-singular-point definition and
  Frobenius form and indicial equation with worked `r = +/-1`, the repeated-root log term and the
  `a_0 x^r` leading behavior, Legendre's equation and `P_2` and the `x = +/-1` singularities and
  orthogonality, Bessel's regular singular point and indicial roots `+/-nu`, Hermite (`H_2`,
  weight `e^{-x^2}`), Chebyshev (`T_2`, `T_n(cos theta) = cos n theta`), and the Laguerre
  hydrogen-atom application.
- Unit 14 mastery spans all four modules: linear combination, span, basis conditions, the span of
  two independent plane vectors, the worked `3(1,0) + 2(0,1) = (3,2)`, the linearity identity,
  columns as basis images, the worked product `rows (1,2),(3,4)` on `(1,1) -> (3,7)`, the
  determinant as area scaling and `ad - bc` with worked `rows (2,3),(1,4) -> 5`, `det = 0` and
  negative-det meanings, change of basis and the `P^{-1} A P` similarity and eigenbasis
  diagonalization, the eigenvector definition and the characteristic equation, diagonal and
  triangular eigenvalues, trace-as-sum and determinant-as-product, the `2x2` characteristic
  quadratic and the trick with worked `rows (2,1),(1,2) -> 3, 1`, the `x' = Ax` trial reducing to
  `A v = lambda v`, the superposition general solution with worked diagonal system, the
  sign-of-lambda stability, and complex eigenvalues giving oscillation.

### 1.4 Combined single-source-of-truth generator `scripts/gen_units13_14_quizzes.py` (RETAINED)
- Modeled on the retained `scripts/gen_units11_12_quizzes.py`, authoring two units at once.
  Content authored once as Python dicts with single-backslash KaTeX raw strings; `json.dumps`
  then emits both (i) the JS object-literal splices patched into `quiz-data.js` via the unique
  anchors, and (ii) the `app/data/quizzes.json` mirror via `json.dump(indent=2)`, so JS-source
  and JSON escaping stay identical (`\\frac` -> single backslash at runtime).
- Two-unit design: separate `MASTERY_13` and `MASTERY_14` lists (populated by `m13`/`m14`
  helpers); `update_js()` emits both `unit_mastery` blocks in a single end-of-file splice joined
  by `",\n\n"`, and `update_json()` assigns both unit keys. The 18 micro blocks emit in one
  micro splice.
- Built-in validation: exactly 18 micro videos, 5-items-per-video, two 30-item mastery lists,
  exactly one `correct` option and 4 options per item, a global duplicate-ID guard across all
  items, and a hard reject of any em-dash, en-dash, or ampersand in user-facing strings. All
  asserts passed (`18 micro videos, 30 + 30 mastery items, copy rules OK`).
- COPY-RULE / MATRIX CAVEAT enforced for Unit 14: KaTeX matrix environments use `&` as a column
  separator, which is forbidden, so all matrices are described by their rows in words
  (e.g. "the matrix with rows `(2, 1)` and `(1, 2)`") and vectors as tuples `(x, y)`, never via
  `\begin{bmatrix}`/`\begin{pmatrix}`. Eigenvalue questions give the matrix this way.
- Anti-truncation: the generator was assembled incrementally via the editor in module-sized
  chunks behind a `# <<<CONTENT_INSERTION_POINT>>>` sentinel (helper functions `C`/`W`/`item`
  keep the dictionaries compact), well within output limits.

### 1.5 Verification (clean KaTeX and structural integrity)
- A validate-only dry run (importing the module and calling `validate()` without splicing) passed
  before any file mutation, confirming counts and copy rules up front.
- `node` eval of `quiz-data.js`: file parses cleanly; both new unit keys present with 30 items
  each; all 18 Unit 13/14 video keys resolve with 5 items each (90 total); exactly one correct
  + 4 options per item across all 1450 items in the file. micro keys 182 -> 200 (+18),
  unit_mastery 13 -> 15 (+2). Zero structural problems.
- Runtime escaping: 0 double-backslash occurrences in any new runtime string, confirming
  single-backslash KaTeX (`"\\frac"` in source -> `\frac` at runtime).
- JS<->JSON mirror: 0 mismatches across all keys; 200 micro keys and 15 mastery keys in both.
  `difflib.SequenceMatcher` confirmed both files changed by PURE INSERTS (quiz-data.js +1746,
  quizzes.json +3640, both with 0 deletions and 0 replaced lines).
- Runtime KaTeX confirmed by installing `katex@0.16.8` (the exact CDN version pinned in
  `app/index.html`) in a temp dir and rendering every extracted `$...$` and `$$...$$` segment
  from the new content with `throwOnError: true`, `$$`-before-`$` precedence: 1516 segments
  rendered, 0 failures. Temp install removed afterward.
- Copy-rule scan (0 em-dashes/en-dashes/ampersands across 1500 new strings), `$`-balance scan
  (0 unbalanced), and doubled-word scan (0 hits) all clean on the runtime strings.

---
---

# SESSION LOG, 2026-06-17 (Units 11 and 12 quiz suite generation)

Branch: `master`. Session start HEAD: `7a80df3` (prior Units 9 and 10 quiz-suite docs commit).
Session end HEAD (pushed): this docs/feature commit
(`feat/docs: generate Units 11 and 12 quiz suites and append session state save`) advances HEAD
one further and is pushed at session close.
Scope of this session: generated the complete quiz suites for BOTH Unit 11 ("Mechanical
Vibrations and Oscillators") and Unit 12 ("The Laplace Transform") in a single batch.
Thirty-seven Micro-Practice quizzes (one per video across all 18 modules, 5 items each = 185
items) plus two 30-question Unit Mastery quizzes (60 items), all appended to
`app/js/quiz-data.js` and synced to the `app/data/quizzes.json` mirror. Authored via a new
retained combined generator at `scripts/gen_units11_12_quizzes.py`, following the workflow in the
`quiz-authoring-workflow` project memory. One feature/docs commit, COMMITTED and PUSHED. Units 0
through 12 are now done; Units 13 through 18 remain (the curriculum runs Unit 0 through Unit 18,
19 units total).

---

## 1. Completed Tasks and File Modifications

### 1.1 Unit 11 Micro-Practice quizzes (12 videos, 60 items)
- Appended 12 `micro_practice` keys to `app/js/quiz-data.js` (5 items each, IDs
  `mp_<video_id>_<n>`), inserted via the unique splice anchor before `unit_mastery: {`.
  No existing data overwritten; the prior final micro array picked up a trailing comma only.
- video_ids by module (from `app/js/curriculum-data.js`, authoritative):
  - 11.1 Simple Harmonic Motion and Hooke's Law: `gZ_KnZHCn4M`, `Z52emur7Rko`
  - 11.2 Free Undamped Vibrations: `f2wGE_n5xtA`, `3F6NlCP4CWU`
  - 11.3 Damped Vibrations and the Damping Discriminant: `CTd1uVq5-l8`, `dQ6_aaFlVTo`
  - 11.4 Forced Vibrations, Beats, and Resonance: `PzuhMbZYVLA`, `7bYgGelMC-E`
  - 11.5 Coupled Oscillators and Normal Modes: `-pXnfzQfupE`, `Ye92jN6FrlU`
  - 11.6 The Harmonic Oscillator Solved Four Ways: `8ePqqZXSMQs`, `obXbCCWEKfY`
- Pedagogy: physics-payoff unit, computation-heavy where the techniques allow. Items require
  Hooke's law `F = -kx` and the restoring-sign meaning, the natural frequency `omega_0 =
  sqrt(k/m)` with worked `2x'' + 18x = 0` giving `3`, period `T = 2 pi / omega_0`, equilibrium
  stretch `mg = kL` (worked `k = 50`), amplitude-phase combination `C = sqrt(c1^2 + c2^2)`
  (worked `3,4 -> 5` and `5,12 -> 13`), free-undamped IVPs (`x'' + 4x = 0 -> cos 2t`,
  `x'' + 9x = 0 -> 2 sin 3t`), the damping term identification and the discriminant
  `c^2 - 4mk` classification, critical damping `c = 2 sqrt(mk)` (worked `m=1,k=9 -> 6`), the
  three solved cases (overdamped `x'' + 5x' + 6x`, critically damped `x'' + 2x' + x`,
  underdamped `x'' + 2x' + 5x -> -1 +/- 2i`), resonance at `omega = omega_0` with the
  `t`-multiply trial and worked `x'' + 25x` giving `omega = 5`, beats for near-but-unequal
  frequencies with the `cos A - cos B` product identity and the `1/(m(omega_0^2 - omega^2))`
  blowup, coupled-oscillator normal modes and the eigenvalue frequencies (`lambda = -1, -3 ->
  omega = 1, sqrt 3`), superposition of modes, the four-ways characteristic roots `+/- i
  omega_0` and the energy first integral, and the oscillator as the universal linearization
  near a stable equilibrium with the LC analog `omega_0 = 1/sqrt(LC)`.

### 1.2 Unit 12 Micro-Practice quizzes (25 videos, 125 items)
- Appended 25 `micro_practice` keys (5 items each) in the same micro splice, immediately after
  the Unit 11 keys. Module 12.1 correctly handled as a three-video module.
- video_ids by module:
  - 12.1 Intuition and Motivation: `j0wJBEZdwLs`, `-j8PzkZ70Lg`, `n2y7n6jw5d0`
  - 12.2 Definition and Computing Transforms: `KqokoYr_h1A`, `hfKycVR4kSw`
  - 12.3 Properties, Linearity, Existence, and Inverses: `zModDQ-ST30`, `D7CP4WTQpBo`
  - 12.4 Transforms of Derivatives and Integrals: `zfhyeXbb0d4`, `F21roAB7Zy0`
  - 12.5 Inverse Transforms and Partial Fractions: `yz-_EKIzz80`, `c6YnYr8KsSo`
  - 12.6 Solving Initial Value Problems: `fuxFrpaMLtw`, `Kdli4yUyu94`
  - 12.7 Heaviside and Piecewise Functions: `zo4Stc-raQE`, `kRyOk1Ab90Q`
  - 12.8 The Dirac Delta and Impulse Forcing: `SxNVcCVj-3c`, `LOoM3qlpYuU`
  - 12.9 Convolution and Periodic Functions: `AgKQQtEc9dk`, `GqsYBeKGdgA`
  - 12.10 The Fourier Connection: `3gjJDuCAEQQ`, `7UvtU75NXTg`
  - 12.11 Frequency Domain and Pole Diagrams: `noycLIZbK_k`, `pUFSXhoazY8`
  - 12.12 Deriving the Transform from First Principles: `zNnHr1bjC04`, `zvbdoSeGAgI`
- Pedagogy: transform unit, computation balanced with the conceptual content the visual and
  first-principles modules demand. Items require the definition `F(s) = int_0^infty e^{-st}
  f(t) dt` and the `t`-to-`s` mapping, the `s = sigma + i omega` decay-vs-oscillation split and
  the differentiation-eigenfunction property of `e^{st}`, the table (`L{1} = 1/s`, `L{e^{at}} =
  1/(s-a)` with `s > a`, `L{t} = 1/s^2`, `L{t^n} = n!/s^{n+1}` worked `t^3 -> 6/s^4`,
  `L{sin bt} = b/(s^2+b^2)`, `L{cos bt} = s/(s^2+b^2)`), linearity and existence
  (piecewise-continuous plus exponential order) with worked `L{3 + 2t}` and `L^{-1}{1/(s-2)} =
  e^{2t}` and `L{4 sin 3t} = 12/(s^2+9)`, the derivative rules `L{y'} = sY - y(0)` and
  `L{y''} = s^2 Y - s y(0) - y'(0)` and the integral rule `F(s)/s`, partial-fraction inverses
  (`1/(s^2+4) -> (1/2) sin 2t`, `s/(s^2+9) -> cos 3t`, `1/((s-1)(s-2)) -> -1/(s-1) + 1/(s-2)`,
  `1/(s(s-2)) -> -1/2 + (1/2) e^{2t}`, `1/(s-1)^2 -> t e^t`, `2/(s-3) -> 2 e^{3t}`), the
  transform-solve-invert IVP procedure with worked `y' - y = 0 -> e^t` and `y' + 2y = 0,
  y(0)=3 -> 3 e^{-2t}` and `y'' - y = 0 -> 1/(s^2-1)`, the Heaviside step and `L{u(t-a)} =
  e^{-as}/s` and the second-shifting theorem `e^{-as} F(s)` with worked `u(t-1)(t-1)^2 ->
  e^{-s} 2/s^3` and `L^{-1}{e^{-3s}/s} = u(t-3)`, the Dirac delta sifting `int f delta(t-a) =
  f(a)` and `L{delta(t-a)} = e^{-as}` and `L{delta(t)} = 1` and the impulse response
  `y'' + y = delta -> sin t` with the velocity-jump and transfer-function reading, convolution
  `int_0^t f(tau) g(t-tau) d tau` and the theorem `L{f*g} = F G` and `1 * 1 = t`, the periodic
  formula `(1/(1-e^{-sT})) int_0^T e^{-st} f dt`, the Fourier-vs-Laplace frequency distinction
  and the `s = i omega` reduction and region of convergence, time-vs-frequency domain and the
  transfer function, pole diagrams (denominator roots, LHP decay, imaginary-axis sustained
  oscillation, RHP instability) with worked poles of `1/((s+1)(s-2))` at `-1, 2`, and the
  first-principles generating-function origin (`x = e^{-s}`, `x^n -> e^{-sn}`, sum -> integral).

### 1.3 Unit 11 and Unit 12 Mastery quizzes (30 items each)
- Appended 2 `unit_mastery` keys, `"Unit 11: Mechanical Vibrations and Oscillators"` (IDs
  `um_11_1` .. `um_11_30`) and `"Unit 12: The Laplace Transform"` (IDs `um_12_1` .. `um_12_30`),
  exact unit titles from `curriculum-data.js`, via the end-of-file splice anchor. Both blocks
  emitted in one splice joined by a comma; the prior final array (Unit 10) picked up a trailing
  comma only.
- Unit 11 mastery spans all six modules: Hooke's law and the restoring sign, `omega_0 =
  sqrt(k/m)`, period, the undamped general solution, free-undamped IVPs and amplitude-phase, the
  damping term and discriminant classification, critical damping `c = 2 sqrt(mk)` (worked
  `m=1,k=25 -> 10`), the three solved damping cases, fastest non-oscillatory return (critically
  damped), resonance condition and the `t`-trial and worked resonance frequency, beats, the
  amplitude blowup as `omega -> omega_0`, coupled-oscillator definition and normal modes and the
  eigenvalue frequencies (`lambda = -1, -4 -> omega = 1, 2`), superposition of modes, the
  characteristic roots `+/- i omega_0`, the energy first integral, and the LC-circuit
  universality.
- Unit 12 mastery spans all twelve modules: the definition integral and pole meaning, the table
  (`L{1}`, `L{e^{at}}`, `L{t^n}`), linearity and existence and `L^{-1}{1/(s-2)}`, the derivative
  rules and the integral rule, partial fractions and the inverses `1/(s^2+4)` and `1/(s-1)^2`,
  the transform-solve-invert procedure with worked `y' + 2y = 0, y(0)=3 -> 3 e^{-2t}`, the
  Heaviside step and `L{u(t-a)}` and the second-shifting theorem, the delta sifting and
  `L{delta(t-a)}` and the delta-as-derivative-of-step relation, convolution and the convolution
  theorem and the periodic formula, the Fourier-vs-Laplace distinction and the `s = i omega`
  reduction, pole diagrams and RHP instability, and the first-principles generating-function
  origin.

### 1.4 Combined single-source-of-truth generator `scripts/gen_units11_12_quizzes.py` (RETAINED)
- Modeled on the retained `scripts/gen_units9_10_quizzes.py`, authoring two units at once.
  Content authored once as Python dicts with single-backslash KaTeX raw strings; `json.dumps`
  then emits both (i) the JS object-literal splices patched into `quiz-data.js` via the unique
  anchors, and (ii) the `app/data/quizzes.json` mirror via `json.dump(indent=2)`, so JS-source
  and JSON escaping stay identical (`\\frac` -> single backslash at runtime).
- Two-unit design: separate `MASTERY_11` and `MASTERY_12` lists (populated by `m11`/`m12`
  helpers); `update_js()` emits both `unit_mastery` blocks in a single end-of-file splice joined
  by `",\n\n"`, and `update_json()` assigns both unit keys. The 37 micro blocks emit in one
  micro splice.
- Built-in validation: exactly 37 micro videos, 5-items-per-video, two 30-item mastery lists,
  exactly one `correct` option and 4 options per item, a global duplicate-ID guard across all
  items, and a hard reject of any em-dash, en-dash, or ampersand in user-facing strings. All
  asserts passed on the run (`37 micro videos, 30 + 30 mastery items, copy rules OK`).
- Anti-truncation: the generator was assembled incrementally via the editor in module-sized
  chunks behind a `# <<<CONTENT_INSERTION_POINT>>>` sentinel (helper functions `C`/`W`/`item`
  keep the dictionaries compact), well within output limits.
- One copy fix applied during authoring: an option in `mp_j0wJBEZdwLs_1` was missing its closing
  `$`, caught by a `$`-balance scan and corrected before splicing.

### 1.5 Verification (clean KaTeX and structural integrity)
- `node` eval of `quiz-data.js`: file parses cleanly; both new unit keys present with 30 items
  each; all 37 Unit 11/12 video keys resolve with 5 items each (185 total); exactly one correct
  + 4 options per item across all 1300 items in the file. micro keys 145 -> 182 (+37),
  unit_mastery 11 -> 13 (+2). Zero structural problems; zero copy-rule violations at runtime.
- JS<->JSON mirror: 0 mismatches across all keys; 182 micro keys and 13 mastery keys in both.
  `difflib.SequenceMatcher` confirmed the mirror change is PURE INSERTS (5958 inserts, 0
  deletes, 0 replaces); `quiz-data.js` is +2886 with 0 deletions and a single boundary
  `replace` (the trailing comma added after the prior final array).
- Runtime KaTeX confirmed by installing `katex@0.16.8` (the exact CDN version pinned in
  `app/index.html`) in a temp dir and rendering every extracted `$...$` and `$$...$$` segment
  from the new content with `throwOnError: true`, `$$`-before-`$` precedence: 2054 segments
  rendered, 0 failures. Temp install removed afterward.
- Copy-rule scan (0 em-dashes/en-dashes/ampersands), doubled-word scan (the lone `x x'` hit is
  legitimate KaTeX math, not prose), and `$`-balance scan (0 unbalanced) all clean on the
  runtime strings.

---
---

# SESSION LOG, 2026-06-17 (Units 9 and 10 quiz suite generation)

Branch: `master`. Session start HEAD: `59143aa` (prior Units 7 and 8 quiz-suite docs commit).
Session end HEAD (pushed): this docs/feature commit
(`feat/docs: generate Units 9 and 10 quiz suites and append session state save`) advances HEAD
one further and is pushed at session close.
Scope of this session: generated the complete quiz suites for BOTH Unit 9 ("Second-Order Linear
Equations, Theory and Structure") and Unit 10 ("Nonhomogeneous Equations and Forced Response") in
a single batch. Thirty-one Micro-Practice quizzes (one per video across all 16 modules, 5 items
each = 155 items) plus two 30-question Unit Mastery quizzes (60 items), all appended to
`app/js/quiz-data.js` and synced to the `app/data/quizzes.json` mirror. Authored via a new
retained combined generator at `scripts/gen_units9_10_quizzes.py`, following the workflow in the
`quiz-authoring-workflow` project memory. One feature/docs commit, COMMITTED and PUSHED. Units 0
through 10 are now done; Units 11 through 18 remain (the curriculum runs Unit 0 through Unit 18,
19 units total).

---

## 1. Completed Tasks and File Modifications

### 1.1 Unit 9 Micro-Practice quizzes (15 videos, 75 items)
- Appended 15 `micro_practice` keys to `app/js/quiz-data.js` (5 items each, IDs
  `mp_<video_id>_<n>`), inserted via the unique splice anchor before `unit_mastery: {`.
  No existing data overwritten; the prior final micro array picked up a trailing comma only.
- video_ids by module (from `app/js/curriculum-data.js`, authoritative):
  - 9.1 Introduction to Second-Order Equations: `xCCeV-glFdM`, `fi54Hz5TiWI`
  - 9.2 Existence, Uniqueness, Superposition, and Independence: `VpZOuOJ_ob4`, `S-wzapPZGzE`
  - 9.3 Linear Independence and the Wronskian: `4z5aL3aGVQs`, `clc2poliklA`
  - 9.4 Abel's Identity: `wJspS9lQsHM`, `h4w3euxv_i4`
  - 9.5 Reduction of Order: `oQSFW8BIrY0`, `i9HSsR8aF4I`
  - 9.6 Constant Coefficient Homogeneous Equations: `znO6v-8pvXo`, `r8Uk4tbuxVE`
  - 9.7 The Cauchy-Euler Equation: `AMixzLG1h5E` (single video)
  - 9.8 Higher-Order Linear Equations: `7vwDp94wEhg`, `vA3MjD0cfS4`
- Pedagogy: structure-and-theory unit, so conceptual checks are balanced with the computational
  items the topics admit. Items require the linear/nonlinear and homogeneous classifications,
  solution verification by substitution (`cos x` into `y'' + y = 0`), the constant count and
  initial-condition count for order two, the Wronskian definition and concrete computations
  (`W` of `{e^x, e^2x}` is `e^{3x}`, of `{x, 2x}` is `0`, of `{sin x, cos x}` is `-1`), the
  nonzero-Wronskian independence test, Abel's identity `W = W(x_0) exp(-int p dx)` with worked
  cases (`y'' - 3y' + 2y = 0` giving `e^{3x}`, `y'' + 2x y' + y = 0` giving `e^{-x^2}`), the
  `W' = -pW` derivation, reduction of order `y_2 = v y_1` with the repeated-root `x e^x` result
  and the `y_1 int exp(-int p)/y_1^2` formula, the characteristic-equation casework (distinct
  real `y'' - 5y' + 6y = 0`, repeated `y'' - 4y' + 4y = 0`, complex `y'' + 4y' + 13y = 0`), the
  discriminant classification, Cauchy-Euler `x^r` trial with worked indicial
  `x^2 y'' - 2x y' - 4y = 0` giving roots `4, -1`, and higher-order theory (n independent
  solutions, n conditions, multiplicity `x^k e^{rx}` terms, conjugate complex pairs, worked
  `y''' - y' = 0`).

### 1.2 Unit 10 Micro-Practice quizzes (16 videos, 80 items)
- Appended 16 `micro_practice` keys (5 items each) in the same micro splice, immediately after
  the Unit 9 keys.
- video_ids by module:
  - 10.1 The Method of Undetermined Coefficients: `P3fc6v191mA`, `AgyeJEO2a-k`
  - 10.2 Variation of Parameters: `Ik3YW1JGr_A`, `wSMad7QpaqE`
  - 10.3 Differential Operators and the Annihilator Method: `gEJvwsMZ3lU`, `LC6T-nUVJac`
  - 10.4 Response to Exponential and Oscillating Inputs: `CB9I4mwpQ5E`, `xw3ccgYhFis`
  - 10.5 Complex Exponential Response and General Inputs: `kIT2uMxYh6M`, `0r2L3wTqkBc`
  - 10.6 Transient and Steady-State Solutions: `S2-26LR8_Es`, `GLd1MgiTne4`
  - 10.7 Unit Step and Impulse Response: `LjqUV6vqwkg`, `q0PxCQWG3ic`
  - 10.8 Duhamel's Principle: `MT2_9Ek9Y4o`, `Tv8vAhkrEXQ`
- Pedagogy: forced-response unit, computation-heavy where the techniques allow. Items require the
  `y = y_h + y_p` structure, undetermined-coefficients trial selection (exponential, full
  polynomial, sine-and-cosine pairing) with the resonance `x`-multiply fix for `y'' + y = cos x`,
  a worked `y'' - y = e^{2x}` giving `A = 1/3`, variation of parameters (the `y_p = u_1 y_1 +
  u_2 y_2` form, the `u_1' y_1 + u_2' y_2 = 0` constraint, Cramer `u_1' = -y_2 g / W`, the full
  two-integral formula, the standard-form normalization caveat, and the `sec x` use case),
  annihilator operators (`(D - 3)` for `e^{3x}`, `D^3` for `x^2`, `(D^2 + 4)` for `cos 2x`,
  `(D-a)^2 + b^2` for `e^{ax} cos bx`, products for sums), the exponential response formula
  `y_p = e^{at}/p(a)` with worked `y'' + 3y' + 2y = e^t` giving `e^t/6` and the resonant
  `t e^{at}/p'(a)` fix, the complex gain `1/p(i omega)` with amplitude `|1/p(i omega)|` and phase
  `arg(1/p(i omega))`, transient (homogeneous, decaying, carries initial-condition dependence)
  versus steady-state (particular, persistent), the delta and step relationship
  (`delta = u'`, sifting `int f delta(t-a) = f(a)`, unit area), and Duhamel convolution
  `int_0^t w(t - tau) f(tau) d tau` with the linearity-plus-time-invariance basis, causality
  limits, and Green's-function identification.

### 1.3 Unit 9 and Unit 10 Mastery quizzes (30 items each)
- Appended 2 `unit_mastery` keys, `"Unit 9: Second-Order Linear Equations, Theory and Structure"`
  (IDs `um_9_1` .. `um_9_30`) and `"Unit 10: Nonhomogeneous Equations and Forced Response"` (IDs
  `um_10_1` .. `um_10_30`), exact unit titles from `curriculum-data.js`, via the end-of-file
  splice anchor. Both blocks emitted in one splice joined by a comma; the prior final array
  (Unit 8) picked up a trailing comma only.
- Unit 9 mastery spans all eight modules: linear form and constant count, existence-uniqueness
  continuity, the superposition zero-versus-2g distinction, fundamental sets, the Wronskian
  definition and the `{e^x, e^3x}` computation (`2 e^{4x}`), the dependence-implies-zero
  result, Abel's identity and the `y'' - 3y' + 2y` worked case, reduction of order and the
  repeated-root `x e^x`, the characteristic casework (distinct, repeated, complex) and the
  discriminant, Cauchy-Euler `x^r` and the worked `x^2 y'' - 2x y' - 4y` giving `x^4` and
  `x^{-1}`, and higher-order theory (n solutions, n conditions, multiplicity terms, conjugate
  pairs, worked `y''' - y' = 0`, superposition at all orders).
- Unit 10 mastery spans all eight modules: the `y_h + y_p` structure, undetermined-coefficients
  trials and the `y'' + y = cos x` resonance fix, the worked `y'' - y = e^{2x}` (`A = 1/3`),
  superposition over summed forcings, variation of parameters (form, constraint, Cramer
  `u_1' = -y_2 g / W`, advantage over guessing, standard-form caveat), annihilators
  (`(D-3)`, `(D^2+4)`), the exponential response formula and the worked
  `y'' + 3y' + 2y = e^t` (`e^t/6`), the resonance meaning of `p(a) = 0`, the amplitude-and-phase
  reshaping of sinusoids, the complex gain content, superposition of inputs, the convolution
  construction, transient versus steady-state with the worked `y'' + 5y' + 6y` roots `-2, -3`,
  the delta and step relationship and sifting, and Duhamel convolution with linearity plus time
  invariance and causality limits.

### 1.4 Combined single-source-of-truth generator `scripts/gen_units9_10_quizzes.py` (RETAINED)
- Modeled on the retained `scripts/gen_units7_8_quizzes.py`, authoring two units at once.
  Content authored once as Python dicts with single-backslash KaTeX raw strings; `json.dumps`
  then emits both (i) the JS object-literal splices patched into `quiz-data.js` via the unique
  anchors, and (ii) the `app/data/quizzes.json` mirror via `json.dump(indent=2)`, so JS-source
  and JSON escaping stay identical (`\\frac` -> single backslash at runtime).
- Two-unit design: separate `MASTERY_9` and `MASTERY_10` lists (populated by `m9`/`m10`
  helpers); `update_js()` emits both `unit_mastery` blocks in a single end-of-file splice joined
  by `",\n\n"`, and `update_json()` assigns both unit keys. The 31 micro blocks emit in one
  micro splice.
- Built-in validation: exactly 31 micro videos, 5-items-per-video, two 30-item mastery lists,
  exactly one `correct` option and 4 options per item, a global duplicate-ID guard across all
  items, and a hard reject of any em-dash, en-dash, or ampersand in user-facing strings. All
  asserts passed on the run (`31 micro videos, 30 + 30 mastery items, copy rules OK`).
- Anti-truncation: the generator was assembled incrementally via the editor in module-sized
  chunks behind a `# <<<CONTENT_INSERTION_POINT>>>` sentinel (helper functions `C`/`W`/`item`
  keep the dictionaries compact), well within output limits.
- One copy fix applied during authoring: `um_9_24` had a missing closing `$` on one distractor
  (`$y = c_1 x^{-4} + c_2 x`), caught by a `$`-balance scan and corrected before splicing.

### 1.5 Verification (clean KaTeX and structural integrity)
- `node` eval of `quiz-data.js`: file parses cleanly; both new unit keys present with 30 items
  each; all 31 Unit 9/10 video keys resolve with 5 items each (155 total); exactly one correct
  + 4 options per item. micro keys 114 -> 145 (+31), unit_mastery 9 -> 11 (+2). Zero structural
  problems.
- JS<->JSON mirror: 0 mismatches across all keys; 145 micro keys and 11 mastery keys in both.
  `difflib.SequenceMatcher` confirmed the mirror change is PURE INSERTS (5226 inserts, 0
  deletes, 0 replaces); `quiz-data.js` diff is +2526 with 0 deletions.
- Runtime KaTeX confirmed by installing `katex@0.16.8` (the exact CDN version pinned in
  `app/index.html`) in a temp dir and rendering every extracted `$...$` and `$$...$$` segment
  from the new content with `throwOnError: true`, `$$`-before-`$` precedence: 1762 segments
  rendered, 0 failures. Temp install removed afterward.
- Copy-rule scan (0 em-dashes/en-dashes/ampersands), doubled-word scan (0 hits), and
  `$`-balance scan (0 unbalanced) all clean on the runtime strings.

---
---

# SESSION LOG, 2026-06-16 (Units 7 and 8 quiz suite generation)

Branch: `master`. Session start HEAD: `ce4c4e4` (prior session-log correction commit, which
followed the Units 5 and 6 quiz-suite docs commit).
Session end HEAD (pushed): this docs/feature commit
(`feat/docs: generate Units 7 and 8 quiz suites and append session state save`) advances HEAD
one further and is pushed at session close.
Scope of this session: generated the complete quiz suites for BOTH Unit 7 ("First-Order
Exactness and Methods") and Unit 8 ("Special Equations and Classical Models") in a single batch.
Twenty-seven Micro-Practice quizzes (one per video across all 14 modules, 5 items each = 135
items) plus two 30-question Unit Mastery quizzes (60 items), all appended to
`app/js/quiz-data.js` and synced to the `app/data/quizzes.json` mirror. Authored via a new
retained combined generator at `scripts/gen_units7_8_quizzes.py`, following the workflow in the
`quiz-authoring-workflow` project memory. One feature/docs commit, COMMITTED and PUSHED. Units 0
through 8 are now done; Units 9 through 18 remain (the curriculum runs Unit 0 through Unit 18, 19
units total).

---

## 1. Completed Tasks and File Modifications

### 1.1 Unit 7 Micro-Practice quizzes (14 videos, 70 items)
- Appended 14 `micro_practice` keys to `app/js/quiz-data.js` (5 items each, IDs
  `mp_<video_id>_<n>`), inserted via the unique splice anchor before `unit_mastery: {`.
  No existing data overwritten; the prior final micro array picked up a trailing comma only.
- video_ids by module (from `app/js/curriculum-data.js`, authoritative):
  - 7.1 Exact Differential Equations: `_c3iEPReTVQ`, `pgJci5CI9n4`
  - 7.2 Exact Equations Intuition: `iEpqcdaJNTQ`, `a7wYAtMjORQ`
  - 7.3 Exact Equations Motivation and Theory: `VlE2jWCtH8c`, `ljMaoSYfE1Q`
  - 7.4 Non-Exact Equations and Special Integrating Factors: `rPZG1pfeJK0`, `wQ0lwznTSvY`
  - 7.5 Almost Exact Differential Equations: `u5NGfwNNqHw`, `tOdlvZpdako`
  - 7.6 Exact Equations and Conservative Vector Fields: `3rrfhp-VCMM`, `1Db3jPmQTsI`
  - 7.7 Applications in Thermodynamics: `S8exyRjtatE`, `ATEKyjK9b4Y`
- Pedagogy: rigorous exactness tests and partial-derivative computation mandated per the
  directive. Items require the `M_y = N_x` exactness test on concrete forms (`(2xy)dx + (x^2)dy`
  exact via `2x = 2x`), partial-derivative drills (`M_y` of `2xy^3`, `N_x` of `x^2 cos y`),
  full potential reconstruction by integrating `M` in `x` then matching `F_y = N` (worked
  `(2x+y)dx + (x+2y)dy` giving `x^2 + xy + y^2 = C`), the `g(y)` integration-constant logic,
  integrating-factor diagnostics (`(M_y - N_x)/N` x-only and `(N_x - M_y)/M` y-only), the worked
  `mu(y) = y` repair of `(y)dx + (2x)dy` and `mu(x) = x` repair of the almost-exact
  `(3xy + y^2)dx + (x^2 + xy)dy`. Conceptual items cover the total differential, the
  conservative-field / exact-equation correspondence, gradient-perpendicular level curves, the
  simply connected hypothesis, closed-loop circulation, and thermodynamic state functions with
  `dU` exact, `delta Q` inexact, and the `1/T` integrating factor giving `dS`.

### 1.2 Unit 8 Micro-Practice quizzes (13 videos, 65 items)
- Appended 13 `micro_practice` keys (5 items each) in the same micro splice, immediately after
  the Unit 7 keys. Module 8.4 correctly handled as a single-video module (one micro key).
- video_ids by module:
  - 8.1 The Clairaut Equation and Singular Solutions: `A1hy1skVwzI`, `DNT2eXBTW34`
  - 8.2 Pursuit Curves and the Tractrix: `5kyE-vUoymU`, `apf5UDQU1NE`
  - 8.3 Fluid Dynamics and Viscosity: `hFcgcxw_TPo`, `VvDJyhYSJv8`
  - 8.4 Economic Models, Supply, Demand, and Price: `yvFr5D7UAMQ` (single video)
  - 8.5 The Lotka-Volterra Predator-Prey Model: `Tc05IbqTsFM`, `QXLmsKKr1Zs`
  - 8.6 Competition Models: `8GxwFrAyD9Q`, `obasfCufOr0`
  - 8.7 A Survey of Solution Methods: `4K8-PAX0VYQ`, `0kY3Wpvutfs`
- Pedagogy: classical curve problems and modeling applications mandated per the directive.
  Items require Clairaut general-solution substitution (`y = cx + c^2`) and singular-solution
  envelope elimination (worked `y = -x^2/4` from `x + 2p = 0`), the tractrix slope
  `dy/dx = -y / sqrt(a^2 - y^2)` and its Pythagorean string-triangle leg, supply-demand
  equilibrium algebra (`100 - 2p = 20 + 3p` giving `p = 16`), Lotka-Volterra equation reading
  (prey exponential growth without predators, predator decline without prey) and the coexistence
  equilibrium `x = c/d`, `y = a/b`. Conceptual items cover pursuit-curve aiming, the envelope
  tangency, Newtonian viscosity (shear stress proportional to velocity gradient, `mu` as dynamic
  viscosity), streamlines, continuity/conservation of mass, nonlinearity of realistic flow,
  predator-prey closed cycles, competitive exclusion and coexistence via intra- vs interspecific
  balance, nullcline equilibria, and method selection (separable, linear integrating factor,
  Bernoulli `v = y^{1-n}`, exact, characteristic equation, undetermined coefficients, Laplace,
  numerical fallback).

### 1.3 Unit 7 and Unit 8 Mastery quizzes (30 items each)
- Appended 2 `unit_mastery` keys, `"Unit 7: First-Order Exactness and Methods"` (IDs `um_7_1`
  .. `um_7_30`) and `"Unit 8: Special Equations and Classical Models"` (IDs `um_8_1` ..
  `um_8_30`), exact unit titles from `curriculum-data.js`, via the end-of-file splice anchor.
  Both blocks emitted in one splice joined by a comma; the prior final array (Unit 6) picked up
  a trailing comma only.
- Unit 7 mastery spans all seven modules: exactness test and partial-derivative computation,
  potential reconstruction and worked solutions, the `g(y)` logic, non-exact detection and both
  integrating-factor diagnostics, the worked almost-exact `mu(x) = x` repair, the conservative
  field correspondence, closed-loop and gradient-line-integral evaluation (`5` from `x^2 + y^2`),
  the simply connected hypothesis, and thermodynamic state functions (`dU` exact, `delta Q`
  inexact, `1/T` integrating factor, zero cyclic `oint dU`).
- Unit 8 mastery spans all seven modules: Clairaut general and singular solutions with worked
  envelope, pursuit aiming and tractrix slope/triangle, fluid continuity / streamlines /
  Newtonian viscosity / nonlinearity, supply-demand equilibrium algebra (`p = 16`),
  Lotka-Volterra prey/predator behavior and the `x = c/d`, `y = a/b` coexistence equilibrium and
  closed cycles, competition cross-term signs / exclusion / coexistence balance / nullclines,
  and method selection including the Bernoulli `v = y^{1-n}` substitution.

### 1.4 Combined single-source-of-truth generator `scripts/gen_units7_8_quizzes.py` (RETAINED)
- Modeled on the retained `scripts/gen_units5_6_quizzes.py`, authoring two units at once.
  Content authored once as Python dicts with single-backslash KaTeX raw strings; `json.dumps`
  then emits both (i) the JS object-literal splices patched into `quiz-data.js` via the unique
  anchors, and (ii) the `app/data/quizzes.json` mirror via `json.dump(indent=2)`, so JS-source
  and JSON escaping stay identical (`\\frac` -> single backslash at runtime).
- Two-unit design: separate `MASTERY_7` and `MASTERY_8` lists; `update_js()` emits both
  `unit_mastery` blocks in a single end-of-file splice joined by `",\n\n"`, and `update_json()`
  assigns both unit keys. The 27 micro blocks emit in one micro splice.
- Built-in validation: exactly 27 micro videos, 5-items-per-video, two 30-item mastery lists,
  exactly one `correct` option and 4 options per item, a global duplicate-ID guard across all
  items, and a hard reject of any em-dash or ampersand in user-facing strings. All asserts
  passed on the run (`27 micro videos, 30 + 30 mastery items, copy rules OK`).
- Anti-truncation: the generator was assembled incrementally via the editor in module-sized
  chunks (helper functions `C`/`W`/`item` keep the dictionaries compact), well within limits.
  Note: an initial heredoc append attempt failed on embedded quotes and was abandoned in favor
  of editor-based appends.

### 1.5 Verification (clean KaTeX and structural integrity)
- `node -e` eval of `quiz-data.js`: file parses cleanly; both new unit keys present with 30
  items each; all 27 Unit 7/8 video keys resolve with 5 items each (135 total); exactly one
  correct + 4 options per item; copy-rule (no em-dash, no ampersand) re-checked at runtime.
  micro keys 87 -> 114 (+27), unit_mastery 7 -> 9 (+2). Zero structural problems.
- `json.load`/parse of the mirror: valid JSON, 9 mastery units total, both new mastery quizzes
  present with 30 items, 114 micro keys; item shape (`id`, `prompt`, `hint`, `answerOptions`)
  confirmed with 4 options and exactly one correct on a sampled item.
- Runtime KaTeX confirmed by installing `katex@0.16.8` (the exact CDN version pinned in
  `app/index.html`) in a temp dir and rendering every extracted `$...$` and `$$...$$` segment
  from the new content with `throwOnError: true`, using auto-render `$$`-before-`$` delimiter
  precedence: 1488 segments rendered, 0 failures.

---

# SESSION LOG, 2026-06-16 (Units 5 and 6 quiz suite generation)

Branch: `master`. Session start HEAD: `9358313` (prior Unit 4 quiz-suite docs commit).
Session end HEAD (pushed): this docs/feature commit
(`feat/docs: generate Units 5 and 6 quiz suites and append session state save`) advances HEAD
one further and is pushed at session close.
Scope of this session: generated the complete quiz suites for BOTH Unit 5 ("Numerical Methods")
and Unit 6 ("Multivariable Calculus Foundations") in a single batch. Thirty-two Micro-Practice
quizzes (one per video across all 16 modules, 5 items each = 160 items) plus two 30-question
Unit Mastery quizzes (60 items), all appended to `app/js/quiz-data.js` and synced to the
`app/data/quizzes.json` mirror. Authored via a new retained combined generator at
`scripts/gen_units5_6_quizzes.py`, following the workflow in the `quiz-authoring-workflow`
project memory. One feature/docs commit, COMMITTED and PUSHED. Units 0 through 6 are now done;
Units 7 through 18 remain (the curriculum runs Unit 0 through Unit 18, 19 units total).

---

## 1. Completed Tasks and File Modifications

### 1.1 Unit 5 Micro-Practice quizzes (12 videos, 60 items)
- Appended 12 `micro_practice` keys to `app/js/quiz-data.js` (5 items each, IDs
  `mp_<video_id>_<n>`), inserted via the unique splice anchor before `unit_mastery: {`.
  No existing data overwritten; the prior final micro array picked up a trailing comma only.
- video_ids by module (from `app/js/curriculum-data.js`, authoritative):
  - 5.1 Euler's Method: `Q6A_m9YGG6A`, `6GfkRtf-A4M`
  - 5.2 Improved and Modified Euler Methods: `A5ObpYPADPQ`, `2-vJuL2hqN4`
  - 5.3 Heun's Method: `wTS8SDG0_sQ`, `aeRqofNb3_4`
  - 5.4 The RK4 Method: `ydFM5yON-24`, `1YZnic1Ug9g`
  - 5.5 The Runge-Kutta Family: `b-OSyxOpxKc`, `Rad-9gfIj5s`
  - 5.6 Adaptive Step Size and Method Comparison: `JcRsGD2pKlA`, `uzCD3K3VOV4`
- Pedagogy: computation mandated on the technique-heavy numerical material. Items require
  single-step Euler updates (`y'=y`, `y'=t+y` giving `1.1`, `1.5`), improved-Euler and midpoint
  steps (`1.105`, `1.22`), Heun predictor/corrector evaluations (`1.24`, the exact `0.25` for
  `y'=2t`), the RK4 weighted average `(k1 + 2k2 + 2k3 + k4)/6` (worked `y'=2t` giving `1`), the
  halving-error factors (Euler halves, RK4 by `16`), and the RKF45 order-(4,5) pairing.
  Conceptual items cover tangent-line geometry, local `O(h^2)` vs global `O(h)` order, the
  Simpson/trapezoidal connections, the Butcher tableau, consistency (weights sum to one),
  explicit vs implicit stages, adaptive error-driven step control, and orbital energy drift.

### 1.2 Unit 6 Micro-Practice quizzes (20 videos, 100 items)
- Appended 20 `micro_practice` keys (5 items each) in the same micro splice, immediately after
  the Unit 5 keys.
- video_ids by module:
  - 6.1 Partial Derivatives: `Rvnv3bPDCs8`, `JJG2t5IBKy4`
  - 6.2 Clairaut's Theorem on Mixed Partials: `DmB8I0TFlc8`, `wLkXUmVKyi4`
  - 6.3 The Multivariable Chain Rule: `9yCtWfI_Vjg`, `hFvBZf-Jx28`
  - 6.4 Line Integrals and the Fundamental Theorem: `2MN0aq5gNOg`, `we88mTXj6Yc`
  - 6.5 Conservative Vector Fields: `76nzOtupeRc`, `ZGUvyGeNT44`
  - 6.6 Potential Functions: `jlza4rEFXKM`, `nQkHh2psLck`
  - 6.7 Level Curves: `uaHiAxFESc4`, `CCLrfpD5_sE`
  - 6.8 Indifference Curves: `iOmDo5jLFw8`, `4RLEf70CDnw`
  - 6.9 Contour Maps: `Id-EziWyZMQ`, `hdtKiH51J9Y`
  - 6.10 Exact and Inexact Differentials: `vTVCQos6Mj4`, `IYlzo-bxrqs`
- Pedagogy: computation on partial derivatives (`f_x`, `f_y`, second and mixed partials of
  `x^3 y^2`, `x^2 y^3`), chain-rule rates (`2t + 4t^3`, the `cos^2 t - sin^2 t` dot-product
  form), gradient-line-integral evaluation via the Fundamental Theorem (`5` from
  `x^2 + y^2`), the `P_y = Q_x` conservative/exactness test applied to concrete fields,
  potential reconstruction by integrating `P` then matching `Q`, level-curve circle radii, and
  the `-p1/p2` budget-line slope. Conceptual items cover Clairaut symmetry and its continuity
  hypothesis, path independence and zero closed-loop circulation, simply connected domains,
  level-curve/gradient perpendicularity, contour steepness and saddle patterns, trace vs level
  curve, indifference-curve utility and tangency optimum, and exact differentials as
  thermodynamic state functions.

### 1.3 Unit 5 and Unit 6 Mastery quizzes (30 items each)
- Appended 2 `unit_mastery` keys, `"Unit 5: Numerical Methods"` (IDs `um_5_1` .. `um_5_30`) and
  `"Unit 6: Multivariable Calculus Foundations"` (IDs `um_6_1` .. `um_6_30`), exact unit titles
  from `curriculum-data.js`, via the end-of-file splice anchor. Both blocks emitted in one
  splice joined by a comma; the prior final array (Unit 4) picked up a trailing comma only.
- Unit 5 mastery spans all six modules (~5 items each): Euler formula/step/order/halving,
  improved and midpoint methods and why averaging helps, Heun predictor-corrector and its
  worked step, the full RK4 average / `k1` location / order / `16x` halving / four evaluations /
  worked `y'=2t` step, the Runge-Kutta family (one-stage Euler, Butcher tableau, order-vs-cost,
  consistency), and adaptive stepping (error-driven `h`, shrink-and-retry, RKF45, orbital
  energy conservation as a quality check).
- Unit 6 mastery spans all ten modules (~3 items each): partials and the four second partials,
  Clairaut equality and hypothesis, chain-rule formula/computation/dot-product, the Fundamental
  Theorem of Line Integrals and zero loop integral, conservative definition and `P_y = Q_x`
  test, potential reconstruction and uniqueness-up-to-constant, level-curve definition and
  gradient perpendicularity, indifference-curve utility / MRS = price ratio / budget line,
  contour maps and traces, and the exactness test with the state-function interpretation.

### 1.4 Combined single-source-of-truth generator `scripts/gen_units5_6_quizzes.py` (RETAINED)
- Modeled on the retained `scripts/gen_unit4_quizzes.py`, extended to author two units at once.
  Content authored once as Python dicts with single-backslash KaTeX raw strings; `json.dumps`
  then emits both (i) the JS object-literal splices patched into `quiz-data.js` via the unique
  anchors, and (ii) the `app/data/quizzes.json` mirror via `json.dump(indent=2)`, so JS-source
  and JSON escaping stay identical (`\\frac` -> single backslash at runtime).
- Two-unit design: separate `MASTERY_5` and `MASTERY_6` lists; `update_js()` emits both
  `unit_mastery` blocks in a single end-of-file splice joined by `",\n\n"`, and `update_json()`
  assigns both unit keys. The 32 micro blocks emit in one micro splice.
- Built-in validation: exactly 32 micro videos, 5-items-per-video, two 30-item mastery lists,
  exactly one `correct` option and 4 options per item, a global duplicate-ID guard across all
  items, and a hard reject of any em-dash or ampersand in user-facing strings. All asserts
  passed on the run (`32 micro videos, 30 + 30 mastery items, copy rules OK`).
- Anti-truncation: the generator was assembled efficiently from part files (helper functions
  `C`/`W`/`item` keep the dictionaries compact), well within output limits.

### 1.5 Verification (clean KaTeX and structural integrity)
- `node`/`vm` eval of `quiz-data.js`: file parses cleanly; both new unit keys present with 30
  items each; all 32 Unit 5/6 video keys resolve with 5 items each (160 total); exactly one
  correct + 4 options per item. micro keys 55 -> 87 (+32), unit_mastery 5 -> 7 (+2). Zero
  structural problems.
- `json.load`/parse of the mirror: valid JSON, 7 mastery units total, both new mastery quizzes
  present with 30 items, 87 micro keys; JS and JSON byte-identical for a sampled video.
- Runtime KaTeX confirmed by installing `katex@0.16.8` (the exact CDN version pinned in
  `app/index.html`) in a temp dir and rendering every extracted `$...$` and `$$...$$` segment
  from the new content with `throwOnError: true`: 1323 segments rendered, 0 failures. Temp
  install removed afterward.

---

# SESSION LOG, 2026-06-16 (Unit 4 quiz suite generation)

Branch: `master`. Session start HEAD: `e0d0bdb` (prior Unit 3 quiz-suite docs commit).
Session end HEAD (pushed): this docs/feature commit
(`feat/docs: generate Unit 4 quiz suite and append session state save`) advances HEAD one
further and is pushed at session close.
Scope of this session: generated the complete Unit 4 quiz suite, the "Autonomous Equations,
Equilibrium Solutions, and Stability" unit. Twelve Micro-Practice quizzes (one per video,
5 items each = 60 items) plus one 30-question Unit 4 Mastery quiz, both appended to
`app/js/quiz-data.js` and synced to the `app/data/quizzes.json` mirror. Authored via a new
retained single-source-of-truth generator at `scripts/gen_unit4_quizzes.py`, following the
workflow recorded in the `quiz-authoring-workflow` project memory. One feature/docs commit,
COMMITTED and PUSHED. Units 0 through 4 are now done; Units 5 through 12 remain.

---

## 1. Completed Tasks and File Modifications

### 1.1 Unit 4 Micro-Practice quizzes (12 videos, 60 items)
- Appended 12 `micro_practice` keys to `app/js/quiz-data.js` (5 items each, IDs
  `mp_<video_id>_<n>`), inserted after the last Unit 3 key via the unique splice anchor.
  No existing data overwritten; the previous final array picked up a trailing comma at the
  splice boundary only.
- video_ids by module (from `app/js/curriculum-data.js`, authoritative):
  - 4.1 Equilibrium Solutions and Stability: `7q33RFkMMpY`, `NmntYoB1uJg`
  - 4.2 Autonomous Equations and the Phase Line: `pZOmToxx-R8`, `swt-let4pCI`
  - 4.3 The Logistic Equation: `aP4YXOo-Uko`, `TCkLSYxx21c`
  - 4.4 Bernoulli Differential Equations: `NjIMGAIPbzg`, `iCN8nGXE29o`
  - 4.5 Solving Bernoulli Equations, Worked Examples: `DdJzN4MS_0k`, `QnTt9huzdNU`
  - 4.6 Applications of Bernoulli Equations: `tsBxekziI6Q`, `DW4rItB20h4`
- Pedagogy: computation mandated on the technique-heavy material per the directive's
  pedagogical engine. Items require finding equilibria of products like `y(y-3)` and
  `(y-1)(y+2)`, phase-line sign analysis and arrow direction, stability classification via
  the sign of `f'(y*)` (e.g. `y - y^3` at `y = 1`), logistic equilibria/carrying-capacity and
  a numeric rate (`r=0.5, K=100, P=25` giving `9.375`), the Bernoulli substitution
  `v = y^{1-n}` and its derivative, dividing by `y^n`, and computing an integrating factor.
  Conceptual items cover the autonomous definition, sink/source/semi-stable phase-line types,
  time-shift invariance, and the fluid Bernoulli energy-conservation application in 4.6.

### 1.2 Unit 4 Mastery quiz (30 items)
- Appended 1 `unit_mastery` key
  `"Unit 4: Autonomous Equations, Equilibrium Solutions, and Stability"` (exact unit title
  from `curriculum-data.js`), with 30 items `um_4_1` .. `um_4_30`, at the end of the
  `unit_mastery` object via the end-of-file splice anchor. The previous final array (Unit 3)
  picked up a trailing comma only.
- Comprehensive mix spanning all six modules: equilibrium definition and classification,
  semi-stable double-root behavior, the autonomous/phase-line characterization, sink vs source
  arrows, logistic carrying capacity / equilibria / peak growth at `K/2` / small-population
  exponential approximation, the Bernoulli form and `v = y^{1-n}` substitution mechanics
  (including the `n = 0, 1` already-linear cases), reciprocal back-substitution, and the
  fluid Bernoulli energy statement with the dynamic-pressure-quadruples computation.

### 1.3 Single-source-of-truth generator `scripts/gen_unit4_quizzes.py` (RETAINED)
- Modeled directly on the retained `scripts/gen_unit3_quizzes.py`. Content authored once as
  Python dicts with single-backslash KaTeX raw strings; `json.dumps` then emits both (i) the
  JS object-literal splice patched into `quiz-data.js` via the unique anchors, and (ii) the
  `app/data/quizzes.json` mirror via `json.dump(indent=2)`, so JS-source and JSON escaping stay
  identical (`\\frac` -> single backslash at runtime).
- Built-in validation: exactly 12 micro videos, 5-items-per-video and 30-mastery counts,
  exactly one `correct` option and 4 options per item, and a hard reject of any em-dash or
  ampersand in user-facing strings. All asserts passed on the run.
- Copy-rule consequence handled: no `\begin{pmatrix}` matrices (which use `&` as a column
  separator); the fluid-dynamics module 4.6 uses the word "and" throughout rather than an
  ampersand, including in its video-title comments.

### 1.4 Verification
- `node` eval of `quiz-data.js` (via a temporary `scripts/_verify_unit4.js`, since removed):
  file parses cleanly, the new unit key is present with 30 items, all 12 Unit 4 video keys
  resolve with 5 items each (60 total), exactly one correct + 4 options per item, and zero
  em-dashes/ampersands. Runtime KaTeX confirmed to resolve to a SINGLE backslash (`\frac`
  present, `\\frac` absent in the evaluated object).
- `json.load` of the mirror: valid JSON, 5 mastery units total, Unit 4 mastery present with
  30 items, all 12 Unit 4 micro videos present (55 micro keys total). JS and JSON stay in sync.
- Splice boundaries inspected: trailing commas added correctly after the prior final arrays,
  indentation matches existing hand formatting, file terminates properly.

### 1.5 Copy fix, doubled word in `um_4_1`
- A grep for doubled words caught "which which" in the `um_4_1` prompt
  ("...is a constant `$y = c$` for which which condition holds?"). Fixed to
  "...for which condition?" in three places to keep the generator and outputs consistent:
  the source `scripts/gen_unit4_quizzes.py`, `app/js/quiz-data.js`, and
  `app/data/quizzes.json`. The output files were edited directly rather than by re-running the
  generator, because the splice is append-only and a second run against the already-spliced
  anchors would have duplicated the Unit 4 block. Re-verification after the fix passed clean.

---

# SESSION LOG, 2026-06-15 (Unit 3 quiz suite generation)

Branch: `master`. Session start HEAD: `8a573a5`. Session end HEAD (pushed): this docs/feature
commit (`feat/docs: generate Unit 3 quiz suite and append session state save`) advances HEAD
one further and is pushed at session close.
Scope of this session: generated the complete Unit 3 quiz suite, the "Existence, Uniqueness,
and Geometry" unit. Twelve Micro-Practice quizzes (one per video, 5 items each = 60 items)
plus one 30-question Unit 3 Mastery quiz, both appended to `app/js/quiz-data.js` and synced to
the `app/data/quizzes.json` mirror. Introduced a retained single-source-of-truth generator at
`scripts/gen_unit3_quizzes.py`, and saved a new project memory documenting the quiz-authoring
workflow for the remaining Units 4 through 12. One feature/docs commit, COMMITTED and PUSHED.

---

## 1. Completed Tasks and File Modifications

### 1.1 Unit 3 Micro-Practice quizzes (12 videos, 60 items)
- Appended 12 `micro_practice` keys to `app/js/quiz-data.js` (5 items each, IDs
  `mp_<video_id>_<n>`), inserted after the last Unit 2 key. No existing data overwritten;
  the previous final array picked up a trailing comma at the splice boundary only.
- video_ids by module (from `app/js/curriculum-data.js`, authoritative):
  - 3.1 Slope Fields and Vector Fields: `ccDMpj2UK_M`, `m9Y8U9f9_Bw`
  - 3.2 The Phase Plane and Phase Portraits: `lsbUlubAiGw`, `zmzyW1rP-hk`
  - 3.3 Existence and Uniqueness of Solutions: `_WpncZ3RkTg`, `BVKyaEu1FWk`
  - 3.4 Fixed Points and Iteration: `csInNn6pfT4`, `bEZ6JLLjM3Y`
  - 3.5 The Banach Fixed Point Theorem: `qHnXE_h5c2M`, `ewGo7rtTHE0`
  - 3.6 Picard Iteration: `9gos-d1v-2s`, `fsbVJxOhRcU`
- Pedagogy: computational items dominate the technique-heavy videos (reading slope values
  off `f(x,y)`, solving isocline equations, finding equilibria and nullclines, eigenvalue
  classification of the origin, fixed-point algebra and stability via `|g'(x*)|`, contraction
  bounds, and computing Picard iterates such as the partial sums of `e^x`), with conceptual
  items covering definitions and theorem hypotheses mixed in.

### 1.2 Unit 3 Mastery quiz (30 items)
- Appended 1 `unit_mastery` key `"Unit 3: Existence, Uniqueness, and Geometry"` (exact unit
  title from `curriculum-data.js`), with 30 items `um_3_1` .. `um_3_30`, at the end of the
  `unit_mastery` object. The previous final array (Unit 2) picked up a trailing comma only.
- Comprehensive mix spanning all six modules: slope fields and isoclines, integral curves,
  phase-plane state and trajectories, nullclines, eigenvalue classification (node, saddle,
  center), existence vs uniqueness hypotheses, non-uniqueness of `y' = y^{1/3}`, the linear
  global-interval guarantee, fixed points and cobwebs, contraction mappings, the Banach
  guarantee, the Picard integral equation, and Picard-Lindelof via Lipschitz.

### 1.3 Single-source-of-truth generator `scripts/gen_unit3_quizzes.py` (RETAINED)
- Unlike the Unit 2 throwaway script, this generator is kept in the repo. Content is authored
  once as Python dicts with single-backslash KaTeX raw strings; `json.dumps` then emits both
  (i) the JS object-literal splice patched into `quiz-data.js` via unique anchors, and (ii)
  the `app/data/quizzes.json` mirror via `json.dump(indent=2)`, so JS-source and JSON escaping
  stay identical (`\\frac` -> single backslash at runtime).
- Built-in validation: 5-items-per-video and 30-mastery counts, exactly one `correct` option
  and 4 options per item, and a hard reject of any em-dash or ampersand in user-facing strings.
- Copy-rule consequence handled: KaTeX matrix environments use `&` as a column separator, so
  the eigenvalue questions express matrices as decoupled systems (e.g. `x' = 2x`, `y' = 3y`)
  rather than `\begin{pmatrix}`.

### 1.4 Verification
- `node -e` eval of `quiz-data.js`: parses cleanly, 43 total `micro_practice` keys (31 prior
  + 12 new), new unit key present, exactly one correct + 4 options per item across Unit 3.
- `json.load` of the mirror: 43 micro keys, all 12 Unit 3 videos with 5 items, Unit 3 mastery
  present with 30 items; sample prompts confirm runtime single-backslash KaTeX.
- Splice boundaries inspected: trailing commas added correctly after the prior final arrays,
  indentation matches existing hand formatting, file terminates properly.

### 1.5 New project memory (Units 4 through 12)
- Saved `quiz-authoring-workflow.md` (type: project) under the project memory directory, with
  a pointer line added to `MEMORY.md`. It records the two-file structure (`quiz-data.js` plus
  the `quizzes.json` mirror), the item schema, the Math Confidence Reset pedagogy, the copy
  rules (no em-dashes/ampersands, the matrix `&` caveat), the double-escaped KaTeX convention,
  and the generator-based workflow, so Units 4 through 12 can repeat this reliably. Memory
  files live outside the repo and are not part of this commit.

---

# SESSION LOG, 2026-06-15 (Unit 2 quizzes + Desmos checkpoint native-slider refactor)

Branch: `master`. Session start HEAD: `dff4879`. Session end HEAD (pushed): `684102d`
(this docs commit advances HEAD one further and is pushed at session close).
Scope of this session: (A) generated all Unit 2 quiz content (micro-practice for 10 videos
plus the 30-question unit-mastery quiz) and synced the JSON mirror, then (B) fixed and then
refactored the two Unit 2 Desmos curve-fitting checkpoints to use custom native HTML range
sliders instead of the built-in Desmos expressions sidebar. Three feature/fix commits plus
this docs commit, all COMMITTED and PUSHED.

---

## 1. Completed Tasks and File Modifications

### 1.1 Unit 2 quiz generation (committed `1a63644`, pushed)
- Appended 10 `micro_practice` keys to `app/js/quiz-data.js` (5 items each = 50 items,
  IDs `mp_<video_id>_<n>`) and 1 `unit_mastery` key
  `"Unit 2: First Order Linear Differential Equations"` (30 items, `um_2_1` .. `um_2_30`).
  Inserted after the last Unit 1 keys; no existing data overwritten.
- video_ids by module (from `app/js/curriculum-data.js`, authoritative):
  - 2.1 Separable Equations: `7Y-frhf-1Zk`, `wU0gYqxZT1g`
  - 2.2 First-Order Linear and Integrating Factors: `kATxKuVSc9I`, `gd1FYn86P0c`
  - 2.3 Initial Value Problems: `_lp33noEO50`, `2urpuhYVc14`
  - 2.4 Homogeneous Equations and Substitution: `TjmVeZpajz8`, `3y37ug3NON8`
  - 2.5 Applications, Cooling, Growth, Decay: `_bAjWNsNrQA`, `iRfo9hDV5Ss`
- Pedagogy: solving-technique unit, so most items per video are computational (separation
  of variables, integrating factor `e^{\\int P dx}`, solving IVPs, the `y=vx` substitution,
  cooling/growth/decay models), with conceptual items mixed in. Mastery is a comprehensive
  mix. Copy rule enforced (no em-dashes, no ampersands); KaTeX double-escaped.
- Method (single source of truth): a throwaway Python generator
  (`scripts/_gen_unit2_quizzes.py`, created then deleted) defined the content once with
  single-backslash KaTeX, then (i) emitted JS-formatted text matching the existing hand
  formatting and patched `quiz-data.js` via unique anchors, and (ii) updated the
  `app/data/quizzes.json` mirror via `json.dump(indent=2)`. Built-in asserts: 5/30 counts,
  exactly one `correct`, 4 options each, unique ids, zero em-dash/ampersand.
- Verified: `node` eval of `quiz-data.js` (valid, structure correct); JS<->JSON deep match
  (0 mismatches); every Unit 0/1 entry preserved byte-for-byte. Files written CRLF to match
  repo. NOTE: `git diff --stat` reported ~670 phantom deletions on the mirror; this is a
  cosmetic Myers-diff realignment of repetitive JSON lines. `difflib.SequenceMatcher`
  confirmed the change is PURE INSERTS only (two blocks added, nothing removed).

### 1.2 Desmos sidebar visibility fix (committed `072a5dd`, pushed) -- SUPERSEDED by 1.3
- Located Desmos init in the shared helper `CheckpointCore.desmosGraph`
  (`app/js/checkpoints/checkpoint-core.js:360`), which defaults `expressions: false` and
  merges per-call options via `Object.assign`.
- The two Unit 2 curve checkpoints (`desmos_ivp_curve_selector`,
  `desmos_cooling_curve_fitter`) relied on Desmos-NATIVE sliders (`sliderBounds`) yet called
  `desmosGraph(body, {})`, inheriting `expressions: false`, so the very sliders the prompts
  told users to drag were hidden. Interim fix: pass `{ expressions: true }` for those two and
  raise `.desmos-frame` height 340 -> 420. This whole approach was then replaced in 1.3.
- Confirmed `.desmos-frame` width was already `100%` (responsive, not clipping the panel);
  no fixed/narrow container width in any rule or media query.

### 1.3 Native HTML slider refactor (committed `684102d`, pushed) -- CURRENT STATE
- Reverted both checkpoints to `expressions: false` (native sidebar stays hidden). Parameters
  are now plain Desmos variable definitions with NO `sliderBounds`: `varC` for IVP, and
  `varA` / `varT0` / `varK` for cooling. The model curves reference these vars.
- Inject a `<div class="slider-panel">` just below `.desmos-frame`, populated via the existing
  `CheckpointCore.rangeControl(parent, {label,min,max,step,value,onChange})` helper (builds a
  `<label>` + `<input type="range">` + live readout, returns a `{ value }` holder).
  - IVP: one `C` slider, min -5, max 5, step 0.1, default 1.
  - Cooling: `A` 0..40 step 0.5 default 10; `T0` 20..100 step 0.5 default 60; `k` -0.6..0
    step 0.01 default -0.05. Correct-answer targets all reachable within tolerance
    (A~20, T0~90, k~-0.3, C=3).
- Each slider `onChange` calls `calc.setExpression({ id, latex })` (e.g. `latex: "k=" + v`)
  so the curve redraws in real time. The grading logic now reads the `rangeControl` `.value`
  holders directly; `CheckpointCore.observeValue` is no longer used here.
- CSS (`app/css/main.css`): added `.slider-panel` (theme-aware via `--panel-bg` /
  `--panel-border`); refined shared slider styles: `.slider-label` min-width 2.25rem and
  `flex-shrink:0` for column alignment, `.slider-row input[type=range]` `flex:1 1 auto` +
  `min-width:0` so the track shrinks responsively inside the card, `.slider-readout`
  `flex-shrink:0`. `.desmos-frame` restored to height 360 with the sidebar comment corrected.
- Verified live in the browser MCP (served `app/` on `http://127.0.0.1:8765`): rendered both
  checkpoints via `CheckpointRegistry.render(id, host, moduleData)`, clicked Begin; confirmed
  the panel sits below the frame with correct slider counts/labels/bounds/defaults. Captured
  the Desmos instance (monkey-patching `Desmos.GraphingCalculator`) and dispatched `input`
  events: `getExpressions()` showed `A=20`, `T_0=90`, `k=-0.3` updated live; readouts tracked;
  both checkpoints reached the success state at the correct values; screenshot confirmed clean
  alignment. Only console error was a benign `favicon.ico` 404. Test artifacts removed.

### 1.4 Source control (this session, DONE)
- `1a63644` "feat: generate Unit 2 micro-practice and unit-mastery quizzes, sync json mirror"
  (`dff4879..1a63644`, 2 files: `app/js/quiz-data.js`, `app/data/quizzes.json`).
- `072a5dd` "fix: reveal Desmos slider sidebar for Unit 2 curve-fitting checkpoints"
  (`1a63644..072a5dd`, 2 files: `app/js/checkpoints/widgets-unit2.js`, `app/css/main.css`).
- `684102d` "refactor: drive Unit 2 Desmos checkpoints with native HTML range sliders"
  (`072a5dd..684102d`, 2 files, +94/-18).
- Each commit staged ONLY its intended files. The unrelated working-tree PDF changes (see Git
  State below) were deliberately left unstaged throughout.

---

## 2. Architecture State (delta this session)

- NEW CONVENTION for Desmos checkpoints that need parameter input: keep `expressions: false`,
  store parameters as plain Desmos variable definitions (no `sliderBounds`), inject a
  `.slider-panel` of `CheckpointCore.rangeControl` controls just below `.desmos-frame`, and on
  each slider change call `calc.setExpression({ id, latex })` to update the graph live. Grade
  by reading the `rangeControl` `.value` holders, not `observeValue`. Prefer this over the
  native Desmos expressions sidebar for future Desmos checkpoints.
- NEW reusable CSS component `.slider-panel` in `app/css/main.css`, plus refined shared
  `.slider-row` / `.slider-label` / `.slider-readout` rules (used by both Desmos and
  canvas widgets). `.desmos-frame` default height is now 360 and its native sidebar stays
  hidden by design.
- CheckpointCore / Registry API confirmed this session:
  `rangeControl(parent, {label,min,max,step,value,onChange}) -> { value }` (live readout);
  `desmosGraph(parent, options)` merges `options` over `{ expressions:false, settingsMenu:false,
  zoomButtons:true, border:false, keypad:false }`; `CheckpointRegistry.render(checkpointId,
  container, moduleData)` (note arg order: id first).
- Content inventory: Units 0, 1, and 2 now complete. Source totals: `micro_practice` 31 keys
  (40 + 65 + 50 = 155 items), `unit_mastery` 3 keys (90 items). `app/data/quizzes.json`
  deep-equals source for all of these.
- Mirror-sync approach this session (one Python generator emitting BOTH the JS patch and the
  JSON mirror) supersedes the prior throwaway `_gen_mirror.js` node approach. Either keeps the
  mirror deep-equal; the mirror remains a documentation artifact, NOT loaded at runtime.
- No engine/router/state/schema changes. The quiz subsystem and `QUIZ_DATA` schema documented
  in earlier logs remain current and accurate.

---

## 3. Immediate Next Actions

### 3.1 Content backlog (primary)
- micro_practice: ~219 videos remain (250 total minus 31 done across Units 0, 1, 2).
- unit_mastery: 16 units remain (Units 3 .. 18); Units 0, 1, 2 done.
- Recommended next batch: `"Unit 3: Existence, Uniqueness, and Geometry"`. Pull its
  video_ids and exact unit title from `app/js/curriculum-data.js`. Mostly geometric/theoretical
  (slope fields, phase plane, existence and uniqueness, fixed points, Picard iteration), so
  balance conceptual items with the few computational ones the topics admit.

### 3.2 Per-batch checklist (apply every time)
1. Append to `app/js/quiz-data.js` only; do not overwrite existing keys. Unique IDs
   `mp_<video_id>_<n>` and `um_<unit>_<n>`. Exactly one `correct`; rationale on every option;
   `hint` per item.
2. Enforce copy rule (no em-dashes, no ampersands); double-backslash KaTeX.
3. Run the static integrity + copy-rule scan; for content changes, `node`-eval `quiz-data.js`
   and assert the JS<->JSON mirror deep-equals.
4. Regenerate `app/data/quizzes.json` from the same source; confirm deep-equal. Write files
   CRLF to match repo. Ignore phantom `--stat` deletions on the mirror (Myers realignment);
   verify with `difflib` that real changes are pure inserts.
5. Commit staging ONLY the intended files. Push `origin master` when asked.

### 3.3 Git State at session close
- HEAD `684102d` on `master`, in sync with `origin/master` (this docs commit advances and is
  pushed on top).
- Pre-existing, UNRELATED, still-unstaged working-tree changes (do NOT bundle with app work):
  - Deleted: `Differential Equations Whiteboard Notes/Diff. EQ 5 - 1st Order Linear ODE's - Gabriel.pdf`
  - Deleted: `ODE_Phase_Plane_Study_Guide.pdf`
  - Untracked PDFs (7): `1st & 2nd Order ODE Cheat Sheet & Review Guide.pdf`,
    `Differential Equations Whiteboard Notes/Diff. EQ 5 - 2nd Order Linear ODE's - Gabriel.pdf`,
    `Eigenvalues & Eigenfunctions Cheat Sheet & Practice.pdf`,
    `Forced Vibrations Cheat Sheet & Practice.pdf`,
    `Nonhomogeneous Equations & Undetermined Coefficients Cheat Sheet & Practice.pdf`,
    `ODE Phase Plane Study Guide.pdf`, `ODE Systems Terminology Cheat Sheet & Review Guide.pdf`,
    `Solving ODE Systems Cheat Sheet & Practice.pdf`. (Ampersands here are in PDF filenames,
    unrelated to the app copy rule.)
- Git warns LF -> CRLF on edited files (Windows autocrlf); harmless.

---
---

# SESSION LOG, 2026-06-13 (Unit 1 quiz content)

Branch: `master`. Session start HEAD: `685ef44`. Session end HEAD (pushed): `5954750`.
Scope of this session: generated all Unit 1 quiz content (micro-practice for 13 videos
plus the 30-question unit-mastery quiz), reconciled a copy-rule deviation, and upgraded
`app/data/quizzes.json` from a stub to a full generated mirror. Two commits made and pushed.
Unlike the prior session, ALL work here is COMMITTED and PUSHED.

---

## 1. Completed Tasks and File Modifications

### 1.1 Unit 1 micro_practice generation (committed `d18bdc3`, pushed)
- Appended 13 new `micro_practice` keys to `app/js/quiz-data.js`, one per Unit 1 video,
  5 items each = 65 items. IDs `mp_<video_id>_<n>`. Inserted after the last Unit 0 key
  (`zm_UqjVLViU`), before the `micro_practice` object close. No existing data overwritten.
- video_ids by module (from `app/js/curriculum-data.js`, authoritative):
  - 1.1 Calculus Review: `f0BxAtprWts`, `-NhgElcA3K8`
  - 1.2 The Number e: `m2MIpDrF7Es`, `ozbqRDOTxuk`
  - 1.3 Euler's Formula and Identity (3 videos): `v0YEaeIClKY`, `TLgZit1HTxA`, `sn3orkHWqUQ`
  - 1.4 Order, Degree, Linearity: `hiL356ExeIw`, `C3WKwB5gBqI`
  - 1.5 What It Means to Be a Solution: `6n-TgcDTAPw`, `sOKctbWGnSk`
  - 1.6 Forming DEs by Eliminating Constants: `qBNYAqQ_Glo`, `4tcvZU4ACho`
- Pedagogy: technique-teaching modules (1.1, 1.4, 1.5, 1.6) carry >=3 computational items
  each (derivative/integral evaluation, order/degree/linearity classification, solution
  verification by substitution, forming ODEs by eliminating constants). 1.2 and 1.3 mix
  concept with concrete calculation (limits, Euler-formula evaluation, polar multiplication).

### 1.2 Copy-rule reconciliation (em-dash strip), same commit `d18bdc3`
- A one-off content directive requested em-dashes; the initial Unit 1 draft contained 118.
  This conflicted with the standing COPY RULE (no em-dashes, no ampersands). Per user
  decision, all em-dashes were stripped from Unit 1 to match the Unit 0 house style.
- Conversion rules applied programmatically over `quiz-data.js`:
  1. Leading affirmations `"Correct — X"` / `"Yes — X"` -> `"Correct. X"` / `"Yes. X"`
     with the next letter capitalized (matches Unit 0 `"Yes."`/`"Correct."`).
  2. Em-dash before a guiding-question/imperative starter (what/which/is/does/do/...) ->
     `". "` + capitalized word (19 such cases hand-vetted to avoid touching legitimate
     relative clauses such as `", which shrinks as the gap closes"`).
  3. All other mid-sentence em-dashes -> comma.
- Result verified: 0 em-dashes, 0 ampersands across the file.

### 1.3 JSON mirror upgraded to full generated mirror (commits `d18bdc3` and `5954750`)
- `app/data/quizzes.json` was previously a single-item `demo_`-prefixed stub
  (1 micro_practice key, 0 unit_mastery). It is now a complete generated mirror of
  `QUIZ_DATA`, preserving the existing `_doc` header string.
- Regeneration is done by a throwaway node script (`_gen_mirror.js`, created then deleted)
  that loads `QUIZ_DATA` from the JS via
  `eval('(function(){' + src + '\\nreturn QUIZ_DATA;})()')`, then writes
  `{ _doc, micro_practice, unit_mastery }` with `JSON.stringify(obj, null, 2) + "\\n"`.
- Post-sync state: micro_practice 21 keys / 105 items; unit_mastery 2 keys / 60 items.
  Mirror deep-equals source (`mp+um` JSON identical). The `demo_` stub is gone; mirror IDs
  now equal source IDs (`mp_*`, `um_*`). NOTE: this supersedes the prior session's claim
  that the mirror is "stale/minimal" and uses a `demo_` prefix.

### 1.4 Unit 1 unit_mastery generation (committed `5954750`, pushed)
- Added `unit_mastery` key `"Unit 1: Foundations and Prerequisites"` (exact unit title),
  30 items, IDs `um_1_1` .. `um_1_30`. Inserted after the Unit 0 mastery array. No overwrite.
- Coverage map: 1.1 calculus (um_1_1..5: power/chain/product rule, integration with C,
  meaning of derivative); 1.2 the number e (um_1_6..10: self-derivative, base with ln=1,
  limit definition, continuous compounding -> e^{rt}, dy/dt=y); 1.3 Euler/complex
  (um_1_11..16: Euler's formula, e^{i\\pi}, e^{i\\pi/2}, modulus, polar multiplication,
  multiply-by-i rotation); 1.4 definitions (um_1_17..22: order, degree, ODE vs PDE,
  identify nonlinear, initial-condition count, IVP); 1.5 solutions (um_1_23..26: holds
  everywhere, verify by substitution, general vs particular, implicit vs explicit);
  1.6 eliminating constants (um_1_27..30: differentiation count, eliminate C from y=Cx^3,
  eliminate from A cos x + B sin x, order = number of independent constants).

### 1.5 Source control (this session, DONE)
- `d18bdc3` "feat: generate Unit 1 micro-practice quizzes and sync json mirror"
  (`685ef44..d18bdc3`, 2 files, +4058/-6).
- `5954750` "feat: generate Unit 1 unit-mastery quiz and sync json mirror"
  (`d18bdc3..5954750`, 2 files, +1055).
- Each commit staged ONLY `app/js/quiz-data.js` and `app/data/quizzes.json`. Unrelated
  working-tree PDF changes (see Git State below) were deliberately left unstaged.

---

## 2. Architecture State (delta this session)

- No code/engine/schema changes. `quiz-engine.js`, `state.js`, `router.js`, CSS unchanged.
  The 2.x architecture documented in the prior session log remains current and accurate.
- `app/data/quizzes.json` is now a FULL deep-equal mirror of `QUIZ_DATA` (not a stub).
  Treat it as a generated artifact: after any `quiz-data.js` content change, regenerate it
  (see 1.3) so the two stay identical. It is still NOT loaded at runtime (file:// constraint).
- Content inventory after this session: Unit 0 complete (8 micro videos / 40 items + 30
  mastery), Unit 1 complete (13 micro videos / 65 items + 30 mastery). Totals in source:
  21 micro_practice keys, 2 unit_mastery keys.
- Copy rule re-affirmed and now consistent across Units 0 and 1: no em-dashes, no
  ampersands; KaTeX double-escaped in JS strings (e.g. `"$\\\\frac{dy}{dx}$"`).
- Verification this session was static only (content change, no UI change): load `QUIZ_DATA`
  via the eval helper above and assert per quiz: exactly 5 (micro) / 30 (mastery) items,
  exactly one `correct:true` per item, 4 options each, `hint` and `prompt` present, unique
  IDs, and a scan for `—` and `&` returning zero. Browser MCP was not exercised.

---

## 3. Immediate Next Actions

### 3.1 Content backlog (primary)
- micro_practice: 229 videos remain (250 total minus 21 done across Units 0 and 1).
- unit_mastery: 17 units remain (Units 2 .. 18); Units 0 and 1 done.
- Recommended next batch: `"Unit 2: First Order Linear Differential Equations"`. Pull its
  video_ids and exact unit title from `app/js/curriculum-data.js`. Unit 2 modules:
  2.1 Separable, 2.2 Integrating Factors, 2.3 IVPs, 2.4 Homogeneous/Substitution,
  2.5 Applications (cooling, growth/decay). This is a solving-technique unit, so apply the
  >=3-computational-items-per-video rule and include worked KaTeX equations in mastery.

### 3.2 Per-batch checklist (apply every time)
1. Append to `app/js/quiz-data.js` only; do not overwrite existing keys. Schema per prior
   log section 2.2. Unique IDs `mp_<video_id>_<n>` and `um_<unit>_<n>`.
2. Enforce copy rule (no em-dashes, no ampersands); double-backslash KaTeX.
3. Run the static integrity + copy-rule scan (section 2 above).
4. Regenerate `app/data/quizzes.json` (throwaway `_gen_mirror.js`, delete after) and confirm
   `mirror deep-equals source`.
5. Commit staging ONLY the two quiz files. Suggested message pattern:
   "feat: generate Unit N micro-practice quizzes ..." / "... unit-mastery quiz ...".
   Push `origin master` when asked.

### 3.3 Git State at session close
- HEAD `5954750` on `master`, in sync with `origin/master`.
- Pre-existing, UNRELATED, still-unstaged working-tree changes (do NOT bundle with quiz work):
  - Deleted: `Differential Equations Whiteboard Notes/Diff. EQ 5 - 1st Order Linear ODE's - Gabriel.pdf`
  - Untracked: `1st & 2nd Order ODE Cheat Sheet & Review Guide.pdf`
  - Untracked: `Differential Equations Whiteboard Notes/Diff. EQ 5 - 2nd Order Linear ODE's - Gabriel.pdf`
- Git warns LF -> CRLF on the two quiz files (Windows autocrlf); harmless.

---
---

# Session State, ODE Student Roadmap

Last updated: 2026-06-13. Branch: `master`. Pushed HEAD: `7cbe89a`.
Scope of this session: (A) recovered a blocked git push, (B) designed and built a
vanilla JS multiple choice quiz subsystem, (C) generated Unit 0 quiz content.

All quiz subsystem work and all generated content are CURRENTLY UNCOMMITTED in the
working tree (see Git State). Nothing from section B or C has been committed or pushed.

---

## 1. Completed Tasks and File Modifications

### 1.1 Git push recovery (committed, pushed)
- `git add .` had staged 337 files into commit `de7e640`, including two files over
  GitHub's 100 MB hard limit, which caused `pre-receive hook declined`:
  - `A Course in Differential Equations ... [Swift ...].pdf` (101.37 MB)
  - `Differential Equations Whiteboard Notes/Tutoring Gabriel Introduction to Diff. Eq. [05-22-2026].mp4` (372.46 MB)
- Also swept in unintentionally: `node_modules/` (~180 files) and `ODE-Manager-Local/credentials.json` (secrets). Neither reached the remote (push failed before upload).
- Resolution: updated `.gitignore`, `git reset --mixed d22e350`, re-staged, recommitted
  as `7cbe89a`, pushed successfully (`d22e350..7cbe89a master`). File count 337 -> 149.
- `.gitignore` additions: `credentials.json`, `node_modules/`, `*.mp4`, and
  `A Course in Differential Equations with Boundary Value Problems*.pdf` (glob, not the
  literal bracketed name, because `[...]` is a gitignore character class).

### 1.2 Quiz subsystem (UNCOMMITTED)
New files:
- `app/js/quiz-engine.js` — `QuizEngine` IIFE, single public method `mount(container, config)`.
- `app/js/quiz-data.js` — `QUIZ_DATA` global, the runtime content store.

Modified files:
- `app/js/state.js` — added `KEYS.quizProgress = "ode_quiz_progress"`; added methods
  `getQuizProgress(quizId)`, `setQuizAnswerCorrect(quizId, questionId)`,
  `clearQuizProgress(quizId)`; added `quizProgress` removal to `resetAllProgress()`.
- `app/js/router.js` — two injection points (see 2.3).
- `app/index.html` — added `<script src="js/quiz-data.js">` then
  `<script src="js/quiz-engine.js">` immediately before `js/router.js`.
- `app/css/main.css` — full quiz style block (launch card, modal, options, inline
  rationales, hint toggle, summary, retry).
- `app/css/theme.css` — added `--error-color` (light `#b3261e`, dark `#f2746b`).
- `app/data/quizzes.json` — documentation mirror only, not loaded at runtime.

### 1.3 UX iterations applied to the engine (final state reflects all of these)
1. v1: questions rendered inline beneath each video (superseded).
2. v2: replaced with launch card plus full-screen modal overlay, one question at a time.
3. v3: added general hint toggle, per-option inline rationales, post-quiz summary screen.
4. v4: added "Retry Quiz" button on the summary screen.
- Bug found and fixed via browser verification: `openModal` built the overlay but never
  attached it; fix added `document.body.appendChild(overlay)`.
- Restore behavior: a previously solved question re-renders with the correct option marked
  and its confirming rationale shown (not a reveal, it was already answered correctly).

### 1.4 Content generated (UNCOMMITTED)
- Unit 0 micro_practice: 8 videos x 5 questions = 40 items. IDs `mp_<video_id>_<n>`.
  video_ids: `Em339AlejIs`, `R2QtleY5asQ`, `B5IjsTONKkw`, `ghjOS7Q82s0`,
  `p_di4Zn4wz4`, `9fQkLQZe3u8`, `ifbaAqfqpc4`, `zm_UqjVLViU`.
  (The original demo on `Em339AlejIs` was promoted to final content, IDs `demo_*` -> `mp_*`.)
- Unit 0 unit_mastery: key `"Unit 0: What are Differential Equations"`, 30 items,
  IDs `um_0_1` .. `um_0_30`, spanning the full unit (definition, order, solving and
  verification, families and initial conditions, general vs particular, slope fields,
  ODE vs PDE, linearity, determinism, qualitative analysis, dynamical systems, phase
  space, equilibrium, modeling, Newton's second law, dP/dt=kP, dN/dt=-lambda N,
  Newton's cooling, shared exponential structure).

---

## 2. Architecture State

### 2.1 Runtime data loading model (critical constraint)
The app can be opened from `file://`, where `fetch()` of local JSON is blocked. Therefore
ALL data is delivered as plain `<script>` globals, never fetched:
- `app/js/curriculum-data.js` defines `const CURRICULUM_DATA = {...}`, then derives
  `const CURRICULUM = CURRICULUM_DATA.curriculum` and `const ALL_MODULES = [...]`.
- `app/js/quiz-data.js` defines `const QUIZ_DATA = {...}`.
- `app/data/curriculum.json` and `app/data/quizzes.json` are stale documentation mirrors,
  NOT loaded at runtime. Do not rely on them; edit the `.js` globals.
- Script load order in `index.html` matters: curriculum-data, state, theme, modes,
  checkpoint-registry, checkpoint-core, widgets-unit0..unit12, quiz-data, quiz-engine, router.
- Pre-existing gap (out of scope, unrelated to quizzes): `widgets-unit13.js` and
  `widgets-unit14.js` exist on disk but are NOT referenced in `index.html`.

### 2.2 QUIZ_DATA schema (single source of truth)
```
QUIZ_DATA = {
  micro_practice: { "<video_id>": [ Item x5 ], ... },   // keyed by video_id
  unit_mastery:   { "<exact unit title>": [ Item x30 ], ... } // keyed by unit title
}
Item = {
  id:      string, stable, unique, used as the persistence sub-key
  prompt:  string, may contain KaTeX ($inline$ and $$display$$)
  hint:    string, the general hint revealed by the "Need a hint?" toggle
  answerOptions: [ { text, correct?, rationale } ]   // exactly one correct:true
}
```
- Exactly one option has `correct: true`. Every option (correct and incorrect) carries a
  `rationale`. Correct rationale confirms; incorrect rationale is a first-principles guiding
  question that NEVER names or reveals the correct option (the "Math Confidence Reset").
- `answerOptions` count is 4 in current content (engine tolerates >=2; integrity check
  enforces 4 for mastery, >=3 for micro).

### 2.3 router.js injection points
- Per-video micro practice: inside `buildModuleSection`, within the existing
  `if (unlocked)` block, inside `moduleData.videos.forEach`, appended into each
  `li.video-card` after the watch label. Reads `QUIZ_DATA.micro_practice[video.video_id]`.
  Mounts with `config.id = "micro::" + video.video_id`, host class `video-quiz-host`.
- Unit mastery: in `renderCurriculum`, after `unitSection.appendChild(moduleList)` and
  before `container.appendChild(unitSection)`, so it is the last element of the unit.
  Reads `QUIZ_DATA.unit_mastery[unitData.unit]`. Mounts with
  `config.id = "mastery::" + unitData.unit`, host class `unit-mastery-host`.
- Both guarded by `if (items && items.length)`, so empty/absent keys render nothing.

### 2.4 QuizEngine internal structure (app/js/quiz-engine.js)
- Public: `mount(container, config)` where
  `config = { id, title, intro?, items }`. Renders a launch card only.
- Launch card: title, optional intro, progress line `Progress: X of N complete`, and one
  button labeled `Launch Practice` / `Resume Practice` (partial) / `Review Practice` (all done).
- `openModal(config, live, refresh)`: builds overlay appended to `document.body`, sets
  `body.quiz-modal-open` (scroll lock). Header has title, live `Score: X of N`, and
  `Exit Practice`. Closes via Exit button, Escape key, or backdrop click; close() calls refresh.
- Pacing: opens at first unsolved index (else 0 for review). One question at a time.
- Per question: counter `Question i of N`, prompt, optional `Need a hint?` toggle, options
  as `.quiz-option-row` (button + hidden `.quiz-rationale`). Wrong click -> mark `.incorrect`,
  disable, fill that option's rationale `incorrect` (red), no advance, score unchanged.
  Correct click -> mark `.correct`, lock all, persist, increment score, reveal Next.
- Footer button: `Next Question`, or `Finish` on the last item. Finish -> `renderSummary()`.
- `renderSummary()`: clears body, shows `Practice Complete`, `You answered X of N correctly.`,
  a concluding message, and footer buttons `Retry Quiz` then `Close Practice`.
- `restart()` (Retry Quiz): `ODEState.clearQuizProgress(config.id)`, `live.solved.length = 0`,
  `index = 0`, `updateScore()`, `renderQuestion()`. Restarts in place, modal stays open.
- KaTeX via local `renderMath(el)` calling `renderMathInElement` with delimiters
  `$$`(display) and `$`(inline). Mirrors checkpoint-core.js.

### 2.5 Persistence (app/js/state.js, ODEState)
- localStorage key `ode_quiz_progress` -> `{ [quizId]: [solvedQuestionId, ...] }`.
- quizId is the engine `config.id` (`micro::<video_id>` or `mastery::<unit title>`).
- `getQuizProgress(quizId) -> string[]`, `setQuizAnswerCorrect(quizId, qid)` (dedup push),
  `clearQuizProgress(quizId)` (delete one key only, leaves others intact),
  `resetAllProgress()` now also clears `ode_quiz_progress`.

### 2.6 CSS (app/css/main.css), classes added
Launch: `.video-quiz-host`, `.unit-mastery-host`, `.quiz-launch-card`, `.quiz-title`,
`.quiz-intro`, `.quiz-progress`, `.quiz-launch-btn`.
Modal: `body.quiz-modal-open`, `.quiz-modal-overlay` (fixed, z-index 1000), `.quiz-modal`,
`.quiz-modal-header`, `.quiz-modal-title`, `.quiz-score`, `.quiz-modal-close`,
`.quiz-modal-body`, `.quiz-counter`, `.quiz-prompt`.
Hint: `.quiz-hint-toggle`, `.quiz-hint`, `.quiz-hint-text`.
Options: `.quiz-option-row`, `.quiz-option`, `.quiz-option.correct`, `.quiz-option.incorrect`.
Rationale: `.quiz-rationale`, `.quiz-rationale.correct`, `.quiz-rationale.incorrect`,
`.quiz-rationale-text`.
Summary/footer: `.quiz-summary`, `.quiz-summary-title`, `.quiz-summary-score`,
`.quiz-summary-message`, `.quiz-modal-footer`, `.quiz-next-btn`, `.quiz-retry-btn`.
All colors use theme.css variables; `--error-color` was added for the incorrect state.

### 2.7 Hard constraints (enforce on all future content and UI strings)
- COPY RULE: no em-dashes and no ampersands in any user-facing string. Use commas and the
  word "and". (The only `&` permitted in source is the `&&` operator in code.)
- Math Confidence Reset: incorrect rationales must not reveal the correct answer.
- KaTeX strings in a JS file require double backslashes, e.g. `"$\\frac{dP}{dt} = kP$"`.

### 2.8 Dependencies
- No new build tooling or npm runtime deps for the quiz subsystem. KaTeX (0.16.8) and the
  existing CDN scripts already present in index.html. `node_modules/` (playwright) exists
  locally but is gitignored and unrelated to the app runtime.

### 2.9 Verification method used (reproduce next session)
- Local static server: `python -m http.server 8765` from `app/` (run in background).
  Browser MCP blocks `file://`, so HTTP is required. Stop the listener on 8765 when done.
- Browser MCP (`mcp__browser-test__*`) drove real DOM interactions and asserted behavior.
  Note: KaTeX rewrites option `textContent`, so match options by clicking enabled buttons
  until Next/Finish appears rather than by text equality.
- Static checks: `node --check <file>` for syntax; load `QUIZ_DATA` via
  `new Function(src.replace("const QUIZ_DATA =","return "))()` to run integrity checks
  (count, single correct, every option has text+rationale, hint present, unique ids) and a
  copy-rule scan (`/[–—]/` and `/&/` after stripping `&&`).
- Current content passed all checks; console shows only a benign `favicon.ico` 404.

---

## 3. Immediate Next Actions

### 3.1 Content generation remaining (primary backlog)
Curriculum has 19 units (Unit 0 .. Unit 18) and 250 videos total. Unit 0 is fully done
(40 micro + 30 mastery). Remaining:
- micro_practice: 242 videos still need a 5-question quiz each, keyed by `video_id`.
- unit_mastery: Units 1 .. 18 still need a 30-question quiz each, keyed by exact unit title.
- Recommended next batch (pending user choice of order): Unit 1 ("Unit 1: Foundations and
  Prerequisites") micro-practice for each of its videos, then its 30-question mastery quiz.
- Pull exact video_ids and unit titles from `app/js/curriculum-data.js` (authoritative).
  Unit 1 covers calculus review, the number e, Euler's formula, plus order/degree/linearity
  and solution vocabulary, so mastery there can include light algebra and KaTeX.
- Append content into `app/js/quiz-data.js` only (the `.js` global). Optionally keep
  `app/data/quizzes.json` mirror minimal; it is documentation, not runtime.

### 3.2 Per-batch checklist (apply every time)
1. Write items on the 2.2 schema. Exactly one `correct`; rationale on every option;
   `hint` per item; unique `id` (`mp_<video_id>_<n>` or `um_<unit>_<n>`).
2. Enforce copy rule (no em-dashes, no ampersands). Double-backslash KaTeX.
3. `node --check app/js/quiz-data.js` then run the integrity + copy-rule scan from 2.9.
4. Serve on 8765 and render-verify in the browser MCP (mount, score, summary, KaTeX, console).

### 3.3 Source control (not yet done)
- All section 1.2 and 1.3 changes are uncommitted. When ready, commit the quiz subsystem
  and Unit 0 content on `master`. Suggested message: "feat: add interactive quiz engine and
  Unit 0 micro-practice and mastery content".
- Working tree at session close:
  - Modified: `.gitignore`, `app/css/main.css`, `app/css/theme.css`, `app/data/quizzes.json`,
    `app/index.html`, `app/js/router.js`, `app/js/state.js`.
  - Untracked: `app/js/quiz-data.js`, `app/js/quiz-engine.js`.
  - Playwright MCP may have written `app/.playwright-mcp/` artifacts; gitignore or remove
    before committing if present.

### 3.4 Optional, unrelated cleanup (do not bundle with quiz work)
- `index.html` does not load `widgets-unit13.js` / `widgets-unit14.js`. Pre-existing; fix
  only if checkpoint behavior for Units 13/14 is being addressed.

## Session 2026-06-27 (normalization)
Unit 13 and 14 standardized to full asset suite.

## Session 2026-06-27 (Unit 15 build)
Unit 15 ("Systems of Linear Differential Equations") full 3-asset suite built and compiled.
- Authored `scripts/unit15_data.json` (5 modules + 10-problem practice/solutions block).
- Rendered Unit-15-Cheat-Sheet.tex, Unit-15-Practice-Set.tex, Unit-15-Reference-Guide.tex
  via `render_latex.py --unit 15 --mode cheat|practice|master`.
- Content focus: MethodBox decoupling (A=PDP^{-1}, y=P^{-1}x -> y'=Dy) in 15.2; matrix
  exponential e^{At}=Pe^{Dt}P^{-1} as formulaboxes in 15.3 (no TheoryBox in the 6-box
  guide); one worked matrix A=[[1,2],[3,2]] (lambda=4,-1) threaded through 15.2->15.3->15.4
  with P, P^{-1}, D, e^{At}, and the fundamental matrix all computed in full.
- Compiled all three from repo-root copies (tectonic --outdir app/assets/pdfs/), clean, no debris.
- Logged Unit 15 retrospective in PIPELINE_LEARNINGS.md.



### Milestone: Unit 16 (Phase Plane Analysis) Completed [2026-06-27]

- **Status:** Standardized Full Asset Suite (Cheat/Practice/Master) built and pushed (Commit 76f1081).

- **Architecture Notes:** - Renderer logic `render_latex.py` confirmed robust for `--unit 16`.

    - Preamble pathing resolved via root-copy/compile/purge workflow.

    - `psmallmatrix` replaced with `\left(\begin{smallmatrix}...\end{smallmatrix}\right)` for `amsmath` compatibility.

- **Next Unit:** Unit 17 (Numerical Methods and Stability).




### Milestone: Unit 17 (BVP & Sturm–Liouville) Completed [2026-06-27]

- **Status:** Standardized Full Asset Suite (Cheat/Practice/Master) built and pushed (Commit 01ba4b3).

- **Architecture Notes:** - Resolved `]` parser collision in `tcolorbox` titles.

    - Successfully standardized BVP/Sturm–Liouville orthogonal basis derivation.

- **Next Unit:** Unit 18 (Partial Differential Equations).

