# Session State, ODE Student Roadmap

Last updated: 2026-06-13. Branch: `master`. Pushed HEAD: `7cbe89a`.
Scope of this session: (A) recovered a blocked git push, (B) designed and built a
vanilla JS multiple choice quiz subsystem, (C) generated Unit 0 quiz content.

All quiz subsystem work and all generated content are CURRENTLY UNCOMMITTED in the
working tree (see Git State). Nothing from section B or C has been committed or pushed.

---

## 1. Completed Tasks and File Modifications

### 1.1 Git push recovery (committed, pushed)
- `git add .` had staged 337 files into commit `de7e640`, including two files over
  GitHub's 100 MB hard limit, which caused `pre-receive hook declined`:
  - `A Course in Differential Equations ... [Swift ...].pdf` (101.37 MB)
  - `Differential Equations Whiteboard Notes/Tutoring Gabriel Introduction to Diff. Eq. [05-22-2026].mp4` (372.46 MB)
- Also swept in unintentionally: `node_modules/` (~180 files) and `ODE-Manager-Local/credentials.json` (secrets). Neither reached the remote (push failed before upload).
- Resolution: updated `.gitignore`, `git reset --mixed d22e350`, re-staged, recommitted
  as `7cbe89a`, pushed successfully (`d22e350..7cbe89a master`). File count 337 -> 149.
- `.gitignore` additions: `credentials.json`, `node_modules/`, `*.mp4`, and
  `A Course in Differential Equations with Boundary Value Problems*.pdf` (glob, not the
  literal bracketed name, because `[...]` is a gitignore character class).

### 1.2 Quiz subsystem (UNCOMMITTED)
New files:
- `app/js/quiz-engine.js` — `QuizEngine` IIFE, single public method `mount(container, config)`.
- `app/js/quiz-data.js` — `QUIZ_DATA` global, the runtime content store.

Modified files:
- `app/js/state.js` — added `KEYS.quizProgress = "ode_quiz_progress"`; added methods
  `getQuizProgress(quizId)`, `setQuizAnswerCorrect(quizId, questionId)`,
  `clearQuizProgress(quizId)`; added `quizProgress` removal to `resetAllProgress()`.
- `app/js/router.js` — two injection points (see 2.3).
- `app/index.html` — added `<script src="js/quiz-data.js">` then
  `<script src="js/quiz-engine.js">` immediately before `js/router.js`.
- `app/css/main.css` — full quiz style block (launch card, modal, options, inline
  rationales, hint toggle, summary, retry).
- `app/css/theme.css` — added `--error-color` (light `#b3261e`, dark `#f2746b`).
- `app/data/quizzes.json` — documentation mirror only, not loaded at runtime.

### 1.3 UX iterations applied to the engine (final state reflects all of these)
1. v1: questions rendered inline beneath each video (superseded).
2. v2: replaced with launch card plus full-screen modal overlay, one question at a time.
3. v3: added general hint toggle, per-option inline rationales, post-quiz summary screen.
4. v4: added "Retry Quiz" button on the summary screen.
- Bug found and fixed via browser verification: `openModal` built the overlay but never
  attached it; fix added `document.body.appendChild(overlay)`.
- Restore behavior: a previously solved question re-renders with the correct option marked
  and its confirming rationale shown (not a reveal, it was already answered correctly).

### 1.4 Content generated (UNCOMMITTED)
- Unit 0 micro_practice: 8 videos x 5 questions = 40 items. IDs `mp_<video_id>_<n>`.
  video_ids: `Em339AlejIs`, `R2QtleY5asQ`, `B5IjsTONKkw`, `ghjOS7Q82s0`,
  `p_di4Zn4wz4`, `9fQkLQZe3u8`, `ifbaAqfqpc4`, `zm_UqjVLViU`.
  (The original demo on `Em339AlejIs` was promoted to final content, IDs `demo_*` -> `mp_*`.)
- Unit 0 unit_mastery: key `"Unit 0: What are Differential Equations"`, 30 items,
  IDs `um_0_1` .. `um_0_30`, spanning the full unit (definition, order, solving and
  verification, families and initial conditions, general vs particular, slope fields,
  ODE vs PDE, linearity, determinism, qualitative analysis, dynamical systems, phase
  space, equilibrium, modeling, Newton's second law, dP/dt=kP, dN/dt=-lambda N,
  Newton's cooling, shared exponential structure).

---

## 2. Architecture State

### 2.1 Runtime data loading model (critical constraint)
The app can be opened from `file://`, where `fetch()` of local JSON is blocked. Therefore
ALL data is delivered as plain `<script>` globals, never fetched:
- `app/js/curriculum-data.js` defines `const CURRICULUM_DATA = {...}`, then derives
  `const CURRICULUM = CURRICULUM_DATA.curriculum` and `const ALL_MODULES = [...]`.
- `app/js/quiz-data.js` defines `const QUIZ_DATA = {...}`.
- `app/data/curriculum.json` and `app/data/quizzes.json` are stale documentation mirrors,
  NOT loaded at runtime. Do not rely on them; edit the `.js` globals.
- Script load order in `index.html` matters: curriculum-data, state, theme, modes,
  checkpoint-registry, checkpoint-core, widgets-unit0..unit12, quiz-data, quiz-engine, router.
- Pre-existing gap (out of scope, unrelated to quizzes): `widgets-unit13.js` and
  `widgets-unit14.js` exist on disk but are NOT referenced in `index.html`.

### 2.2 QUIZ_DATA schema (single source of truth)
```
QUIZ_DATA = {
  micro_practice: { "<video_id>": [ Item x5 ], ... },   // keyed by video_id
  unit_mastery:   { "<exact unit title>": [ Item x30 ], ... } // keyed by unit title
}
Item = {
  id:      string, stable, unique, used as the persistence sub-key
  prompt:  string, may contain KaTeX ($inline$ and $$display$$)
  hint:    string, the general hint revealed by the "Need a hint?" toggle
  answerOptions: [ { text, correct?, rationale } ]   // exactly one correct:true
}
```
- Exactly one option has `correct: true`. Every option (correct and incorrect) carries a
  `rationale`. Correct rationale confirms; incorrect rationale is a first-principles guiding
  question that NEVER names or reveals the correct option (the "Math Confidence Reset").
- `answerOptions` count is 4 in current content (engine tolerates >=2; integrity check
  enforces 4 for mastery, >=3 for micro).

### 2.3 router.js injection points
- Per-video micro practice: inside `buildModuleSection`, within the existing
  `if (unlocked)` block, inside `moduleData.videos.forEach`, appended into each
  `li.video-card` after the watch label. Reads `QUIZ_DATA.micro_practice[video.video_id]`.
  Mounts with `config.id = "micro::" + video.video_id`, host class `video-quiz-host`.
- Unit mastery: in `renderCurriculum`, after `unitSection.appendChild(moduleList)` and
  before `container.appendChild(unitSection)`, so it is the last element of the unit.
  Reads `QUIZ_DATA.unit_mastery[unitData.unit]`. Mounts with
  `config.id = "mastery::" + unitData.unit`, host class `unit-mastery-host`.
- Both guarded by `if (items && items.length)`, so empty/absent keys render nothing.

### 2.4 QuizEngine internal structure (app/js/quiz-engine.js)
- Public: `mount(container, config)` where
  `config = { id, title, intro?, items }`. Renders a launch card only.
- Launch card: title, optional intro, progress line `Progress: X of N complete`, and one
  button labeled `Launch Practice` / `Resume Practice` (partial) / `Review Practice` (all done).
- `openModal(config, live, refresh)`: builds overlay appended to `document.body`, sets
  `body.quiz-modal-open` (scroll lock). Header has title, live `Score: X of N`, and
  `Exit Practice`. Closes via Exit button, Escape key, or backdrop click; close() calls refresh.
- Pacing: opens at first unsolved index (else 0 for review). One question at a time.
- Per question: counter `Question i of N`, prompt, optional `Need a hint?` toggle, options
  as `.quiz-option-row` (button + hidden `.quiz-rationale`). Wrong click -> mark `.incorrect`,
  disable, fill that option's rationale `incorrect` (red), no advance, score unchanged.
  Correct click -> mark `.correct`, lock all, persist, increment score, reveal Next.
- Footer button: `Next Question`, or `Finish` on the last item. Finish -> `renderSummary()`.
- `renderSummary()`: clears body, shows `Practice Complete`, `You answered X of N correctly.`,
  a concluding message, and footer buttons `Retry Quiz` then `Close Practice`.
- `restart()` (Retry Quiz): `ODEState.clearQuizProgress(config.id)`, `live.solved.length = 0`,
  `index = 0`, `updateScore()`, `renderQuestion()`. Restarts in place, modal stays open.
- KaTeX via local `renderMath(el)` calling `renderMathInElement` with delimiters
  `$$`(display) and `$`(inline). Mirrors checkpoint-core.js.

### 2.5 Persistence (app/js/state.js, ODEState)
- localStorage key `ode_quiz_progress` -> `{ [quizId]: [solvedQuestionId, ...] }`.
- quizId is the engine `config.id` (`micro::<video_id>` or `mastery::<unit title>`).
- `getQuizProgress(quizId) -> string[]`, `setQuizAnswerCorrect(quizId, qid)` (dedup push),
  `clearQuizProgress(quizId)` (delete one key only, leaves others intact),
  `resetAllProgress()` now also clears `ode_quiz_progress`.

### 2.6 CSS (app/css/main.css), classes added
Launch: `.video-quiz-host`, `.unit-mastery-host`, `.quiz-launch-card`, `.quiz-title`,
`.quiz-intro`, `.quiz-progress`, `.quiz-launch-btn`.
Modal: `body.quiz-modal-open`, `.quiz-modal-overlay` (fixed, z-index 1000), `.quiz-modal`,
`.quiz-modal-header`, `.quiz-modal-title`, `.quiz-score`, `.quiz-modal-close`,
`.quiz-modal-body`, `.quiz-counter`, `.quiz-prompt`.
Hint: `.quiz-hint-toggle`, `.quiz-hint`, `.quiz-hint-text`.
Options: `.quiz-option-row`, `.quiz-option`, `.quiz-option.correct`, `.quiz-option.incorrect`.
Rationale: `.quiz-rationale`, `.quiz-rationale.correct`, `.quiz-rationale.incorrect`,
`.quiz-rationale-text`.
Summary/footer: `.quiz-summary`, `.quiz-summary-title`, `.quiz-summary-score`,
`.quiz-summary-message`, `.quiz-modal-footer`, `.quiz-next-btn`, `.quiz-retry-btn`.
All colors use theme.css variables; `--error-color` was added for the incorrect state.

### 2.7 Hard constraints (enforce on all future content and UI strings)
- COPY RULE: no em-dashes and no ampersands in any user-facing string. Use commas and the
  word "and". (The only `&` permitted in source is the `&&` operator in code.)
- Math Confidence Reset: incorrect rationales must not reveal the correct answer.
- KaTeX strings in a JS file require double backslashes, e.g. `"$\\frac{dP}{dt} = kP$"`.

### 2.8 Dependencies
- No new build tooling or npm runtime deps for the quiz subsystem. KaTeX (0.16.8) and the
  existing CDN scripts already present in index.html. `node_modules/` (playwright) exists
  locally but is gitignored and unrelated to the app runtime.

### 2.9 Verification method used (reproduce next session)
- Local static server: `python -m http.server 8765` from `app/` (run in background).
  Browser MCP blocks `file://`, so HTTP is required. Stop the listener on 8765 when done.
- Browser MCP (`mcp__browser-test__*`) drove real DOM interactions and asserted behavior.
  Note: KaTeX rewrites option `textContent`, so match options by clicking enabled buttons
  until Next/Finish appears rather than by text equality.
- Static checks: `node --check <file>` for syntax; load `QUIZ_DATA` via
  `new Function(src.replace("const QUIZ_DATA =","return "))()` to run integrity checks
  (count, single correct, every option has text+rationale, hint present, unique ids) and a
  copy-rule scan (`/[–—]/` and `/&/` after stripping `&&`).
- Current content passed all checks; console shows only a benign `favicon.ico` 404.

---

## 3. Immediate Next Actions

### 3.1 Content generation remaining (primary backlog)
Curriculum has 19 units (Unit 0 .. Unit 18) and 250 videos total. Unit 0 is fully done
(40 micro + 30 mastery). Remaining:
- micro_practice: 242 videos still need a 5-question quiz each, keyed by `video_id`.
- unit_mastery: Units 1 .. 18 still need a 30-question quiz each, keyed by exact unit title.
- Recommended next batch (pending user choice of order): Unit 1 ("Unit 1: Foundations and
  Prerequisites") micro-practice for each of its videos, then its 30-question mastery quiz.
- Pull exact video_ids and unit titles from `app/js/curriculum-data.js` (authoritative).
  Unit 1 covers calculus review, the number e, Euler's formula, plus order/degree/linearity
  and solution vocabulary, so mastery there can include light algebra and KaTeX.
- Append content into `app/js/quiz-data.js` only (the `.js` global). Optionally keep
  `app/data/quizzes.json` mirror minimal; it is documentation, not runtime.

### 3.2 Per-batch checklist (apply every time)
1. Write items on the 2.2 schema. Exactly one `correct`; rationale on every option;
   `hint` per item; unique `id` (`mp_<video_id>_<n>` or `um_<unit>_<n>`).
2. Enforce copy rule (no em-dashes, no ampersands). Double-backslash KaTeX.
3. `node --check app/js/quiz-data.js` then run the integrity + copy-rule scan from 2.9.
4. Serve on 8765 and render-verify in the browser MCP (mount, score, summary, KaTeX, console).

### 3.3 Source control (not yet done)
- All section 1.2 and 1.3 changes are uncommitted. When ready, commit the quiz subsystem
  and Unit 0 content on `master`. Suggested message: "feat: add interactive quiz engine and
  Unit 0 micro-practice and mastery content".
- Working tree at session close:
  - Modified: `.gitignore`, `app/css/main.css`, `app/css/theme.css`, `app/data/quizzes.json`,
    `app/index.html`, `app/js/router.js`, `app/js/state.js`.
  - Untracked: `app/js/quiz-data.js`, `app/js/quiz-engine.js`.
  - Playwright MCP may have written `app/.playwright-mcp/` artifacts; gitignore or remove
    before committing if present.

### 3.4 Optional, unrelated cleanup (do not bundle with quiz work)
- `index.html` does not load `widgets-unit13.js` / `widgets-unit14.js`. Pre-existing; fix
  only if checkpoint behavior for Units 13/14 is being addressed.
